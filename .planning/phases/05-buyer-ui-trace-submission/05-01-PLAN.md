---
phase: 05-buyer-ui-trace-submission
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - services/ai/tests/test_file_extract.py
  - services/ai/tests/test_input_wrap.py
  - docs/qa/phase5-playwright.spec.ts
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02

must_haves:
  truths:
    - "test_file_extract.py imports resolve and all stubs raise NotImplementedError (RED)"
    - "test_input_wrap.py imports resolve and stub raises NotImplementedError (RED)"
    - "phase5-playwright.spec.ts exists and covers the full 5-screen buyer journey"
  artifacts:
    - path: "services/ai/tests/test_file_extract.py"
      provides: "Unit test stubs for POST /extract/file-text dispatcher (PDF/DOCX/XLSX/PPTX)"
      contains: "class TestFileExtractRoute"
    - path: "services/ai/tests/test_input_wrap.py"
      provides: "Unit test stub for POST /input/raw-text → valid VendorResponse"
      contains: "test_raw_text_wrap_returns_valid_vendor_response"
    - path: "docs/qa/phase5-playwright.spec.ts"
      provides: "Playwright E2E spec for the full buyer journey"
      contains: "test('RFQ Overview loads'"
  key_links:
    - from: "services/ai/tests/test_file_extract.py"
      to: "services/ai/api/app.py"
      via: "TestClient(app)"
      pattern: "from api.app import app"
    - from: "services/ai/tests/test_input_wrap.py"
      to: "services/ai/schemas/domain.py"
      via: "VendorResponse.model_validate"
      pattern: "from schemas.domain import VendorResponse"
---

<objective>
Create the Nyquist test infrastructure before any implementation. All stubs are RED (raise
NotImplementedError or are skip-marked) so subsequent plans go GREEN against real targets.
Playwright spec exists so the phase-gate E2E can run without scaffolding under deadline.

Purpose: Nyquist contract — per 05-VALIDATION.md, INPUT-01 and INPUT-02 must have failing
tests before their backend is written. Wave 0 is a blocking precondition for Wave 1.
Output: Three test files. No production code yet.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-VALIDATION.md

<interfaces>
From services/ai/tests/test_sse_demo.py (analog for test structure):
- `from fastapi.testclient import TestClient`
- `from api.app import app` — no context manager, skips lifespan (avoids OpenAI ping)
- `client = TestClient(app, raise_server_exceptions=True)`
- Class-based test structure: `class TestSomething: def test_x(self) -> None: ...`

From services/ai/schemas/domain.py:
- `class VendorResponse(BaseModel):`
  - `vendor_name: str`, `persona: str`, `mess_spec: list[MessSpecItem]`
  - `source_id: str`, `format_label: str`, `raw_text: str`
- `VendorResponse.model_validate(dict)` for round-trip assertion

Planned new routes (created in Plan 05-02 — reference here for stub alignment):
- `POST /extract/file-text` — UploadFile → `{"text": str, "filename": str, "chars": int}`
- `POST /input/raw-text` — `{"vendor_name": str, "raw_text": str}` → VendorResponse JSON
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED stubs for file-text extraction and raw-text wrap endpoints</name>
  <files>services/ai/tests/test_file_extract.py, services/ai/tests/test_input_wrap.py</files>
  <read_first>
    - services/ai/tests/test_sse_demo.py (lines 1–50: TestClient setup, class structure, fixture-free pattern)
    - services/ai/tests/test_sample_fixtures.py (lines 1–30: repo_root import, DATA_DIR pattern)
    - services/ai/api/app.py (lines 26–43: imports, existing route signatures)
    - services/ai/schemas/domain.py (lines 104–120: VendorResponse fields)
  </read_first>
  <action>
Create two test files following the test_sse_demo.py class-based structure (TestClient without
lifespan context manager).

test_file_extract.py — class TestFileExtractRoute with four test methods:
- test_pdf_returns_text_and_chars: POST /extract/file-text with a minimal synthetic PDF bytes,
  assert status 200, response has "text" (str) and "chars" (int) keys
- test_docx_returns_text: same pattern, synthetic DOCX bytes (empty is fine — best-effort)
- test_xlsx_returns_text: synthetic XLSX bytes
- test_pptx_returns_text: synthetic PPTX bytes
- test_weak_extraction_not_an_error: upload a tiny file, assert status 200 (NOT 422/500);
  "chars" < 200 is allowed — caller decides to show Alert, not the server

All four format tests: raise NotImplementedError (the route does not exist yet). Mark with
`pytest.raises(Exception)` or `pytest.mark.skip(reason="Wave 1 — route not yet implemented")`
so the collection does not crash but is clearly RED.

Use `io.BytesIO` to create in-memory file bytes for TestClient multipart upload:
`client.post("/extract/file-text", files={"file": ("test.pdf", b"%PDF-1.4 ...", "application/pdf")})`

test_input_wrap.py — single function test_raw_text_wrap_returns_valid_vendor_response:
- POST /input/raw-text with `{"vendor_name": "Test Vendor", "raw_text": "We offer..."}`
- assert status 200
- `VendorResponse.model_validate(r.json())` — proves output feeds /extract/vendor contract
- assert `vendor.vendor_name == "Test Vendor"` and `vendor.raw_text is not None`

Mark as `pytest.mark.skip(reason="Wave 1 — route not yet implemented")` so it is RED
(skipped = not green) but import-valid.

Both files: use `from __future__ import annotations`; import `pytest`, `TestClient`, `app`,
and the relevant schema. Follow the 05-PATTERNS.md FastAPI test pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_file_extract.py tests/test_input_wrap.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_file_extract.py tests/test_input_wrap.py --collect-only` exits 0 (imports resolve)
    - All collected tests are marked skip or xfail — zero PASSED, zero ERROR at import time
    - `from api.app import app` succeeds without an OpenAI key (TestClient, no lifespan)
    - `from schemas.domain import VendorResponse` is present in test_input_wrap.py
  </acceptance_criteria>
  <done>Both test files collected by pytest with all tests skipped (RED). No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E spec scaffold for the full buyer journey</name>
  <files>docs/qa/phase5-playwright.spec.ts</files>
  <read_first>
    - docs/qa/phase-02-functional-checklist.md (existing QA doc — follow the structure)
    - docs/assignment.md (§11 Playwright handoff requirement, §22 rubric items to cover)
    - apps/web/package.json (check if @playwright/test is already present)
    - package.json (monorepo root — check devDependencies for @playwright/test)
  </read_first>
  <action>
First, add @playwright/test to root devDependencies if not present:
`pnpm add -D -w @playwright/test`

Create docs/qa/phase5-playwright.spec.ts — a Playwright TypeScript spec covering the full
5-screen buyer journey per CLAUDE.md §11. This is a real spec (not pseudocode) that will
run against the live app in Wave 5.

Structure: one `test.describe('Phase 5 — Buyer Journey E2E')` block with these test cases:

1. `test('RFQ Overview loads committed data')`:
   - Navigate to /rfq
   - Assert page contains "RFQ" heading text and at least one line item name
   - Assert "Regenerate RFQ" button visible

2. `test('Vendor Input — sample load hero path (D-04)')`:
   - Navigate to /input
   - Assert three sample vendor cards visible (Thorough / Cheap / Fluff)
   - Click "Load Sample" on first card
   - Expect navigation to /extraction

3. `test('Extraction Review — gaps panel visible with evidence (D-07, D-08, UI-06)')`:
   - Navigate to /extraction (after loading a sample vendor)
   - Assert "Gaps & Risks" panel heading visible
   - Assert at least one FlagBadge (non-"present") is visible
   - Assert at least one "Source:" evidence snippet text visible

4. `test('Comparison — comparability matrix renders + no-rank framing (D-11, D-13, D-14)')`:
   - Navigate to /comparison
   - Assert comparability matrix table visible (vendors as columns, dimensions as rows)
   - Assert text "Data readiness" visible
   - Assert text "Comparability determined in code from evidence" visible

5. `test('Prompt Trace — trace selector and downgrade diff visible (D-15)')`:
   - Navigate to /trace
   - Assert tabs visible for trace files
   - Assert "Code overruled the model" text visible (or "code disproves" — either matches)

6. `test('Extraction Review — error state is explicit not blank (D-25)')`:
   - Navigate to /extraction with no vendors loaded
   - Assert empty-state message visible (not a blank screen)

Use `const BASE_URL = process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'` for
flexibility across local and deployed runs.

Use `page.goto`, `page.getByText`, `page.getByRole`, `page.waitForSelector` — standard
Playwright locators. Add `test.setTimeout(60_000)` for SSE-heavy tests.

Add a `playwright.config.ts` at the monorepo root if it does not already exist, pointing
baseURL to `http://localhost:3000`.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain && npx playwright test docs/qa/phase5-playwright.spec.ts --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --list` shows ≥6 test titles without parse errors
    - `@playwright/test` is present in root package.json devDependencies
    - Spec file imports from `@playwright/test` only (no other dependencies)
    - `const BASE_URL` is defined using env fallback pattern
  </acceptance_criteria>
  <done>Playwright spec with ≥6 tests listed. Spec does not run yet (app not built), but collection succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app.py | TestClient calls go through FastAPI routing in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | No new package installs this plan | accept | No new PyPI or npm packages in Wave 0 (test files only) |
</threat_model>

<verification>
- `cd services/ai && uv run pytest tests/test_file_extract.py tests/test_input_wrap.py -v` → all tests skipped, 0 errors
- `npx playwright test docs/qa/phase5-playwright.spec.ts --list` → 6+ tests listed
- `@playwright/test` present in root package.json devDependencies
</verification>

<success_criteria>
Both Python test files import cleanly and are collected as skipped (RED, not broken).
Playwright spec lists all buyer-journey test cases. Wave 1 can proceed.
</success_criteria>

<output>
Create `.planning/phases/05-buyer-ui-trace-submission/05-01-SUMMARY.md` when done.
</output>
