---
phase: 05-buyer-ui-trace-submission
plan: "09"
type: execute
wave: 5
depends_on:
  - "05-08"
files_modified:
  - docs/qa/phase5-playwright.spec.ts
  - docs/demo/
autonomous: false
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - SHIP-04

must_haves:
  truths:
    - "All 6 Playwright tests pass against the deployed (or local) running app"
    - "Evidence snippets are visible in the DOM on /extraction (not hidden by CSS)"
    - "Non-comparable vendors are visibly flagged before any scoring on /comparison"
    - "No fabricated numbers appear in extracted or comparison output"
    - "SSE progress is visible during extraction (not a blank wait then result)"
    - "UAT script is repeatable and captured in docs/qa/ for submission"
    - "SHIP-04: demo video recorded (≤5 min) and committed or linked in docs/demo/"
  artifacts:
    - path: "docs/qa/phase5-playwright.spec.ts"
      provides: "Complete Playwright E2E spec with passing tests"
      contains: "test('RFQ Overview loads'"
    - path: "docs/demo/"
      provides: "SHIP-04: demo video file or hosted link committed alongside demo-script.md"
      contains: "demo"
  key_links:
    - from: "docs/qa/phase5-playwright.spec.ts"
      to: "apps/web (deployed or local)"
      via: "PLAYWRIGHT_BASE_URL env var"
      pattern: "PLAYWRIGHT_BASE_URL"
---

<objective>
Run the Playwright E2E buyer journey end-to-end, fix any failures, produce the final
phase verification, and record the ≤5-min demo video. This is the handoff gate per
CLAUDE.md §11 — the phase is not done until Playwright passes and the demo video exists.

Purpose: Code-level tests (pytest) prove the backend. Playwright proves the product. The
UAT script and demo video are both submission artifacts.
Output: All 6+ Playwright tests passing. Demo video recorded and committed. Human confirms both.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/docs/assignment.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-VALIDATION.md

<interfaces>
From docs/qa/phase5-playwright.spec.ts (created in Plan 05-01):
  - 6 test cases covering the full buyer journey
  - BASE_URL = process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'
  - Tests: RFQ loads, sample load hero, gaps panel visible, comparability matrix, trace diff, empty state

Playwright (global v1.58.0):
  Run against local: `PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test docs/qa/phase5-playwright.spec.ts`
  Run against deployed: `PLAYWRIGHT_BASE_URL=https://[vercel-url] npx playwright test docs/qa/phase5-playwright.spec.ts`

Failure modes to watch for:
  - SSE timeout: test waits for result but SSE stream takes longer than test.setTimeout(60_000)
    → increase timeout or use page.waitForSelector with a longer timeout
  - Evidence snippet not visible: EvidenceSnippet may need text to be in the DOM tree
    (not just CSS-visible) for Playwright to assert getByText("Source:")
  - Comparability matrix: if comparison requires ≥2 vendors, test must load 2 before asserting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Playwright suite + fix any failures</name>
  <files>docs/qa/phase5-playwright.spec.ts</files>
  <read_first>
    - docs/qa/phase5-playwright.spec.ts (full file — current test implementations)
    - apps/web/app/(buyer)/extraction/page.tsx (verify evidence snippet DOM structure)
    - apps/web/app/(buyer)/comparison/page.tsx (verify comparability badge DOM structure)
    - apps/web/app/(buyer)/trace/page.tsx (verify "Code overruled the model" text in DOM)
  </read_first>
  <action>
Step 1 — Ensure the local stack is running:
  Terminal 1: `cd services/ai && uv run uvicorn api.app:app --reload --port 8000`
  Terminal 2: `cd apps/web && pnpm dev`

Step 2 — Run the full Playwright suite:
  `cd /Users/aayush/projects/aerchain && npx playwright test docs/qa/phase5-playwright.spec.ts --headed 2>&1`

Step 3 — For each failing test, diagnose and fix. Common failure patterns:
  - Timeout on SSE result: increase test.setTimeout to 90_000; add
    `await page.waitForSelector('[data-testid="extraction-result"]', {timeout: 80000})`
    Add data-testid attributes to key DOM elements if locators are flaky:
      data-testid="gaps-panel" on the Gaps & Risks card
      data-testid="extraction-result" on the per-category section container
      data-testid="comparability-matrix" on the matrix table
      data-testid="trace-diff" on the downgrade diff card
  - Text locator mismatch: if getByText("Gaps & Risks") fails, verify the exact text in
    the DOM (check UI-SPEC Copywriting § for exact copy strings).
  - Sample load not navigating: verify setLoadedVendors + router.push in input/page.tsx.
  - Comparison matrix empty: ensure test loads 2 vendors before navigating to /comparison.

Step 4 — After all fixes, run the suite non-headed to confirm clean pass:
  `npx playwright test docs/qa/phase5-playwright.spec.ts 2>&1 | tail -20`

Step 5 — Run against deployed stack (if available from Plan 05-08):
  `PLAYWRIGHT_BASE_URL=https://[vercel-url] npx playwright test docs/qa/phase5-playwright.spec.ts 2>&1 | tail -20`

Step 6 — Also run the full pytest suite one final time:
  `cd services/ai && uv run pytest tests/ -q 2>&1 | tail -10`

Do not skip or xfail passing tests to make the suite "green" — fix the actual failures.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain && npx playwright test docs/qa/phase5-playwright.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test docs/qa/phase5-playwright.spec.ts` exits 0 with all tests PASSED (not skipped)
    - `cd services/ai && uv run pytest tests/ -q` exits 0 (full suite still green)
    - `grep -c "PASSED\|passed" <playwright-output>` ≥ 6 (all buyer-journey tests pass)
    - No test is marked `.skip()` or `.fixme()` in phase5-playwright.spec.ts (check: grep -c "skip\|fixme" docs/qa/phase5-playwright.spec.ts returns 0)
    - Evidence snippet assertion passes ("Source:" text visible in DOM on /extraction)
    - Comparability matrix assertion passes (matrix table visible on /comparison)
    - Downgrade diff assertion passes ("Code overruled the model" visible on /trace)
  </acceptance_criteria>
  <done>All Playwright tests passing. Full pytest suite green. UAT script is the passing phase5-playwright.spec.ts.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human UAT — demo-quality buyer journey verification</name>
  <what-built>
All five buyer screens are built and deployed. Playwright tests pass. Claude has verified
the automated assertions. Human now does a final qualitative check against the rubric.
  </what-built>
  <how-to-verify>
Run the demo arc from docs/demo/demo-script.md end-to-end in a real browser:

1. Open the deployed Vercel URL (or http://localhost:3000 for local).
2. /rfq — Confirm RFQ data loads instantly. Regenerate button visible.
3. /input — Load the "Fluff" vendor sample. Confirm navigation to /extraction.
4. /extraction — Confirm Gaps & Risks panel is at the top (not buried). Count the flagged
   fields. Confirm ≥1 evidence snippet is visible ("Source: ...") under a field value.
   Click the "Show in context" collapsible and confirm the source passage appears.
5. /input — Load 2 more vendor samples (Thorough + Cheap).
6. /comparison — Confirm: (a) Needs Attention panel is at the top, (b) comparability matrix
   renders with color badges, (c) "Comparability determined in code from evidence" note
   is visible below the matrix, (d) "Data readiness" label visible, (e) no numeric score.
7. /trace — Select comparison_trace_1. Confirm amber-highlighted rows in the clamp diff section.
   "Code overruled the model on N verdict(s)" heading visible. Confirm 6 tabs total (no duplicate).

Rubric check (verify each claim is true):
  - [ ] Missing/unclear/conflicting fields are surfaced, not hidden
  - [ ] Every shown fact has a visible evidence snippet
  - [ ] Non-comparable vendors are flagged before any scoring
  - [ ] No fabricated numbers or claims appear
  - [ ] "Code disproves model" proof is visible in the Trace screen
  - [ ] The UI polish is concentrated on AI-showcase surfaces (evidence, flags, matrix)
        not decorative chrome
  </how-to-verify>
  <resume-signal>
Type "uat-approved" if the full rubric check passes. Describe any issues with their screen/step
number so Claude can fix them (e.g. "Step 4: evidence snippet text not visible, only shows dash").
  </resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Record demo video (SHIP-04)</name>
  <what-built>
The app is deployed and verified (Tasks 1–2). docs/demo/demo-script.md contains the full
narrated script with timestamps. The Trace screen shows the amber-highlighted "code disproves
model" proof. All rubric moments are reachable. The demo is ready to record.

This step is a human action: screen capture with voice-over cannot be automated.
  </what-built>
  <how-to-verify>
Record a ≤5-min screen capture of the demo arc in docs/demo/demo-script.md:

1. Open the deployed Vercel URL (or local stack) in a browser window sized for recording.
2. Follow the arc and timestamps in docs/demo/demo-script.md — hit every rubric moment:
   - RFQ Overview → messy Fluff vendor → Extraction gaps + evidence snippet expand
   - All 3 vendors → Comparison matrix with non-comparable badge
   - Trace screen → comparison_trace_1 → amber clamp diff rows → "Code overruled the model"
   - Prompt Pack list → return to Comparison → closing narration on no-fabrication principle
3. Keep total runtime ≤5 min (strict rubric limit).
4. Export or host the recording. Options:
   - Commit a small video file (<50 MB) directly to docs/demo/demo.mp4 (or .mov, .webm)
   - OR upload to a video host (Loom, YouTube unlisted, Google Drive share link) and commit
     a one-line docs/demo/demo-link.txt containing the URL
   Either is acceptable; the reviewer needs a URL or file they can open.

After committing/uploading, run:
  `ls docs/demo/` — confirm demo-script.md plus demo.mp4 or demo-link.txt is present.
  </how-to-verify>
  <resume-signal>
Type "video-committed" once the recording is committed (file or link) to docs/demo/. Include
the file path or URL so it can be confirmed (e.g. "docs/demo/demo.mp4 committed" or
"docs/demo/demo-link.txt → https://loom.com/share/...").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → running app | E2E test traffic; same as real browser traffic |
| docs/demo/ video | No secrets; screen recording of the public-facing buyer UI only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-A | Spoofing | Test asserting fabricated content as correct | mitigate | Playwright asserts behavioral properties (presence of "Source:" text, absence of numeric score) not exact model output values — model output values will vary per run |
| T-05-09-B | Info Disclosure | Demo video content | accept | Recording shows only the buyer UI; no API keys, env files, or terminal windows with secrets should be visible — verify before committing |
</threat_model>

<verification>
Final phase gate — ALL of these must be true before phase is marked complete:

1. `npx playwright test docs/qa/phase5-playwright.spec.ts` → all PASSED, exit 0
2. `cd services/ai && uv run pytest tests/ -q` → exit 0, ≥ existing test count
3. Human UAT rubric check: all 6 checkboxes confirmed (Task 2)
4. Both services deployed and accessible (Vercel + Render URLs reachable)
5. SHIP-04: demo video recorded and committed — `ls docs/demo/` shows demo.mp4, demo.mov,
   demo.webm, or demo-link.txt alongside demo-script.md (Task 3)
6. Every phase requirement covered:
   - INPUT-01: paste path tested (test_input_wrap.py GREEN + Playwright)
   - INPUT-02: file upload endpoint tested (test_file_extract.py GREEN)
   - INPUT-03: sample load Playwright test passes
   - INPUT-04: extraction called live (Playwright stream test)
   - UI-01..06: all 5 screens render + Playwright assertions pass
   - PROMPT-02: 7 prompt docs in docs/prompts/ (including extraction-prompt-doc.md)
   - SHIP-01: deployed stack verified in browser
   - SHIP-02: README.md at repo root with env vars + run instructions
   - SHIP-03: docs/write-up.md ≥ 800 words
   - SHIP-04: docs/demo/demo-script.md + demo video (file or link) in docs/demo/
   - SHIP-05: Mermaid diagrams in docs/architecture/
</verification>

<success_criteria>
All Playwright tests pass. Human UAT approved. Demo video recorded and committed (≤5 min).
All 15 phase requirements are covered and verified. Phase 5 is complete. The submission package
is ready: README, write-up, architecture diagrams, demo script, demo video, Prompt Pack docs,
and the deployed dual-service stack.
</success_criteria>

<output>
Create `.planning/phases/05-buyer-ui-trace-submission/05-09-SUMMARY.md` when done.
</output>
