---
phase: 05-buyer-ui-trace-submission
plan: "07"
type: execute
wave: 3
depends_on:
  - "05-04"
  - "05-05"
files_modified:
  - apps/web/app/(buyer)/trace/page.tsx
  - README.md
  - docs/write-up.md
  - docs/architecture/system-diagram.md
  - docs/architecture/ai-pipeline-diagram.md
  - docs/demo/demo-script.md
autonomous: true
requirements:
  - UI-05
  - SHIP-02
  - SHIP-03
  - SHIP-04
  - SHIP-05

must_haves:
  truths:
    - "Trace screen renders ≥1 complete pipeline trace (input → prompt → raw output → grounded/clamped final)"
    - "Downgrade diff section highlights model-proposed vs code-clamped verdicts in amber (D-15)"
    - "Prompt Pack list renders all 7 prompt IDs with intent from registry frontmatter"
    - "README covers setup, run, model requirements, env vars, sample flow, assumptions"
    - "Write-up is 1–2 pages covering problem, prompt architecture, product thinking, limitations"
    - "Architecture diagram (system + AI pipeline) exists as Mermaid in docs/architecture/"
    - "Demo script covers the rubric-driven arc: messy vendor → gaps → non-comparable → trace (D-19)"
  artifacts:
    - path: "apps/web/app/(buyer)/trace/page.tsx"
      provides: "UI-05 Prompt Trace screen"
      contains: "Code overruled the model"
    - path: "README.md"
      provides: "SHIP-02 README"
      contains: "OPENAI_API_KEY"
    - path: "docs/write-up.md"
      provides: "SHIP-03 write-up"
      contains: "prompt architecture"
    - path: "docs/architecture/system-diagram.md"
      provides: "SHIP-05 system architecture Mermaid"
      contains: "```mermaid"
    - path: "docs/demo/demo-script.md"
      provides: "SHIP-04 demo storyboard"
      contains: "code disproves model"
  key_links:
    - from: "apps/web/app/(buyer)/trace/page.tsx"
      to: "apps/web/public/traces/*.json"
      via: "/api/traces/[name] Route Handler"
      pattern: "/api/traces/"
    - from: "apps/web/app/(buyer)/trace/page.tsx"
      to: "services/ai/prompts/registry.py"
      via: "prompt list (id, version, intent)"
      pattern: "registry"
---

<objective>
Build the Prompt Trace screen (UI-05) and author the complete submission package: README,
write-up, architecture diagrams, and demo script. These close SHIP-02..05 and the final
rubric deliverables.

Purpose: The Trace screen is the "code disproves model" proof that anchor the demo. The
submission docs are 5% of the grade but required for the evaluation package.
Output: /trace screen + all submission documents.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-UI-SPEC.md
@/Users/aayush/projects/aerchain/docs/assignment.md

<interfaces>
Trace file schema (from 05-RESEARCH.md § Trace Screen Data Contract):

Extraction traces (trace_vendor_*.json, trace_adversarial_fixture.json):
  { input: { vendor_name, source_id, rfq_line_items, raw_text_preview },
    resolved_prompt: { id, version, system_message, human_message_template },
    raw_model_output: { ...ExtractionResult fields... },
    grounding_step: { downgrade_report: { entries: [...] }, fields_downgraded: int },
    final_result: { ...ExtractionResult after grounding... } }

Comparison traces (comparison_trace_*.json):
  { _fixture_mode: bool,
    input: { vendor_names, rfq_title, extraction_summaries },
    resolved_prompt: { id, version, system_message_excerpt },
    raw_model_output: { dimensions, narrative_summary },
    clamp_step: { entries: [ { vendor_name, dimension, model_proposed, code_ceiling,
      clamped_to, ceiling_reason } ] },
    clarification_step: { ... },
    final_result: { ...ComparisonResult... } }

"Code disproves model" data lives in:
  Extraction: grounding_step.downgrade_report.entries
  Comparison: clamp_step.entries

Trace files served via: GET /api/traces/[name] (Route Handler from Plan 05-04, reads public/traces/)

Prompt registry data — read from registry.py to get prompt IDs and then fetch frontmatter.
For the Trace screen (client-side): embed the prompt list statically (the 7 IDs are known)
or fetch from a /api/prompts route. Simplest: a static array of {id, version, intent} objects
derived from the frontmatter of all 7 prompt files — embed at build time via Server Component.

UI-SPEC Trace screen hierarchy (D-15):
  1. Trace selector Tabs: one tab per trace file. Default: comparison_trace_1 (most narrative-rich)
  2. Per-trace view: pipeline timeline (Input → Prompt → Raw output → Final)
  3. Downgrade diff: amber-highlighted ClampEntry[] or DowngradeEntry[] — "Code overruled the model on N verdict(s)"
  4. Raw model output in pre/code block (ScrollArea), copy-to-clipboard button
  5. Prompt Pack list below: id, version, intent, link to docs/prompts/ doc

Env vars required for README:
  OPENAI_API_KEY — OpenAI API key
  MODEL_REASONING — gpt-5.4 (or as confirmed in .env)
  MODEL_CHEAP — gpt-5.4-mini
  NEXT_PUBLIC_AI_BASE_URL — URL of the deployed AI service (browser-public)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prompt Trace screen (UI-05, D-15)</name>
  <files>apps/web/app/(buyer)/trace/page.tsx</files>
  <read_first>
    - docs/traces/comparison_trace_1.json (full file — understand clamp_step.entries structure)
    - docs/traces/trace_vendor_thorough.json (first 30 lines — grounding_step.downgrade_report)
    - apps/web/components/ui/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent)
    - apps/web/components/ui/scroll-area.tsx (ScrollArea for long raw output)
    - apps/web/app/api/traces/[name]/route.ts (Plan 05-04 — the route to fetch traces)
    - services/ai/prompts/registry.py (prompt IDs + load() function — source of truth for 7 prompts)
    - .planning/phases/05-buyer-ui-trace-submission/05-UI-SPEC.md (UI-05 interaction contract)
    - .planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md (D-15 decisions)
  </read_first>
  <action>
Create apps/web/app/(buyer)/trace/page.tsx.

This screen is static-render (no SSE, no session state). Fetch all trace files at render
time via the /api/traces/[name] Route Handler. Use a Server Component with async data fetching,
passing data to client sub-components only where interactivity is needed (tab switching).

TRACE FILE LIST: The 6 trace files are known (from Plan 05-04 public/traces/ copy). Use
fs.readdir on the public/traces/ directory at render time to enumerate the actual files present
— this avoids duplicates and picks up any new traces automatically. The canonical set is:
["comparison_trace_1", "comparison_trace_2", "trace_vendor_thorough", "trace_vendor_cheap",
"trace_vendor_fluff", "trace_adversarial_fixture"] — 6 unique names. Do NOT hard-code duplicates.
The fs.readdir approach deduplifies naturally; if hard-coding as a fallback, use exactly this
6-element list (not 7, the prior list had trace_vendor_cheap twice — that was a bug).

Page structure:
  - Page title: "Prompt Trace" (text-3xl font-semibold)
  - Subtitle: "Input → Prompt → Raw model output → Grounded/clamped final" (text-sm text-muted-foreground)

SECTION 1 — Trace selector Tabs (default: comparison_trace_1, D-15):
  One tab per trace file (truncated display name: "Comparison 1", "Vendor: Thorough", etc.)
  Per-trace panel:

  Pipeline timeline (vertical, 4 stages):
    Stage 1 "Input": render trace.input as summary text (vendor_name or vendor_names + rfq_title)
    Stage 2 "Prompt": prompt ID + version from trace.resolved_prompt.id/.version;
      "View prompt in Prompt Pack" link (anchor to prompt list below)
    Stage 3 "Raw model output": truncated preview (first 300 chars) + ScrollArea with full
      raw_model_output JSON in <pre className="font-mono text-xs"> block.
      Copy-to-clipboard button (navigator.clipboard.writeText): "Copy raw output"
    Stage 4 "Grounded/clamped final": summary of final_result (vendor_name + key field counts)

  Downgrade diff (D-15, the "code disproves model" proof):
    For extraction traces: if grounding_step.downgrade_report.entries.length > 0:
      Card with header: `"Code overruled the model on ${entries.length} verdict(s)"` (exact copy from UI-SPEC)
      Table: field_name | model_value | final_status (highlight row bg-amber-50 for downgraded)
    For comparison traces: if clamp_step.entries.length > 0:
      Same card/header pattern.
      Table: vendor_name | dimension | model_proposed | clamped_to | ceiling_reason
      Highlight rows where model_proposed !== clamped_to with bg-amber-50 (amber = change, D-15)
    If no downgrades/clamps: "No overrides — model output matched grounding constraints."

SECTION 2 — Prompt Pack list (below traces):
  Card title: "Prompt Pack — 7 prompts"
  Embed a static array of the 7 prompts' {id, version, intent, docs_url} from known frontmatter.
  Read frontmatter from the 7 prompt .md files at build time (Server Component file read), OR
  embed the data as a const (Claude's Discretion — static const is simpler and correct for a prototype).
  Each entry: id (text-xs font-semibold), version, intent (text-sm), link to docs/prompts/[doc].
  Render as a list, not a Tabs (simpler; all 7 fit in a scrollable list).

Typography: text-3xl page title, text-xl section headings, text-sm body, text-xs labels.
Responsive: single-column. ScrollArea keeps long raw output from breaking layout.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/apps/web && pnpm tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "Code overruled the model" apps/web/app/\(buyer\)/trace/page.tsx` ≥ 1 (exact D-15 copy)
    - `grep -c "bg-amber-50" apps/web/app/\(buyer\)/trace/page.tsx` ≥ 1 (downgrade highlighting)
    - `grep -c "ScrollArea" apps/web/app/\(buyer\)/trace/page.tsx` ≥ 1 (raw output scroll)
    - `grep -c "Prompt Pack" apps/web/app/\(buyer\)/trace/page.tsx` ≥ 1 (prompt list section)
    - No duplicate tab: `grep -c "trace_vendor_cheap" apps/web/app/\(buyer\)/trace/page.tsx` ≤ 1 if hard-coded (or fs.readdir used)
    - Manual: `pnpm dev` → /trace → comparison_trace_1 tab visible; amber-highlighted rows in clamp diff; 6 tabs total (not 7)
  </acceptance_criteria>
  <done>Trace screen renders pipeline timeline, amber-highlighted downgrade diff, raw output scrollable, prompt pack list. 6 unique tabs, no duplicate.</done>
</task>

<task type="auto">
  <name>Task 2: README + write-up + architecture diagrams + demo script (SHIP-02..05)</name>
  <files>README.md, docs/write-up.md, docs/architecture/system-diagram.md, docs/architecture/ai-pipeline-diagram.md, docs/demo/demo-script.md</files>
  <read_first>
    - docs/assignment.md (§9 deliverables, §20 checklist, §22 rubric weights — anchor every section)
    - docs/assignment.md (§24 anti-patterns — cite explicitly in write-up as "what we avoided")
    - CLAUDE.md §§1,5,6,7 (product principles, architecture, screens, Prompt Pack)
    - services/ai/api/app.py (lines 1–45: existing endpoints for README run instructions)
    - .planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md (D-18..D-22 deployment decisions)
    - .planning/phases/05-buyer-ui-trace-submission/05-RESEARCH.md (Deployment Details section)
  </read_first>
  <action>
Author five Markdown documents. Each must be written to a standard an Aerchain reviewer
will find professionally complete, not a skeleton.

README.md (SHIP-02) — repo root. Sections:
  # Bid Desk — Procurement Copilot
  ## What It Does  (2–3 sentences, buyer value)
  ## Architecture  (tech stack table: Next.js/Vercel + FastAPI+LangGraph/Render)
  ## Prerequisites  (Node 26, pnpm 10, Python 3.12, uv, OPENAI_API_KEY)
  ## Setup
    ```bash
    pnpm install
    cd services/ai && uv sync
    cp services/ai/.env.example services/ai/.env  # add your API key
    ```
  ## Environment Variables
    | Variable | Required | Description |
    | OPENAI_API_KEY | yes | OpenAI API key with GPT-5.4 access |
    | MODEL_REASONING | no | Default: gpt-5.4 |
    | MODEL_CHEAP | no | Default: gpt-5.4-mini |
    | NEXT_PUBLIC_AI_BASE_URL | yes (prod) | Render service URL. Default: http://localhost:8000 |
  ## Run Locally
    ```bash
    # Terminal 1: AI service
    cd services/ai && uv run uvicorn api.app:app --reload --port 8000
    # Terminal 2: Web app
    cd apps/web && pnpm dev
    # Open http://localhost:3000
    ```
  ## Sample Flow  (load sample vendor → extract → compare → trace)
  ## Running Tests  (`cd services/ai && uv run pytest tests/ -q`)
  ## Deployment  (Vercel + Render; link to 05-CONTEXT.md deploy sequence D-18)
  ## Assumptions  (best-effort OCR, single-buyer, GPT-5.4 family only)
  ## Model/API Requirements  (GPT-5.4 / 5.4-mini; confirm access before running)

docs/write-up.md (SHIP-03) — 1–2 pages (1000–1500 words). Structure:
  # Bid Desk — Write-Up
  ## Problem Statement  (what buyers actually struggle with in competitive bids; sense-making under uncertainty)
  ## Assumptions  (marketing services RFQ, 3 messy vendors, best-effort text extraction, no production OCR)
  ## Prompt Architecture  (the 7 prompts, layered design: rfq-gen → vendor-gen → extraction → comparison → clarification; why each is structured as it is)
  ## Product Thinking  (evidence-over-assertion; absence-first; comparability-before-ranking; buyer-first hierarchy; what §24 anti-patterns we avoided and why)
  ## Extraction Approach  (grounding gate in code, four flag types, strict structured output, SSE streaming)
  ## Comparison Approach  (comparability clamp in code, no numeric scoring, clarification questions)
  ## UI/UX Decisions  (5 screens, stage rail, session cache, evidence inline, matrix-first comparison)
  ## Limitations  (best-effort text extraction, single buyer session, no production OCR, demo cold-start)
  ## What's Next  (stateful feedback loop, production OCR, multi-buyer sessions)

docs/architecture/system-diagram.md (SHIP-05, part 1):
  # System Architecture
  Mermaid `flowchart LR` diagram showing:
    Browser → (fetch+SSE) → FastAPI/Render → (OpenAI API) → GPT-5.4
    Browser ← (SSE events) ← FastAPI
    FastAPI → (reads) → data/*.json, docs/traces/*.json
    Next.js → (deployed to) → Vercel
    FastAPI → (deployed to) → Render
  Also show: CORS boundary, sessionStorage on client, NEXT_PUBLIC_AI_BASE_URL env seam.

docs/architecture/ai-pipeline-diagram.md (SHIP-05, part 2):
  # AI Pipeline Architecture
  Mermaid `flowchart TD` diagram showing the agent pipeline:
    RFQ Gen (LangGraph) → data/rfq.json
    Vendor Gen (LangGraph) → data/vendor_*.json
    VendorResponse → Extraction Agent (LangGraph) → ExtractionResult
    ExtractionResult → Grounding Gate (Python code) → [downgrade unsupported]
    ExtractionResult[] → Comparison Agent (LangGraph) → ComparisonDraft
    ComparisonDraft → Comparability Clamp (Python code) → ComparisonResult
    Both agents → SSE → Browser
  Label the grounding gate and clamp as "code-enforced" (the reliability story).

docs/demo/demo-script.md (SHIP-04 storyboard):
  # Demo Script — Bid Desk (≤5 min)
  ## Arc (D-19): "Code disproves the model"
  Sections with timestamps and narration copy:
  00:00–00:30 — Open app, show RFQ Overview, narrate: "Here's the procurement event..."
  00:30–01:30 — Load the Fluff vendor (most messy), navigate to Extraction, narrate:
    "The AI extracted what it could — but look at the Gaps panel: 3 fields missing, 1 conflicting..."
    Show an evidence snippet expand.
  01:30–02:30 — Load all 3 vendors, run comparison, narrate:
    "Before ranking anyone, let's see who's even comparable — matrix shows Vendor A is
    not_comparable on commercial dimension. The attention panel flagged 4 clarification needs."
  02:30–03:30 — Navigate to Trace screen, show comparison_trace_1, narrate:
    "Here's the 'code disproves model' moment — the model proposed 'partially' for Vendor B on
    timeline, but our code clamped it to 'not_comparable' because timeline.status = missing.
    Highlighted in amber."
  03:30–04:30 — Show Prompt Pack list, narrate briefly: "7 versioned prompts, each documented..."
  04:30–05:00 — Return to Comparison, narrate: "No fabrication — only what's in the vendor's
    response. Missing is missing, not filled in. That's the design principle."
  ## Key Points to Hit (rubric coverage)
    List the rubric items (prompt quality, extraction accuracy, product thinking, etc.) and which
    demo moment demonstrates each.
  </action>
  <verify>
    <automated>ls /Users/aayush/projects/aerchain/README.md /Users/aayush/projects/aerchain/docs/write-up.md /Users/aayush/projects/aerchain/docs/architecture/system-diagram.md /Users/aayush/projects/aerchain/docs/architecture/ai-pipeline-diagram.md /Users/aayush/projects/aerchain/docs/demo/demo-script.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist (ls exits 0)
    - `grep -c "OPENAI_API_KEY" README.md` ≥ 1 (env var documented)
    - `grep -c "pnpm dev" README.md` ≥ 1 (run instructions)
    - `grep -c "prompt architecture\|Prompt Architecture" docs/write-up.md` ≥ 1
    - `grep -c '```mermaid' docs/architecture/system-diagram.md` ≥ 1 (Mermaid block present)
    - `grep -c '```mermaid' docs/architecture/ai-pipeline-diagram.md` ≥ 1
    - `grep -c "code disproves\|Code disproves\|code-enforced" docs/demo/demo-script.md` ≥ 1 (D-19 arc)
    - `wc -w docs/write-up.md | awk '{print $1}'` ≥ 800 (≥800 words = 1+ pages)
  </acceptance_criteria>
  <done>README, write-up (1–2 pages), 2 Mermaid architecture diagrams, and demo script all authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trace page → /api/traces/[name] | File path is sanitized (Plan 05-04 route handler) |
| README | No secrets in README (API key referenced by name only, not value) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-A | Info Disclosure | README content | accept | README contains only env var names, no values; .env is gitignored and documented as such |
| T-05-07-B | Tampering | Trace file path in /api/traces | accept | Path sanitization handled in Route Handler (Plan 05-04 T-05-04-A) — no new risk here |
</threat_model>

<verification>
1. All 5 docs exist via ls check
2. `pnpm tsc --noEmit` still exits 0 after adding trace/page.tsx
3. Manual: /trace renders comparison_trace_1 with amber downgrade diff rows; 6 tabs (no duplicate)
4. `wc -w docs/write-up.md` ≥ 800 words
</verification>

<success_criteria>
/trace renders complete pipeline trace with amber-highlighted clamp diffs and prompt pack list.
6 unique trace tabs (no duplicate trace_vendor_cheap). README, write-up (≥800 words), 2 Mermaid
diagrams, and demo script are present and complete.
</success_criteria>

<output>
Create `.planning/phases/05-buyer-ui-trace-submission/05-07-SUMMARY.md` when done.
</output>
