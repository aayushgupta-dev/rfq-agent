---
phase: 05-buyer-ui-trace-submission
plan: "06"
type: execute
wave: 3
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - apps/web/app/(buyer)/extraction/page.tsx
  - apps/web/app/(buyer)/comparison/page.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-06
  - INPUT-04

must_haves:
  truths:
    - "Extraction Review shows a Gaps & Risks panel listing every non-present field (D-08)"
    - "Every extracted field shows an inline evidence snippet; absent-evidence fields show 'No verified source' (D-07)"
    - "Evidence drill-down (Collapsible) opens the source passage (D-07)"
    - "Fields are grouped by schema category: scope, pricing, commercial_terms, timeline, compliance, assumptions, exclusions, risks (D-09)"
    - "Vendor selector Tabs allow switching between loaded vendors (D-10)"
    - "Session-cached result renders instantly; SSE only fires for uncached vendors (D-02)"
    - "SSE on-result handler normalizes the payload: destructures downgrade_report before caching ExtractionResult"
    - "Comparison screen shows 'Load at least 2 vendors to compare' empty-state when fewer than 2 extractions exist"
    - "Comparison screen: clarifications/attention panel visible at top (D-12)"
    - "Comparability matrix (vendors x 6 dimensions) is the hero below the attention panel (D-11)"
    - "Vendors render in stable input order, no sort; readiness labelled 'Data readiness' (D-13)"
    - "Comparability note visible: 'Comparability determined in code from evidence — not a model verdict' (D-14)"
    - "Stream progress driven by the phase field of status events (model → grounding for extraction) not a +20% counter (D-25)"
    - "AbortController created on mount; abort called on component unmount (D-25 / T-05-06-C)"
    - "D-03: visual polish concentrated on evidence snippets, flag badges, and comparability matrix (AI-showcase surfaces)"
    - "D-26: extraction and comparison screens are responsive; AI-showcase surfaces legible at narrow widths"
  artifacts:
    - path: "apps/web/app/(buyer)/extraction/page.tsx"
      provides: "UI-03 Extraction Review screen"
      contains: "Gaps & Risks"
    - path: "apps/web/app/(buyer)/comparison/page.tsx"
      provides: "UI-04 Vendor Comparison screen"
      contains: "Comparability determined in code"
  key_links:
    - from: "apps/web/app/(buyer)/extraction/page.tsx"
      to: "apps/web/lib/api.ts"
      via: "streamExtract"
      pattern: "streamExtract"
    - from: "apps/web/app/(buyer)/extraction/page.tsx"
      to: "apps/web/components/evidence-snippet.tsx"
      via: "EvidenceSnippet component"
      pattern: "EvidenceSnippet"
    - from: "apps/web/app/(buyer)/extraction/page.tsx"
      to: "apps/web/components/flag-badge.tsx"
      via: "FlagBadge component"
      pattern: "FlagBadge"
    - from: "apps/web/app/(buyer)/comparison/page.tsx"
      to: "apps/web/components/comparability-badge.tsx"
      via: "ComparabilityBadge component"
      pattern: "ComparabilityBadge"
---

<objective>
Build the two AI-showcase screens: Extraction Review (UI-03) and Vendor Comparison (UI-04).
These are the highest-stakes screens — they must render evidence snippets, flag badges,
and the comparability matrix clearly, since that's where 35% of the rubric grade is visible.

Purpose: Extraction and Comparison screens are the product's core output. Their information
hierarchy, evidence display, and absence-first design are evaluated directly by Aerchain.
Output: /extraction and /comparison — fully functional, SSE-streaming, session-cached.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-UI-SPEC.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-PATTERNS.md

<interfaces>
## API Contract (authoritative — verified against app.py, extraction.py:109-231, comparison.py:896-897)

### POST /extract/vendor
  Request body: { vendor_response: VendorResponse, rfq: RFQ }
  Built by api.ts streamExtract() — see Plan 05-03. The "vendor_response" key is confirmed.

### POST /compare/vendors
  Request body: { extractions: ExtractionResult[], rfq: RFQ }
  _MIN_VENDORS = 2 enforced server-side. Sending < 2 ExtractionResult items → 422.
  The comparison page MUST guard: if Object.keys(extractions).length < 2, show empty state.

### SSE event: "status"
  payload: { message: string, phase: string }
  Extraction phases in order: "model" → "grounding"
  Drive StreamProgress from the phase field:
    "model"    → value=40, phase="Calling extraction model..."
    "grounding" → value=80, phase="Running grounding gate..."
    done       → value=100

### SSE event: "result" — ASYMMETRIC
  Extraction result (extraction.py:225-231):
    payload = { ...ExtractionResult fields spread at top level..., downgrade_report: {...} }
    NOT a bare ExtractionResult. The client MUST normalize before caching:
      const { downgrade_report, ...result } = event.payload as Record<string, unknown>
      normalizeExtractionPayload(event.payload) from api.ts returns { result, downgrade_report }
      Cache result (ExtractionResult) in extractions[]; keep downgrade_report in downgradeReports[].
      Only the clean ExtractionResult is safe to send to /compare/vendors.

  Comparison result (comparison.py:896-897):
    payload = bare ComparisonResult — cast directly:
      setComparison(event.payload as ComparisonResult)

### SSE event: "error"
  payload: { code: string, message: string, recoverable: boolean }

### SSE event: "done"
  payload: {}

## ExtractionResult fields (from packages/shared-types/index.d.ts — verified)
  vendor_name: string
  scope_summary: FieldStr
  line_items?: LineItemExtraction[]
  pricing_structure: FieldStr
  total_price: FieldStr
  commercial_terms: FieldStr
  timeline: FieldStr
  compliance_points?: FieldStr[]
  assumptions?: FieldStr[]
  exclusions?: FieldStr[]
  risks?: FieldStr[]

## FieldStr
  status: FlagStatus  ← "present"|"missing"|"unclear"|"conflicting"|"unsupported"
  value?: string | null
  evidence?: Evidence[]
  values?: ConflictingValueStr[] | null

## Evidence (from shared-types)
  snippet: string
  char_start: number
  char_end: number
  source_id: string

## LineItemExtraction (from shared-types — exact field names)
  line_item_id: string
  line_item_name: string
  pricing: FieldStr        ← exact field name is "pricing" not "price"
  scope_coverage: FieldStr ← exact field name is "scope_coverage"

## ComparisonResult (from shared-types — verified)
  vendor_names: string[]
  dimensions: DimensionComparison[]
  line_item_offers?: LineItemOffer[]
  vendor_readiness?: VendorReadiness[]
  attention_points?: AttentionPoint[]
  clarification_questions?: ClarificationQuestion[]
  clamp_report: ClampReport

## DimensionComparison
  dimension: ComparisonDimension
  verdicts: DimensionVerdict[]
  narrative: string

## DimensionVerdict
  vendor_name: string
  verdict: ComparabilityVerdict
  reason: string
  model_proposed: ComparabilityVerdict

## AttentionPoint (from shared-types — exact field names, NOT plan's prior guess)
  trigger_type: string
  summary: string          ← NOT "description"
  vendors_affected?: string[]
  dimension_or_field?: string | null  ← NOT "dimension" or "clarification_question"

## ClarificationQuestion (separate interface — NOT a field on AttentionPoint)
  vendor_name: string
  field_path: string
  flag_status: string
  question: string
  why_needed: string

## VendorReadiness
  vendor_name: string
  comparable_count: number
  total_dimensions: number
  descriptor: string

## Category layout for Extraction Review (D-09, maps to ExtractionResult fields)
  scope          → [scope_summary] + line_items[].scope_coverage
  pricing        → [pricing_structure, total_price] + line_items[].pricing
  commercial_terms → [commercial_terms]
  timeline       → [timeline]
  compliance     → compliance_points[]
  assumptions    → assumptions[]
  exclusions     → exclusions[]
  risks          → risks[]

## From apps/web/lib/api.ts (Plan 05-03)
  streamExtract(vendor, rfq, signal?): AsyncGenerator<EventEnvelope>
  streamCompare(extractions, rfq, signal?): AsyncGenerator<EventEnvelope>
  normalizeExtractionPayload(payload): { result: ExtractionResult; downgrade_report: unknown }

## From apps/web/contexts/BuyerContext.tsx (Plan 05-03)
  { loadedVendors, extractions, setExtraction, setDowngradeReport,
    comparison, setComparison }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraction Review screen (UI-03, D-07..D-10, UI-06)</name>
  <files>apps/web/app/(buyer)/extraction/page.tsx</files>
  <read_first>
    - apps/web/contexts/BuyerContext.tsx (useBuyerContext — extractions, setExtraction, setDowngradeReport, loadedVendors)
    - apps/web/lib/api.ts (streamExtract, normalizeExtractionPayload signatures)
    - apps/web/components/evidence-snippet.tsx (EvidenceSnippet — always-visible + "No verified source" for absent)
    - apps/web/components/flag-badge.tsx (FlagBadge for status badges)
    - apps/web/components/stream-progress.tsx (StreamProgress — phase prop, not value counter)
    - apps/web/components/ui/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent)
    - apps/web/components/ui/card.tsx (Card, CardHeader, CardContent, CardTitle)
    - apps/web/components/ui/alert.tsx (Alert, AlertDescription — for error/empty states)
    - apps/web/components/ui/skeleton.tsx (Skeleton rows during streaming)
    - .planning/phases/05-buyer-ui-trace-submission/05-UI-SPEC.md (UI-03 interaction contract)
  </read_first>
  <action>
Create apps/web/app/(buyer)/extraction/page.tsx as "use client".

STRUCTURE:

State:
  - const { loadedVendors, extractions, setExtraction, setDowngradeReport } = useBuyerContext()
  - const [rfq, setRfq] = useState<RFQ | null>(null)
  - const [selectedVendor, setSelectedVendor] = useState<string>("")
  - const [streaming, setStreaming] = useState(false)
  - const [phase, setPhase] = useState("")  ← from status event's "phase" field
  - const [progressValue, setProgressValue] = useState(0)
  - const [error, setError] = useState<string | null>(null)
  - const abortRef = useRef<AbortController | null>(null)  ← abort on unmount

On mount: load rfq from /data/rfq.json (fetch("/data/rfq.json")). Set selectedVendor to first
loadedVendor if available.

On unmount (useEffect cleanup): call abortRef.current?.abort() to cancel any in-flight SSE.
  return () => { abortRef.current?.abort() }

Session cache check + SSE trigger:
  useEffect deps: [selectedVendor, loadedVendors, rfq]
  if extractions[selectedVendor] → do nothing (cached)
  else if loadedVendors.find(v => v.vendor_name === selectedVendor) && rfq → start SSE:
    abortRef.current = new AbortController()
    setStreaming(true); setPhase(""); setProgressValue(0); setError(null)
    for await (const event of streamExtract(vendor, rfq, abortRef.current.signal)) {
      if (event.type === "status") {
        const { phase: p } = event.payload as { message: string; phase: string }
        setPhase(p)
        // Drive progress from known phase sequence (not a +20% counter)
        if (p === "model")     setProgressValue(40)
        if (p === "grounding") setProgressValue(80)
      }
      if (event.type === "result") {
        const { result, downgrade_report } = normalizeExtractionPayload(
          event.payload as Record<string, unknown>)
        setExtraction(selectedVendor, result)
        setDowngradeReport(selectedVendor, downgrade_report)
        setProgressValue(100); setStreaming(false)
      }
      if (event.type === "error") {
        setError((event.payload as { message: string }).message)
        setStreaming(false)
      }
      if (event.type === "done") setStreaming(false)
    }

RENDER:

EMPTY STATE (no vendors): Alert with link to /input:
  "Select or load a vendor on the Input screen to begin extraction."

STREAMING STATE: <StreamProgress phase={phase} value={progressValue} />
then Skeleton rows in each category Card (Skeleton className="h-6 w-full my-2")

LOADED STATE (extraction = extractions[selectedVendor]):

1. Vendor selector Tabs (D-10): one tab per loadedVendor. On tab change: setSelectedVendor.
   data-testid="vendor-tabs" on the Tabs root element.

2. Gaps & Risks panel (D-08, always visible, buyer-first UI-06):
   Card data-testid="gaps-panel" with CardHeader title = "Gaps & Risks — {flaggedCount} issue(s)"
   List all fields where field.status !== "present" in order of severity:
     missing → conflicting → unclear → unsupported
   Each item: field label + FlagBadge(status)
   Use a helper function collectFlaggedFields(extraction: ExtractionResult): {label, status}[]
   that iterates all scalar FieldStr properties + each item in list fields.
   If no issues: "All fields present — no gaps detected." (text-muted-foreground)

3. Category sections (D-09): 8 Cards, one per category:
   Container data-testid="extraction-result" on the sections wrapper.
   Each Card: CardHeader with category name (text-xl font-semibold, heading role)
   Each field row: grid grid-cols-[auto_1fr] gap-2 py-2 border-b border-border last:border-0
     - Left: field label (text-xs font-semibold, label role) + FlagBadge(field.status)
     - Right: field.value ?? "—" (em dash if absent per UI-SPEC Copywriting §)
       + EvidenceSnippet directly below:
         If field.status === "present" and field.evidence?.length:
           <EvidenceSnippet snippet={field.evidence[0].snippet} sourcePassage={field.evidence[0].snippet} />
         Else:
           <EvidenceSnippet snippet={undefined} />
           (EvidenceSnippet renders "No verified source" per Plan 05-03 implementation)
         NOTE: absent/unsupported fields legitimately lack evidence — NEVER render a fake snippet
         or pass a placeholder string. The "No verified source" copy in EvidenceSnippet is
         the canonical render for this case (§8 absence-first, never silently filled).

   For list fields (compliance_points, assumptions, exclusions, risks): render each item as
   its own row. For line_items: render under scope section as sub-rows with line_item_name label.
   Use LineItemExtraction.pricing (not "price") and .scope_coverage (exact field names from shared-types).

4. ERROR STATE: Alert variant="destructive" data-testid="extraction-error":
   "Extraction could not complete. {error} — Try reloading or check the AI service is running."
   (exact copy from UI-SPEC Copywriting §)

Typography: text-3xl page title "Extraction Review", text-xl section headings, text-sm values,
text-xs for labels/badges. Tokens: bg-card, border-border, text-muted-foreground.
Responsive: single-column on narrow; category Cards stack vertically.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/apps/web && pnpm tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "Gaps & Risks" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (panel title text)
    - `grep -c "EvidenceSnippet" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (evidence always visible)
    - `grep -c "FlagBadge" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 2 (in panel + per field)
    - `grep -c "extractions\[selectedVendor\]" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (cache check)
    - `grep -c "normalizeExtractionPayload" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (payload normalized)
    - `grep -c "setExtraction" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (cache population)
    - `grep -c "StreamProgress" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (streaming state)
    - `grep -c "AbortController\|abortRef" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 2 (abort on unmount)
    - `grep -c "phase.*model\|model.*phase\|grounding" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (phase-driven progress)
    - `grep -c "could not complete" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (exact error copy)
    - `grep -c "No verified source\|snippet={undefined}" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 0
      (EvidenceSnippet handles this — check that undefined is passed, not a fake string)
    - `grep -c "data-testid=\"gaps-panel\"" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (Playwright anchor)
    - `grep -c "data-testid=\"extraction-result\"" apps/web/app/\(buyer\)/extraction/page.tsx` ≥ 1 (Playwright anchor)
  </acceptance_criteria>
  <done>Extraction Review renders gaps panel, evidence snippets (with "No verified source" for absent), category layout, phase-driven progress, AbortController on unmount, and error state.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor Comparison screen (UI-04, D-11..D-14, UI-06)</name>
  <files>apps/web/app/(buyer)/comparison/page.tsx</files>
  <read_first>
    - apps/web/contexts/BuyerContext.tsx (extractions, comparison, setComparison)
    - apps/web/lib/api.ts (streamCompare signature)
    - apps/web/components/comparability-badge.tsx (ComparabilityBadge)
    - apps/web/components/stream-progress.tsx (StreamProgress — phase prop)
    - apps/web/components/ui/tooltip.tsx (Tooltip, TooltipContent, TooltipTrigger, TooltipProvider — matrix cell reasons)
    - apps/web/components/ui/collapsible.tsx (drill-down for line-item table, D-11)
    - apps/web/components/ui/card.tsx, alert.tsx, skeleton.tsx
    - packages/shared-types/index.d.ts (ComparisonResult, AttentionPoint, ClarificationQuestion, VendorReadiness)
    - .planning/phases/05-buyer-ui-trace-submission/05-UI-SPEC.md (UI-04 interaction contract)
    - .planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md (D-11..D-14 decisions)
  </read_first>
  <action>
Create apps/web/app/(buyer)/comparison/page.tsx as "use client".

STRUCTURE:

State:
  - const { extractions, comparison, setComparison } = useBuyerContext()
  - const vendorNames = Object.keys(extractions)
  - const extractionList = vendorNames.map(n => extractions[n])
  - const [streaming, setStreaming] = useState(false)
  - const [phase, setPhase] = useState("")
  - const [progressValue, setProgressValue] = useState(0)
  - const [error, setError] = useState<string | null>(null)
  - const abortRef = useRef<AbortController | null>(null)

On unmount: abortRef.current?.abort()

Comparison trigger:
  if comparison → render immediately (cached, D-02)
  else if vendorNames.length >= 2 && rfq → start SSE streamCompare
  NOTE: do NOT start SSE if vendorNames.length < 2 — show empty-state instead (server will 422).
  Load rfq from /data/rfq.json on mount (fetch).

SSE handling: same pattern as extraction. For "status" events:
  const { phase: p } = event.payload as { message: string; phase: string }
  Drive progress from comparison phase sequence:
    "align" → 20%, "comparability" → 40%, "compare" → 70%, "clarify" → 90%, done → 100%

On "result": setComparison(event.payload as ComparisonResult)
  (bare ComparisonResult — no normalization needed; asymmetric with extraction)

RENDER:

EMPTY STATE — insufficient vendors (vendorNames.length === 0):
  Alert + link to /extraction:
  "Run extraction on at least one vendor before comparing."

EMPTY STATE — not enough for comparison (vendorNames.length === 1):
  Alert (variant="default"):
  "Load at least 2 vendors to compare. Go to Vendor Input to load another vendor."
  Include a Button link to /input.
  (The server enforces _MIN_VENDORS=2; this empty-state prevents a 422 that would otherwise
   appear with no user-facing explanation. This is the absence-first (§8) design for this flow.)

STREAMING STATE: StreamProgress with phase and progressValue

LOADED STATE:

0. "Run Comparison" button: if extractionList.length >= 2 but comparison is null:
   Show primary Button "Run Comparison" that triggers streamCompare manually.

1. Clarifications & Attention panel (D-12, always visible at top per UI-06 buyer-first):
   Card title: "Needs Attention — {comparison.attention_points?.length ?? 0} item(s)"
   Each AttentionPoint: render summary (NOT "description" — exact field is "summary") text
   + related dimension or field: dimension_or_field if present
   Below the attention points, list ClarificationQuestion items from
   comparison.clarification_questions (separate field from attention_points):
   Each: vendor_name, question text (text-sm), why_needed (text-xs text-muted-foreground)
   Grouped or listed sequentially (list is simpler and correct).

2. Comparability matrix (D-11, hero):
   data-testid="comparability-matrix" on the table wrapper div.
   Table: vendors as columns (vendorNames), 6 ComparisonDimension values as rows.
   Sticky first column (dimension name). overflow-x-auto for narrow screens (UI-SPEC responsive).
   Wrap with TooltipProvider (required by shadcn Tooltip).
   Each cell: ComparabilityBadge(verdict) + Tooltip with reason text (DimensionVerdict.reason).
   Stable vendor column order = input order of vendorNames (D-13 — never sort).
   Table header row: vendor names.
   Row headers: dimension names (capitalize: "Technical", "Commercial", etc.)

3. "Comparability determined in code from evidence — not a model verdict" note (D-14):
   text-xs text-muted-foreground below the matrix. Always visible.

4. Data readiness row (D-13, below the note):
   Per VendorReadiness: "Data readiness: {comparable_count}/{total_dimensions} dimensions comparable"
   Tooltip: "Not a ranking or score" (exact copy — also displayed as visible text in the tooltip
   trigger so the Playwright grep criterion does not conflict with required on-screen copy).
   NOTE: the acceptance criterion checks grep for "Not a ranking or score" in the file —
   this copy MUST appear in the source (as tooltip content or visible text). It does NOT
   contradict the no-sort/no-rank rule; the label describes the indicator, it does not rank vendors.

5. Line-item offer table (drill-down, Collapsible, D-11):
   Collapsible trigger: "Show line-item offers (8 items)"
   Table: line items as rows, vendors as columns. Cell: LineItemOffer pricing_verbatim + FlagBadge.

6. Per-dimension narrative (Collapsible per dimension, D-11):
   Trigger: "Show {dimension} detail"
   Content: DimensionComparison.narrative text.

7. ERROR STATE: Alert variant="destructive":
   "Comparison could not complete. {error}"

Typography + tokens: same conventions as extraction screen. Matrix cells: compact (py-2 px-3).
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/apps/web && pnpm tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "Comparability determined in code from evidence" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (exact D-14 copy)
    - `grep -c "ComparabilityBadge" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (matrix cells)
    - `grep -c "Needs Attention" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (attention panel)
    - `grep -c "Data readiness" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (D-13 readiness label)
    - `grep -c "Not a ranking or score" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (D-13 exact copy — in tooltip or visible text)
    - `grep -c "at least 2 vendors to compare\|Load at least 2" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (min-vendor empty-state)
    - `grep -c "setComparison" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (cache population)
    - `grep -c "overflow-x-auto" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (responsive matrix)
    - `grep -c "AbortController\|abortRef" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 2 (abort on unmount)
    - `grep -c "attention_points" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (correct field name)
    - `grep -c "\.summary" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (AttentionPoint.summary not .description)
    - `grep -c "data-testid=\"comparability-matrix\"" apps/web/app/\(buyer\)/comparison/page.tsx` ≥ 1 (Playwright anchor)
    - No sort/rank logic: `grep -cE "\bsort\b|\brank\b" apps/web/app/\(buyer\)/comparison/page.tsx` returns 0
      NOTE: "Not a ranking or score" contains "rank" as a substring — use word-boundary \b to avoid
      false positives. The grep command uses -E for regex and \b word boundaries.
  </acceptance_criteria>
  <done>Comparison screen: ≥2-vendor empty-state, attention panel first, comparability matrix hero with stable order, D-14 clamp note, readiness label, AbortController on unmount, streaming, error state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → React state | ExtractionResult/ComparisonResult cross from AI service to client state |
| sessionStorage → React state | Cached results are deserialized from sessionStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-A | Spoofing | LLM fabricated facts rendered as fact | accept | Grounding gate enforced server-side in Phase 3. The UI renders the grounded ExtractionResult as-is; no client-side fact decision. `unsupported` fields are rendered with that badge — never displayed as `present`. |
| T-05-06-B | Tampering | Comparability verdict manipulation | accept | Comparability clamp is code-enforced server-side (Phase 4). UI renders clamped ComparisonResult from the SSE `result` event — no client re-computation. |
| T-05-06-C | DoS | SSE stream that never closes | mitigate | AbortController created on mount, abort() called in useEffect cleanup (unmount). Prevents dangling readers when user navigates away. If the AI service never sends "done", the reader is released on unmount. A timeout (e.g. 2 min AbortController.abort on setTimeout) can be added if live demo shows hangs — add it here if needed during Plan 05-09. |
</threat_model>

<verification>
1. `pnpm tsc --noEmit` exits 0 for both new files
2. Manual: load a sample vendor on /input → navigate to /extraction → Gaps & Risks panel visible; absent-evidence fields show "No verified source"
3. Manual: with extraction cached, navigate to /comparison → attempt with 1 vendor: "Load at least 2 vendors" message; load 2nd vendor, run comparison → attention panel + matrix visible
4. Manual: no sort buttons visible on Comparison screen (stable order, D-13)
5. `grep -cE "\bsort\b|\brank\b" apps/web/app/\(buyer\)/comparison/page.tsx` returns 0 (word-boundary check, not naive sort|rank|score)
</verification>

<success_criteria>
/extraction renders Gaps & Risks panel, inline evidence snippets ("No verified source" for absent),
category layout, phase-driven progress, AbortController on unmount, error states.
/comparison renders ≥2-vendor empty-state, attention panel first (using .summary field), comparability
matrix hero with stable order, D-14 clamp note, and data readiness label. TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/05-buyer-ui-trace-submission/05-06-SUMMARY.md` when done.
</output>
