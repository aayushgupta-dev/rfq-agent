---
phase: 05-buyer-ui-trace-submission
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - services/ai/pyproject.toml
  - services/ai/api/app.py
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-04

must_haves:
  truths:
    - "POST /extract/file-text accepts a file upload and returns {text, filename, chars}"
    - "POST /input/raw-text wraps raw text + vendor name into a valid VendorResponse JSON"
    - "FastAPI CORS middleware allows requests from localhost:3000"
    - "pypdf, python-docx, openpyxl, python-pptx, python-multipart are in pyproject.toml"
    - "test_file_extract.py and test_input_wrap.py are GREEN after this plan"
    - "D-05: all four upload formats (PDF/DOCX/XLSX/PPTX) handled best-effort in /extract/file-text"
    - "D-06: raw text wraps into VendorResponse with buyer-supplied vendor name via /input/raw-text"
  artifacts:
    - path: "services/ai/api/app.py"
      provides: "CORS middleware + 2 new endpoints + file text dispatcher"
      contains: "CORSMiddleware"
    - path: "services/ai/pyproject.toml"
      provides: "5 new Python dependencies"
      contains: "pypdf"
  key_links:
    - from: "services/ai/api/app.py"
      to: "services/ai/schemas/domain.py"
      via: "VendorResponse model import"
      pattern: "from schemas.domain import.*VendorResponse"
    - from: "POST /extract/file-text"
      to: "_extract_text dispatcher"
      via: "suffix routing"
      pattern: "_extract_text\\(content, suffix\\)"

user_setup: []
---

<objective>
Add the three backend capabilities Phase 5's UI depends on: (1) 5 Python file-parsing
dependencies, (2) FastAPI CORS middleware, (3) POST /extract/file-text (best-effort
PDF/DOCX/XLSX/PPTX → plain text), (4) POST /input/raw-text (raw text → VendorResponse JSON).
No existing agent or schema is touched.

Purpose: Without CORS the browser can't call the AI service. Without the two new endpoints,
the Input screen (INPUT-01, INPUT-02) has no backend to call. This plan unblocks all UI work.
Output: Modified app.py + pyproject.toml. Unit tests go GREEN.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/05-buyer-ui-trace-submission/05-RESEARCH.md

<interfaces>
From services/ai/api/app.py (existing patterns to follow):
  Line 26: `from __future__ import annotations`
  Line 31: `from fastapi import FastAPI, HTTPException`
  Line 32: `from pydantic import BaseModel, model_validator`
  Line 33: `from pydantic import Field as pydantic_Field`
  Lines 61–65: Request model pattern:
    class VendorGenRequest(BaseModel):
        persona: str = pydantic_Field(max_length=64)
        rfq_text: str | None = pydantic_Field(default=None, max_length=200_000)
  Line 42: `from schemas.domain import RFQ, ExtractionResult, VendorResponse`

From services/ai/schemas/domain.py:
  class VendorResponse(BaseModel):
      vendor_name: str
      persona: str
      mess_spec: list[MessSpecItem] = pydantic_Field(default_factory=list)
      source_id: str
      format_label: str
      raw_text: str

Planned endpoint contracts (D-05, D-06, RESEARCH.md Patterns 3 and 4):
  POST /extract/file-text:
    Request: multipart/form-data, field "file" (UploadFile)
    Response: {"text": str, "filename": str, "chars": int}
    Security: discard filename in _extract_text(); use only bytes + extension
    Empty-extraction threshold: 200 chars (return 200 OK regardless — caller shows Alert)

  POST /input/raw-text:
    Request: {"vendor_name": str (max 200), "raw_text": str (max 200_000)}
    Response: VendorResponse.model_dump(mode="json")

  CORSMiddleware:
    allow_origins: ["https://*.vercel.app", "http://localhost:3000"]
    allow_methods: ["GET", "POST"]
    allow_headers: ["Content-Type"]
    Add immediately after `app = FastAPI(...)` construction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 Python file-parsing dependencies to pyproject.toml and sync</name>
  <files>services/ai/pyproject.toml</files>
  <read_first>
    - services/ai/pyproject.toml (full file — current dependencies list, version pins format)
  </read_first>
  <action>
Read pyproject.toml to find the `[project]` dependencies array. Add the five new packages
in the same format as existing entries (quoted strings with `>=` version floor):

  "pypdf>=6.14.2",
  "python-docx>=1.2.0",
  "openpyxl>=3.1.5",
  "python-pptx>=1.0.2",
  "python-multipart>=0.0.32",

Place them after the existing entries in the dependencies list. Do not change any existing
entries. After editing, run `uv sync` from services/ai/ to install.

NOTE per 05-RESEARCH.md Package Legitimacy Audit: all five packages are confirmed via PyPI
API query (>10yr age, canonical source repos). slopcheck was unavailable at research time —
packages are tagged [ASSUMED]. Proceed with install; they are well-established libraries.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "import pypdf; import docx; import openpyxl; import pptx; import multipart; print('all ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import pypdf; import docx; import openpyxl; import pptx; import multipart; print('all ok')"` prints "all ok"
    - pyproject.toml contains all five package names (grep: `pypdf`, `python-docx`, `openpyxl`, `python-pptx`, `python-multipart`)
    - Existing test suite still passes: `uv run pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Five packages importable. Existing pytest suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CORS middleware + /extract/file-text + /input/raw-text endpoints in app.py</name>
  <files>services/ai/api/app.py</files>
  <read_first>
    - services/ai/api/app.py (full file — existing structure, imports, route patterns)
    - services/ai/schemas/domain.py (lines 104–120: VendorResponse fields)
    - services/ai/tests/test_file_extract.py (Wave 0 stubs — this plan makes them GREEN)
    - services/ai/tests/test_input_wrap.py (Wave 0 stub — this plan makes it GREEN)
    - .planning/phases/05-buyer-ui-trace-submission/05-RESEARCH.md (Patterns 3, 4, 5 + Security Domain)
  </read_first>
  <behavior>
    - test_raw_text_wrap_returns_valid_vendor_response: POST /input/raw-text with
      {"vendor_name": "Test Vendor", "raw_text": "some text"} → 200, VendorResponse.model_validate succeeds
    - test_pdf_returns_text_and_chars: POST /extract/file-text with PDF bytes → 200, {"text": str, "chars": int}
    - test_weak_extraction_not_an_error: tiny file → 200 (NOT 422/500), chars may be 0
    - CORS: OPTIONS request from http://localhost:3000 → 200 with Access-Control-Allow-Origin header
  </behavior>
  <action>
Modify services/ai/api/app.py. Make the following additions in order:

1. NEW IMPORT at top of imports block (after existing fastapi imports):
   `from fastapi import UploadFile, File`
   `from fastapi.middleware.cors import CORSMiddleware`

2. NEW REQUEST MODEL after VendorGenRequest (line ~65):
   class RawTextInput(BaseModel):
       vendor_name: str = pydantic_Field(max_length=200)
       raw_text: str = pydantic_Field(max_length=200_000)

3. CORS MIDDLEWARE — add immediately after `app = FastAPI(...)` line (before lifespan or
   any route definitions):
   ALLOWED_ORIGINS = ["https://*.vercel.app", "http://localhost:3000"]
   app.add_middleware(CORSMiddleware, allow_origins=ALLOWED_ORIGINS,
       allow_methods=["GET", "POST"], allow_headers=["Content-Type"])

4. PRIVATE HELPER function `_extract_text(content: bytes, suffix: str) -> str`:
   Dispatch by suffix: "pdf" → pypdf PdfReader(io.BytesIO(content)), extract page text
   joined with newlines; "docx" → python_docx Document(io.BytesIO(content)), join all
   para.text; "xlsx" → openpyxl load_workbook(io.BytesIO(content), read_only=True, data_only=True),
   iterate all cells, collect str(cell.value); "pptx" → pptx Presentation(io.BytesIO(content)),
   iterate slides/shapes/text_frame/paragraphs; default: return "".
   Security (T-05-02-A, D-05): `suffix` is derived from `file.filename.rsplit(".", 1)[-1].lower()`
   in the endpoint — the raw filename is never passed into _extract_text; path traversal impossible.

5. NEW ENDPOINT `POST /extract/file-text`:
   @app.post("/extract/file-text")
   async def extract_file_text(file: UploadFile = File(...)) -> dict:
       content = await file.read()
       suffix = (file.filename or "").rsplit(".", 1)[-1].lower()
       text = _extract_text(content, suffix)
       return {"text": text, "filename": file.filename, "chars": len(text)}
   File size guard: if len(content) > 20_000_000: raise HTTPException(status_code=413, detail="File too large (>20 MB)")

6. NEW ENDPOINT `POST /input/raw-text`:
   @app.post("/input/raw-text")
   async def wrap_raw_text(req: RawTextInput) -> dict:
       vendor = VendorResponse(vendor_name=req.vendor_name, persona="buyer-upload",
           mess_spec=[], source_id=f"upload-{req.vendor_name[:20]}",
           format_label="text", raw_text=req.raw_text)
       return vendor.model_dump(mode="json")

Add required `import io` at the top of the file imports (stdlib, already safe).
Add `import pypdf`, `import docx` (python-docx), `import openpyxl`, `import pptx` after
the fastapi imports. Use try/except around each parser call — on exception return "" (best-effort).

Remove the `pytest.mark.skip` from test_file_extract.py and test_input_wrap.py stubs after
implementing, then run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_file_extract.py tests/test_input_wrap.py -v && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_file_extract.py tests/test_input_wrap.py -v` exits 0 with all tests PASSED (not skipped)
    - `uv run pytest tests/ -x -q` exits 0 (full suite, no regressions)
    - `grep -c "CORSMiddleware" services/ai/api/app.py` returns 1 (middleware added exactly once)
    - `grep -c "/extract/file-text" services/ai/api/app.py` returns 1
    - `grep -c "/input/raw-text" services/ai/api/app.py` returns 1
    - `grep -c "max_length=200_000" services/ai/api/app.py` returns ≥1 (DoS guard on raw_text)
    - `grep -c "413" services/ai/api/app.py` returns ≥1 (file size guard)
  </acceptance_criteria>
  <done>test_file_extract.py + test_input_wrap.py GREEN. Full pytest suite passes. CORS and both endpoints present in app.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /extract/file-text | Untrusted binary file upload crosses here |
| browser → /input/raw-text | Untrusted free-text paste crosses here |
| FastAPI CORS → browser | Origin gating for SSE and API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-A | Tampering | /extract/file-text filename param | mitigate | Discard filename in _extract_text(); use only bytes + lowercased extension. Path traversal impossible. |
| T-05-02-B | DoS | /extract/file-text | mitigate | Reject files > 20 MB with 413 before reading into memory |
| T-05-02-C | DoS | /input/raw-text raw_text field | mitigate | pydantic_Field(max_length=200_000) — same cap as ExtractionRequest.raw_text |
| T-05-02-D | Info Disclosure | CORS allow_origins wildcard | mitigate | Explicit allow_origins list (vercel.app + localhost); no wildcard "*"; key stays server-only (D-24) |
| T-05-02-SC | Tampering | pypdf/python-docx/openpyxl/python-pptx/python-multipart installs | accept | 05-RESEARCH.md Package Legitimacy Audit confirms all 5 via PyPI API (>10yr age, canonical repos); slopcheck unavailable but risk is low |
</threat_model>

<verification>
1. `cd services/ai && uv run pytest tests/test_file_extract.py tests/test_input_wrap.py -v` → all PASSED
2. `uv run pytest tests/ -x -q` → full suite green, no regressions
3. `curl -X OPTIONS http://localhost:8000/input/raw-text -H "Origin: http://localhost:3000" -i` → Access-Control-Allow-Origin header present (requires running uvicorn)
</verification>

<success_criteria>
test_file_extract.py (5 tests) and test_input_wrap.py (1 test) GREEN. All existing tests
still pass. CORS, /extract/file-text, and /input/raw-text are in app.py with security guards.
</success_criteria>

<output>
Create `.planning/phases/05-buyer-ui-trace-submission/05-02-SUMMARY.md` when done.
</output>
