---
phase: 04-comparison-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - services/ai/schemas/domain.py
  - packages/shared-types/index.ts
autonomous: true
requirements:
  - COMPARE-01
  - COMPARE-02
  - COMPARE-04
  - COMPARE-05

must_haves:
  truths:
    - "ComparisonResult stub (2-field) is replaced by full schema covering all D-01..D-07 shapes — incl. the two complementary surfaces: the 6-dimension comparability matrix and the 8-line-item×vendor verbatim offer table (D-06)"
    - "ComparisonDraft schema added as the MODEL's structured output target — carries model_proposed verdicts + phrasing only; ComparisonResult is code-constructed, not model-emitted (Review Fix 1+2, per D-03)"
    - "DimensionVerdictDraft added — model emits proposed verdict + narrative per dimension; DimensionVerdict (code-constructed) carries the clamped verdict"
    - "ComparisonDimension(StrEnum) added in domain.py — dimension is a typed enum not a free str; _apply_verdict_clamp uses it for fail-closed key lookup (Review Fix 1)"
    - "ClarificationSet moved into domain.py next to ClarificationQuestion (Review Fix 12)"
    - "ComparabilityVerdict StrEnum lives in domain.py, NOT in envelope.py (resolves WR-01, per D-02)"
    - "All sub-models use list[BaseModel] — no dict[str, Model] shapes"
    - "clamp_report: ClampReport is a field ON ComparisonResult (not a sibling key)"
    - "pydantic2ts codegen drift-check passes (packages/shared-types regenerated)"
    - "test_schema_shape and test_no_dict_shapes go GREEN"
  artifacts:
    - path: "services/ai/schemas/domain.py"
      provides: "Full ComparisonResult + ComparisonDraft schemas with 12+ sub-models; ComparisonDimension StrEnum"
      contains: "class ComparabilityVerdict(StrEnum)"
    - path: "packages/shared-types/index.ts"
      provides: "TS contract regenerated from new ComparisonResult + ComparisonDraft"
      contains: "ComparabilityVerdict"
  key_links:
    - from: "services/ai/schemas/domain.py"
      to: "services/ai/schemas/envelope.py"
      via: "reads FlagStatus; ComparabilityVerdict is separate (D-02)"
      pattern: "class ComparabilityVerdict"
    - from: "packages/shared-types/index.ts"
      to: "services/ai/schemas/domain.py"
      via: "pydantic2ts codegen"
      pattern: "ComparisonResult"
    - from: "services/ai/agents/comparison.py (Wave 3)"
      to: "services/ai/schemas/domain.py"
      via: ".with_structured_output(ComparisonDraft) — model target; ComparisonResult constructed in code"
      pattern: "ComparisonDraft"
---

<objective>
Wave 2: Flesh out the ComparisonResult schema, add the ComparisonDraft model-output split, and regenerate the TS contract.

Purpose: The 2-field ComparisonResult stub is replaced by the full schema. The most important addition over the pre-review plan is the model-draft / code-result split: ComparisonDraft is what the model emits; ComparisonResult is what code constructs and what crosses the SSE boundary. This split closes the "model authors reliability-critical surfaces" BLOCKER (Consensus Fix 1+2, CLAUDE.md §2). ComparisonDimension(StrEnum) closes the fail-open dimension key join (Consensus Fix 1). ClarificationSet moves to domain.py (Consensus Fix 12). Regenerating shared-types satisfies PLAT-02 and makes test_schema_shape and test_no_dict_shapes go GREEN.

Output: domain.py with full ComparisonResult family (13+ models + 2 StrEnums) + regenerated packages/shared-types/index.ts
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-01-SUMMARY.md

<interfaces>
<!-- Key patterns to replicate exactly. -->

From services/ai/schemas/domain.py lines 18–23 (import block — keep as-is, add StrEnum):
  from __future__ import annotations
  from pydantic import BaseModel, ConfigDict, model_validator
  from pydantic import Field as pydantic_Field
  from schemas.envelope import Field

From services/ai/schemas/envelope.py lines 27–37 (FlagStatus StrEnum pattern to mirror for ComparabilityVerdict):
  class FlagStatus(StrEnum):
      present = "present"
      missing = "missing"
      unclear = "unclear"
      conflicting = "conflicting"
      unsupported = "unsupported"

From services/ai/schemas/domain.py lines 34–38 (ConfigDict pattern — every model):
  model_config = ConfigDict(extra="forbid")

From services/ai/schemas/domain.py lines 116–160 (list[BaseModel] not dict[str, Model] pattern):
  line_items: list[LineItemExtraction] = pydantic_Field(default_factory=list)
  compliance_points: list[Field[str]] = pydantic_Field(default_factory=list)

From services/ai/grounding/report.py lines 17–47 (ClampEntry/ClampReport mirrors DowngradeEntry/DowngradeReport):
  class DowngradeEntry(BaseModel):
      model_config = ConfigDict(extra="forbid")
      field_path: str
      original_status: str
      ...
  class DowngradeReport(BaseModel):
      entries: list[DowngradeEntry] = pydantic_Field(default_factory=list)
      @property
      def has_downgrades(self) -> bool: ...

From services/ai/schemas/domain.py lines 163–174 (stub to DELETE):
  vendor_count: Field[int] = Field[int](status="missing")  # type: ignore[call-arg]
  comparable: Field[str] = Field[str](status="missing")    # type: ignore[call-arg]

pydantic2ts run command (from services/ai/scripts/codegen.py):
  uv run python scripts/codegen.py
Drift-check test:
  uv run pytest tests/test_codegen_drift.py -x
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComparisonResult + ComparisonDraft schema redesign in domain.py</name>
  <files>services/ai/schemas/domain.py</files>

  <read_first>
    - services/ai/schemas/domain.py (entire file, lines 1–175) — existing file to modify; note the stub at lines 163–174
    - services/ai/schemas/envelope.py (lines 27–37) — FlagStatus StrEnum to mirror for ComparabilityVerdict
    - services/ai/grounding/report.py (lines 17–47) — DowngradeEntry/DowngradeReport to mirror for ClampEntry/ClampReport
    - .planning/phases/04-comparison-agent/04-RESEARCH.md (Pattern 4 — ComparisonResult schema) — exact field names
    - .planning/phases/04-comparison-agent/04-PATTERNS.md (domain.py section) — stub lines to delete, pattern to apply
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Summary Agreed Concerns 1, 2, 3, 12) — draft/result split, ClarificationSet location
  </read_first>

  <action>
    Modify services/ai/schemas/domain.py as follows. Keep the file header docstring updated.

    ADD to imports at top: `from enum import StrEnum` (needed for ComparabilityVerdict and ComparisonDimension).

    ADD these new types in order, BEFORE the ComparisonResult class:

    1. ComparabilityVerdict(StrEnum) — comparison-level verdict. Three members:
       comparable = "comparable", partially = "partially", not_comparable = "not_comparable"
       Docstring: "Comparison-level verdict — NOT a field-level FlagStatus (D-02). Lives in domain.py, never in envelope.py."

    2. ComparisonDimension(StrEnum) — the 6 typed dimension names (Review Fix 1 — prevents free-string key join bypass):
       technical = "technical", commercial = "commercial", scope = "scope",
       timeline = "timeline", compliance = "compliance", risk = "risk"
       Docstring: "# ponytail: typed enum prevents the free-str dimension key join that would let a mis-cased model-returned dimension name bypass the clamp (Review Fix 1, D-03)."

    3. ClampEntry(BaseModel) — one verdict downgrade record (mirrors DowngradeEntry from report.py).
       Fields: vendor_name(str), dimension(str), model_proposed(str), code_ceiling(str), clamped_to(str), ceiling_reason(str)

    4. ClampReport(BaseModel) — full collection of clamp entries (mirrors DowngradeReport).
       Fields: entries: list[ClampEntry] = pydantic_Field(default_factory=list)
       Property: has_downgrades -> bool (returns len(self.entries) > 0)
       Docstring: "# ponytail: mirrors DowngradeReport from grounding/report.py — same code-authority-over-model pattern at the comparison level (D-03/D-11)."

    5. DimensionVerdictDraft(BaseModel) — MODEL-EMITTED per-vendor verdict proposal (Review Fix 1+2):
       Fields: vendor_name(str), model_proposed(ComparabilityVerdict), reason(str)
       Docstring: "Model-emitted draft verdict for one vendor on one dimension. Carries model_proposed (required — D-11 trace diff) and buyer-facing reason text. Code constructs DimensionVerdict from this after clamping. Per Review Fix 1+2."

    6. DimensionComparisonDraft(BaseModel) — MODEL-EMITTED one-row draft (Review Fix 1+2):
       Fields: dimension(str), verdicts: list[DimensionVerdictDraft], narrative(str)
       Comment on dimension: "# model emits str; Wave 3 _apply_verdict_clamp coerces to ComparisonDimension(StrEnum) — fails closed on unrecognized value (Review Fix 1)"

    7. ComparisonDraft(BaseModel) — THE MODEL'S STRUCTURED OUTPUT TARGET (Review Fix 1+2 BLOCKER):
       Fields: dimensions: list[DimensionComparisonDraft], narrative_summary: str | None = None
       Docstring (critical — must be verbatim): "Model-emitted draft. The model proposes per-dimension verdicts and phrasing ONLY. Code constructs ComparisonResult from this draft — the offer table, vendor_readiness, attention_points, clarification_questions, and clamp_report are ALL CODE-BUILT, never model-authored. Wave 3 uses .with_structured_output(ComparisonDraft, method='json_schema', include_raw=True). (CLAUDE.md §2 / D-03 / Review Fix 1+2)"

    8. DimensionVerdict(BaseModel) — CODE-CONSTRUCTED per-vendor verdict cell (D-01):
       Fields: vendor_name(str), verdict(ComparabilityVerdict), reason(str), model_proposed(ComparabilityVerdict)
       Comment on model_proposed: "# pre-clamp verdict — kept for the D-11 trace diff (D-11)"

    9. DimensionComparison(BaseModel) — CODE-CONSTRUCTED one row of the badge matrix (D-01/D-06):
       Fields: dimension(ComparisonDimension), verdicts: list[DimensionVerdict], narrative(str)
       Note: dimension is ComparisonDimension (typed), not str — this is enforced at construction time

    10. LineItemOffer(BaseModel) — one cell of the 8×vendor offer table (D-06):
        Fields: line_item_id(str), line_item_name(str), vendor_name(str),
        pricing_verbatim: str | None = None, pricing_status(str), scope_verbatim: str | None = None,
        scope_status(str), non_equivalence_flag: str | None = None
        Comment on non_equivalence_flag: "# e.g. 'bundled — not separable', 'quoted EUR vs USD'"
        Docstring: "Code-built from ExtractionResult.line_items[*] verbatim values. Never model-authored. (Review Fix 6 / D-05)"

    11. VendorReadiness(BaseModel) — per-vendor qualitative readiness (D-07):
        Fields: vendor_name(str), comparable_count(int), total_dimensions(int), descriptor(str)
        Add: `# ponytail: no sort key, no rank field — D-07 guardrail. vendors are never ordered by this count.`
        Docstring: "Code-built from the clamped dimension matrix. Never model-authored."

    12. AttentionPoint(BaseModel) — one buyer attention point (D-08): code-triggered, model-phrased.
        Fields: trigger_type(str), summary(str), vendors_affected: list[str] = pydantic_Field(default_factory=list),
        dimension_or_field: str | None = None
        Comment on trigger_type: "# 'comparability_blocker'|'missing_pricing'|'cross_vendor_conflict'|'compliance_gap'|'clarification_generation_failed'"
        Note: 'clarification_generation_failed' trigger type is new (Review Fix 8) — added to the comment.

    13. ClarificationQuestion(BaseModel) — one question per flagged field (D-09/D-10):
        Fields: vendor_name(str), field_path(str), flag_status(str), question(str), why_needed(str)

    14. ClarificationSet(BaseModel) — model's clarification output wrapper (Review Fix 12 — moved from comparison.py):
        Fields: questions: list[ClarificationQuestion] = pydantic_Field(default_factory=list)
        Docstring: "Model-emitted clarification output. Moved to domain.py so it is in the contract/drift-check scope (Review Fix 12). Code validates question count + identity against _collect_flagged_fields before accepting."

    15. FlaggedField(BaseModel) — input item for the clarification seeding (D-09):
        Fields: vendor_name(str), field_path(str), flag_status(str), field_context: str | None = None
        Docstring: "Code-collected flagged field entry passed to the clarification prompt. Never model-generated — always produced by _collect_flagged_fields()."

    REPLACE the ComparisonResult stub (delete both vendor_count and comparable fields):

    class ComparisonResult(BaseModel):
        """Full comparison output — code-constructed from ComparisonDraft + code-built surfaces.
        This is NEVER the model's structured output target — use ComparisonDraft for that.
        Replaces the Phase 4 stub (D-01..D-07 / Review Fix 1+2).
        """
        model_config = ConfigDict(extra="forbid")
        vendor_names: list[str]  # input order preserved, never sorted (D-07)
        dimensions: list[DimensionComparison]  # 6 entries, one per dimension; code-clamped
        line_item_offers: list[LineItemOffer] = pydantic_Field(default_factory=list)  # code-built from ExtractionResult (Review Fix 6)
        vendor_readiness: list[VendorReadiness] = pydantic_Field(default_factory=list)  # code-built; N entries, input order
        attention_points: list[AttentionPoint] = pydantic_Field(default_factory=list)  # code-triggered shells (Review Fix 7)
        clarification_questions: list[ClarificationQuestion] = pydantic_Field(default_factory=list)  # validated against _collect_flagged_fields (Review Fix 8)
        clamp_report: ClampReport  # rides the result payload; D-11 trace + Phase 5 trace viewer

    UPDATE the module docstring to reflect Phase 4 additions and the draft/result split.

    DO NOT touch any existing class before ComparisonResult (MessSpecItem, LineItem, RFQ, VendorResponse, LineItemExtraction, ExtractionResult) — they are correct and complete.
    DO NOT add `not_comparable` to FlagStatus in envelope.py — per D-02 this is resolved by ComparabilityVerdict here.
  </action>

  <behavior>
    - ComparisonResult.model_fields has: vendor_names, dimensions, line_item_offers, vendor_readiness, attention_points, clarification_questions, clamp_report
    - ComparisonResult.model_fields has NO field named: vendor_count, comparable, score, rank, weight
    - ComparisonDraft.model_fields has: dimensions, narrative_summary (the model target, not ComparisonResult)
    - ComparabilityVerdict("not_comparable") == "not_comparable" (StrEnum value equality)
    - ComparisonDimension("technical") == "technical" (StrEnum value equality)
    - ComparisonDimension("Commercial") raises ValueError (case-sensitive StrEnum — enforces fail-closed)
    - ClampReport(entries=[]).has_downgrades is False
    - ClarificationSet is importable from schemas.domain (not buried in agents/comparison.py)
    - All sub-models validate with extra="forbid"
  </behavior>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "
from schemas.domain import (ComparisonResult, ComparisonDraft, ComparisonDimension,
    ComparabilityVerdict, ClampReport, ClampEntry, DimensionComparison, DimensionVerdict,
    DimensionVerdictDraft, DimensionComparisonDraft, LineItemOffer, VendorReadiness,
    AttentionPoint, ClarificationQuestion, ClarificationSet, FlaggedField)
r = ComparisonResult(vendor_names=['v1'], dimensions=[], clamp_report=ClampReport())
assert 'score' not in r.model_fields and 'rank' not in r.model_fields
assert ComparabilityVerdict.not_comparable == 'not_comparable'
assert not ClampReport(entries=[]).has_downgrades
# ComparisonDraft is the model target, not ComparisonResult
assert 'dimensions' in ComparisonDraft.model_fields
assert 'clamp_report' not in ComparisonDraft.model_fields
# ComparisonDimension is typed StrEnum
assert ComparisonDimension('technical') == 'technical'
try:
    ComparisonDimension('Commercial')
    assert False, 'should have raised'
except ValueError:
    pass  # fail-closed confirmed
# ClarificationSet in domain.py
assert 'questions' in ClarificationSet.model_fields
print('schema OK')
"</automated>
  </verify>

  <done>All new types import cleanly from schemas.domain; ComparisonResult validates with required fields; ComparisonDraft is the model target (no clamp_report); ComparisonDimension is a StrEnum that rejects unknown values; ClarificationSet lives in domain.py; no score/rank fields; ComparabilityVerdict lives in domain.py not envelope.py; full suite still green (uv run pytest -q).</done>
</task>

<task type="auto">
  <name>Task 2: Codegen drift-check + test_schema_shape GREEN</name>
  <files>packages/shared-types/index.ts</files>

  <read_first>
    - services/ai/scripts/codegen.py (entire file) — codegen invocation pattern
    - services/ai/tests/test_codegen_drift.py — existing drift-check test to run
    - services/ai/tests/test_comparison_agent.py (test_schema_shape, test_no_dict_shapes stubs) — replace NotImplementedError with real assertions
  </read_first>

  <action>
    Step 1 — Run codegen: cd services/ai && uv run python scripts/codegen.py
    This regenerates packages/shared-types/index.ts from the updated domain.py + envelope.py.
    Confirm ComparisonResult, ComparisonDraft, ComparisonDimension, ComparabilityVerdict, DimensionComparison, DimensionVerdict, DimensionVerdictDraft, DimensionComparisonDraft, LineItemOffer, VendorReadiness, AttentionPoint, ClarificationQuestion, ClarificationSet, ClampReport, ClampEntry, FlaggedField all appear in index.ts.

    Step 2 — Run drift-check: cd services/ai && uv run pytest tests/test_codegen_drift.py -x
    This must pass. If it fails because pydantic2ts generates unexpected Record<string, ...> shapes, that means a dict[str, Model] leaked into domain.py — fix the schema field (use list[Model] instead) and re-run codegen.

    Step 3 — Make test_schema_shape and test_no_dict_shapes GREEN in test_comparison_agent.py:

    test_schema_shape:
    - Assert ComparisonResult.model_fields contains: vendor_names, dimensions, line_item_offers, vendor_readiness, attention_points, clarification_questions, clamp_report
    - Assert 'score' not in ComparisonResult.model_fields
    - Assert 'rank' not in ComparisonResult.model_fields
    - Assert 'vendor_count' not in ComparisonResult.model_fields (old stub field must be gone)
    - Assert isinstance(ComparabilityVerdict.comparable, str) (it is a StrEnum, not imported from envelope)
    - Assert ComparabilityVerdict.__module__ contains 'domain' (not 'envelope') — enforces D-02
    - Assert ComparisonDraft.model_fields contains 'dimensions' but NOT 'clamp_report' (draft/result split — Review Fix 1+2)
    - Assert 'questions' in ClarificationSet.model_fields and ClarificationSet.__module__ contains 'domain' (Review Fix 12)
    - Assert ComparisonDimension.__module__ contains 'domain' (typed StrEnum present — Review Fix 1)

    test_no_dict_shapes:
    - For each field in ComparisonResult.model_fields.values(): assert its annotation does NOT have __origin__ == dict
    - Use typing.get_args and typing.get_origin to check. list[DimensionComparison] has __origin__ == list; dict[str, X] would have __origin__ == dict.
    - Assertion message: "ComparisonResult must use list[Model] not dict[str, Model] — pydantic2ts compat (PLAT-02)"

    Do NOT modify any other test stubs — they remain NotImplementedError until Wave 3/4.
  </action>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_codegen_drift.py tests/test_comparison_agent.py::test_schema_shape tests/test_comparison_agent.py::test_no_dict_shapes -v 2>&1 | tail -10</automated>
  </verify>

  <done>test_codegen_drift.py passes; test_schema_shape passes (including ComparisonDraft split + ClarificationSet in domain assertions); test_no_dict_shapes passes; packages/shared-types/index.ts contains ComparabilityVerdict, ComparisonResult, and ComparisonDraft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain.py → envelope.py | ComparabilityVerdict must NOT be added to FlagStatus (D-02 / WR-01 resolution) |
| domain.py → packages/shared-types | pydantic2ts generates TS from pydantic; dict[str,Model] breaks type safety in UI |
| ComparisonDraft → ComparisonResult | Model emits ComparisonDraft only; code constructs ComparisonResult — the split is enforced at schema level (ComparisonDraft has no clamp_report, no line_item_offers, no vendor_readiness) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | ComparabilityVerdict added to envelope.py FlagStatus | mitigate | test_schema_shape asserts ComparabilityVerdict.__module__ contains 'domain', not 'envelope' |
| T-04-02-02 | Tampering | dict[str, Model] shape in ComparisonResult breaks TS contract | mitigate | test_no_dict_shapes asserts no dict origin; drift-check test catches pydantic2ts Record<> output |
| T-04-02-03 | Spoofing | vendor_count / comparable stub fields left in schema, confusing agents | mitigate | test_schema_shape asserts 'vendor_count' not in model_fields |
| T-04-02-04 | Tampering | Model-authored clamp_report or line_item_offers if ComparisonResult used as structured output target | mitigate | ComparisonDraft (the model target) has no clamp_report, no line_item_offers, no vendor_readiness — schema makes it structurally impossible (Review Fix 1+2). test_schema_shape asserts 'clamp_report' not in ComparisonDraft.model_fields |
| T-04-02-05 | Tampering | Free-string dimension key join: "Commercial" (mis-cased) bypasses clamp | mitigate | ComparisonDimension(StrEnum) in domain.py; _apply_verdict_clamp (Wave 3) coerces to StrEnum, defaults unknown to not_comparable (Review Fix 1). test_dimension_enum_fail_closed stub verifies. |
| T-04-02-06 | Tampering | ClarificationSet defined outside domain.py drift-check scope | mitigate | ClarificationSet moved into domain.py; codegen picks it up; test_schema_shape asserts __module__ contains 'domain' (Review Fix 12) |
</threat_model>

<verification>
Full suite after Wave 2:
cd /Users/aayush/projects/aerchain/services/ai && uv run pytest -q 2>&1 | tail -5
Expected: all previously green tests remain green; test_schema_shape and test_no_dict_shapes newly green; remaining comparison stubs still FAILED (NotImplementedError).
</verification>

<success_criteria>
- domain.py has ComparabilityVerdict, ComparisonDimension, ClampEntry, ClampReport, DimensionVerdictDraft, DimensionComparisonDraft, ComparisonDraft, DimensionVerdict, DimensionComparison, LineItemOffer, VendorReadiness, AttentionPoint, ClarificationQuestion, ClarificationSet, FlaggedField, ComparisonResult (full)
- ComparisonDraft is the model target (no clamp_report, no line_item_offers, no vendor_readiness)
- ComparisonDimension is a typed StrEnum (6 members)
- ClarificationSet lives in domain.py (not comparison.py)
- Old stub fields vendor_count and comparable are gone
- packages/shared-types/index.ts contains ComparisonResult, ComparisonDraft, and ComparabilityVerdict
- test_codegen_drift.py, test_schema_shape, test_no_dict_shapes all pass
- uv run pytest -q shows no regressions
</success_criteria>

<output>
Create .planning/phases/04-comparison-agent/04-02-SUMMARY.md when done.
</output>
