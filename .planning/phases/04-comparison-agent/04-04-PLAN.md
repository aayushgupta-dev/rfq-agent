---
phase: 04-comparison-agent
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - services/ai/prompts/comparison.v1.md
  - services/ai/prompts/clarification.v1.md
  - services/ai/scripts/capture_comparison_trace.py
  - docs/traces/comparison_trace_1.json
  - docs/traces/comparison_trace_1.md
autonomous: false
requirements:
  - COMPARE-01
  - COMPARE-02
  - COMPARE-03
  - COMPARE-04
  - COMPARE-05

must_haves:
  truths:
    - "comparison.v1.md is a complete prompt with role framing, verdict definitions, model_proposed requirement, humility instruction, no-normalization instruction, and few-shot examples (D-05, Review Fix 3)"
    - "clarification.v1.md is a complete prompt with strict-count instruction (one grounded question per flagged field, no arbitrary cap, D-10)"
    - ">=1 comparison trace committed under docs/traces/ with clamp_step showing >=1 verdict downgrade (D-11)"
    - "Trace captured from a DETERMINISTIC injected over-optimistic raw draft (fixture mode) — not dependent on live model misbehavior (Review Fix 5)"
    - "Trace script asserts has_downgrades and fails hard otherwise (Review Fix 5)"
    - "test_comparison_traces_committed goes GREEN (all 20 stubs now green)"
    - "comparison.v1.md explicitly requires the model to emit model_proposed per dimension/vendor (Review Fix 3)"
    - "Human checkpoint: buyer reviews prompt quality and trace readability"
    - "D-01..D-11 decisions each covered in must_haves (decision coverage 11/11)"
  artifacts:
    - path: "services/ai/prompts/comparison.v1.md"
      provides: "Full comparison prompt — 30% grade artifact"
      min_lines: 120
    - path: "services/ai/prompts/clarification.v1.md"
      provides: "Full clarification prompt"
      min_lines: 60
    - path: "services/ai/scripts/capture_comparison_trace.py"
      provides: "Trace capture script with fixture-mode injection (Review Fix 5)"
      min_lines: 100
    - path: "docs/traces/comparison_trace_1.json"
      provides: "Committed comparison trace with clamp_step.entries >= 1"
      contains: "clamp_step"
    - path: "docs/traces/comparison_trace_1.md"
      provides: "Rendered Markdown trace for Aerchain reviewer"
  key_links:
    - from: "services/ai/prompts/comparison.v1.md"
      to: "services/ai/agents/comparison.py"
      via: "load('comparison') in _comparison_chain; model must emit ComparisonDraft with model_proposed"
      pattern: "id: comparison"
    - from: "docs/traces/comparison_trace_1.json"
      to: "services/ai/agents/comparison.py"
      via: "capture_comparison_trace.py calls generate_comparison_with_trace; fixture mode injects raw draft"
      pattern: "clamp_step"
---

<objective>
Wave 4: Author the two full prompts, capture the comparison trace in fixture mode, and close the human checkpoint.

Purpose: The comparison.v1.md and clarification.v1.md prompts are the highest-weighted deliverable (30% of grade). Two review fixes land here: (1) comparison.v1.md must explicitly require model_proposed per dimension/vendor (Consensus Fix 3 BLOCKER — the clamp diff / trace depends on it), and (2) the trace must be captured from a deterministic injected over-optimistic draft rather than live model misbehavior (Consensus Fix 5 HIGH). The trace script uses fixture mode — a hardcoded ComparisonDraft where cheap-but-incomplete is over-optimistically "comparable" — then runs the real clamp, producing a guaranteed clamp diff. This completes all 20 test stubs and gates on human review.

Output: Two complete prompts + trace script (fixture mode) + >=1 committed comparison trace (JSON + Markdown)
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-03-SUMMARY.md

<interfaces>
<!-- Prompt Pack and trace patterns. Read these before authoring. -->

From services/ai/prompts/extraction.v1.md (entire file — the structural template):
  - Frontmatter: id, version, intent, model_tier, failure_handling
  - Role framing opening: "You are a [role]. Your job is [specific job]."
  - Evidence contract section
  - Flag/verdict definitions section (enum-precise)
  - Humility instruction section
  - Output format section (JSON only, schema-name reference)
  - Few-shot examples section (>=3 examples)

From services/ai/prompts/comparison.v1.md (current stub):
  - Frontmatter is correct; replace the TODO body with full prompt content

From services/ai/prompts/clarification.v1.md (current stub):
  - Frontmatter is correct; replace the TODO body with full prompt content

From services/ai/scripts/capture_traces.py (entire file — mirror for comparison):
  - REPO_ROOT / DATA_DIR / TRACES_DIR path setup
  - _load_rfq() / _load_vendor() / _build_trace() / _write_json() / _write_markdown() pattern
  - main() loads 3 committed vendor extractions and drives trace for each

From docs/traces/trace_vendor_cheap.json (P3 trace shape to extend):
  - "input", "resolved_prompt", "raw_model_output", "grounding_step", "final_result" keys
  - Comparison trace ADDS: "clamp_step" section

From .planning/phases/04-comparison-agent/04-RESEARCH.md (Trace Design section):
  Required trace keys: input, resolved_prompt, raw_model_output, clamp_step, clarification_step, final_result
  clamp_step.entries must have >=1 entry for the rubric story

From .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Agreed Concern 3 and 5):
  - Fix 3: comparison.v1.md output format must explicitly require model_proposed per dimension/vendor
  - Fix 5: trace captured from deterministic injected over-optimistic draft; script asserts has_downgrades

Data files available for trace capture:
  data/rfq.json — committed RFQ
  docs/traces/ — committed extraction traces (ExtractionResult objects loadable from final_result key)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: comparison.v1.md and clarification.v1.md — full prompt authoring</name>
  <files>services/ai/prompts/comparison.v1.md, services/ai/prompts/clarification.v1.md</files>

  <read_first>
    - services/ai/prompts/extraction.v1.md (entire file) — the structural template; replicate section by section
    - services/ai/prompts/comparison.v1.md (current stub) — frontmatter to keep, TODO body to replace
    - services/ai/prompts/clarification.v1.md (current stub) — frontmatter to keep, TODO body to replace
    - .planning/phases/04-comparison-agent/04-RESEARCH.md (Pattern 6 — Comparison Prompt Design) — all 8 structural requirements for comparison.v1.md
    - .planning/phases/04-comparison-agent/04-PATTERNS.md (comparison.v1.md and clarification.v1.md sections) — exact section text and examples
    - docs/assignment.md (§22 rubric: Prompt quality 30%; §24 anti-patterns)
    - .planning/phases/04-comparison-agent/04-CONTEXT.md (D-01..D-10 decisions)
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Agreed Concerns 3 and 5) — model_proposed requirement
  </read_first>

  <action>
    COMPARISON PROMPT (comparison.v1.md):
    Replace the TODO body. Keep the frontmatter (id, version, intent, model_tier, failure_handling — update failure_handling to be more specific per D-04/D-05).

    The prompt must include these sections in order:

    1. ROLE FRAMING
       "You are a procurement comparison agent. Your job is to compare vendor proposals side by side, based solely on structured ExtractionResult data. You operate under a strict evidence contract: every fact you cite must appear in the ExtractionResult data. You never introduce new claims, infer numbers not stated, or paraphrase beyond extraction output."

    2. THE THREE COMPARABILITY VERDICTS
       Define comparable, partially, not_comparable with precision. Example for not_comparable:
       "At least one vendor has missing or unsupported data on a contributing field. Comparison on this dimension would be misleading — surface the gap. DO NOT output comparable or partially when contributing fields are missing."

    3. FIELD → DIMENSION MAPPING
       Instruct the model which ExtractionResult fields contribute to each of the 6 dimensions:
       - technical: scope_summary, line_items[*].scope_coverage
       - commercial: pricing_structure, total_price, commercial_terms, line_items[*].pricing
       - scope: line_items[*].scope_coverage (per-item completeness)
       - timeline: timeline
       - compliance: compliance_points
       - risk: risks

    4. COMPARABILITY-FIRST INSTRUCTION
       "Establish comparability per dimension BEFORE writing any narrative. Check every contributing
       field's flag status. If any vendor has status=missing or status=unsupported on a contributing
       field, that dimension is NOT COMPARABLE for that vendor. State it explicitly — do not attempt
       to rank or summarize."

    5. model_proposed REQUIREMENT (Review Fix 3 — BLOCKER):
       Include a dedicated section titled "REQUIRED: model_proposed per verdict".
       Text (must be present verbatim):
       "For every DimensionVerdictDraft in your output, you MUST populate the model_proposed field
       with your own proposed verdict for that vendor/dimension. This field records your judgment
       BEFORE any code-side clamping. It is required for the audit trail and trace diff. If you
       omit model_proposed, the trace cannot demonstrate the code-authority guarantee.
       model_proposed must be one of: 'comparable', 'partially', 'not_comparable'."
       Include a few-shot example showing model_proposed explicitly set.

    6. NO-NORMALIZATION INSTRUCTION
       "DO NOT convert currencies, split bundles, infer per-item prices from bundled totals, or
       compute unit prices. Quote verbatim values only. When offers differ in structure (bundled vs.
       itemized, EUR vs. USD), set non_equivalence_flag to describe the incompatibility:
       'bundled — not separable', 'quoted EUR vs USD'. Do NOT set it to a normalized value."

    7. ATTENTION POINTS INSTRUCTION
       "You will receive a list of attention trigger conditions detected by the system. For each
       trigger, write a buyer-facing summary (summary field on AttentionPoint). Do not add
       AttentionPoint entries for conditions not in the trigger list."

    8. HUMILITY INSTRUCTION
       "A not_comparable that prevents a misleading comparison is better than a comparable built
       on incomplete data. When in doubt: use partially over comparable, and not_comparable over
       partially."

    9. OUTPUT FORMAT
       "Return a single JSON object matching the ComparisonDraft schema (NOT ComparisonResult —
       the model emits a draft; code constructs the final result). The ComparisonDraft has:
       dimensions: list of DimensionComparisonDraft (one per dimension), each with:
         - dimension: one of 'technical'|'commercial'|'scope'|'timeline'|'compliance'|'risk'
         - verdicts: list of DimensionVerdictDraft (one per vendor), each with:
           - vendor_name: str
           - model_proposed: 'comparable'|'partially'|'not_comparable'  ← REQUIRED (Fix 3)
           - reason: str (one-line buyer-facing reason)
         - narrative: str (per-dimension synthesis)
       Do not include any explanation outside the JSON object."

    10. FEW-SHOT EXAMPLES (>=3):
        Example 1: All vendors have present timeline → DimensionVerdictDraft with model_proposed="comparable", reason: "All vendors provided delivery timelines."
        Example 2: One vendor has unclear commercial_terms → model_proposed="partially", reason: "Vendor B's payment terms are unclear; comparison is partial."
        Example 3: One vendor has missing pricing on 3 line items → model_proposed="not_comparable", reason: "Vendor C has missing pricing for TVC Production, Social Organic, and Paid Media — commercial comparison is not possible."
        Each example must show model_proposed explicitly (Review Fix 3).

    CLARIFICATION PROMPT (clarification.v1.md):
    Replace the TODO body. Keep frontmatter.

    Sections:
    1. ROLE FRAMING
    2. STRICT COUNT INSTRUCTION: "N items → exactly N questions"
    3. SPECIFICITY INSTRUCTION with REJECTED/ACCEPTED example pair
    4. ORDERING instruction
    5. OUTPUT FORMAT: ClarificationSet schema reference

    QUALITY BARS (both prompts must meet these — executor self-check):
    - Every verdict state is defined with precision
    - model_proposed is explicitly required in comparison.v1.md output format section (Review Fix 3)
    - Humility instruction is present and unambiguous
    - No-normalization instruction forbids currency conversion, bundle splitting, unit price computation
    - Attention points section says "only for items in the trigger list"
    - Clarification prompt says "exactly N questions for N items — no more, no less"
    - Each prompt has >=2 concrete examples with specific, non-generic text
  </action>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "
from prompts.registry import load
comp = load('comparison')
clar = load('clarification')
comp_body = comp.content.lower()
clar_body = clar.content.lower()
assert 'not_comparable' in comp_body, 'comparison prompt must define not_comparable'
assert 'humility' in comp_body or 'not_comparable that prevents' in comp_body
assert 'normali' in comp_body, 'comparison prompt must address normalization'
assert 'model_proposed' in comp_body, 'comparison prompt must require model_proposed (Review Fix 3)'
assert 'required' in comp_body, 'comparison prompt must mark model_proposed as required'
assert 'exactly' in clar_body or 'n items' in clar_body, 'clarification prompt must state strict count'
assert 'rejected' in clar_body or 'generic' in clar_body, 'clarification prompt must reject generic questions'
print('prompts OK — registry load works, key sections present including model_proposed requirement')
"</automated>
  </verify>

  <done>comparison.v1.md body is >=120 lines with all 10 required sections including model_proposed requirement (Review Fix 3); clarification.v1.md body is >=60 lines with strict-count instruction and specificity examples; both load cleanly via prompts.registry.load().</done>
</task>

<task type="auto">
  <name>Task 2: Fixture-mode trace script + committed comparison trace + test_comparison_traces_committed GREEN</name>
  <files>
    services/ai/scripts/capture_comparison_trace.py,
    docs/traces/comparison_trace_1.json,
    docs/traces/comparison_trace_1.md,
    services/ai/tests/test_comparison_agent.py
  </files>

  <read_first>
    - services/ai/scripts/capture_traces.py (entire file) — the exact pattern to mirror for comparison
    - docs/traces/trace_vendor_cheap.json (lines 1–50) — P3 trace JSON shape to extend with clamp_step
    - .planning/phases/04-comparison-agent/04-RESEARCH.md (Trace Design section) — required keys + clamp_step shape
    - .planning/phases/04-comparison-agent/04-PATTERNS.md (capture_comparison_trace.py section) — _write_comparison_markdown section names
    - services/ai/agents/comparison.py (generate_comparison_with_trace export + _apply_verdict_clamp + _compute_ceilings) — the authorized trace-capture surface
    - services/ai/tests/test_comparison_agent.py (test_comparison_traces_committed stub) — the assertion to satisfy
    - docs/traces/ directory — existing trace files (trace_vendor_*.json) showing committed extraction results to load
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Agreed Concern 5) — fixture-mode injection requirement
  </read_first>

  <action>
    FIXTURE-MODE TRACE STRATEGY (Review Fix 5):
    The trace must demonstrate clamp downgrades without depending on live model misbehavior.
    Strategy: construct a DETERMINISTIC over-optimistic ComparisonDraft in code, inject it as
    the "raw model output" (bypassing the actual model call), then run the real _apply_verdict_clamp
    against the real extractions. The clamp diff is guaranteed because the fixture draft is
    designed to propose "comparable" for the cheap-but-incomplete vendor on commercial/scope.

    Step 1 — Create services/ai/scripts/capture_comparison_trace.py (mirror capture_traces.py):

    REPO_ROOT = pathlib.Path(__file__).parents[3]
    DATA_DIR = REPO_ROOT / "data"
    TRACES_DIR = REPO_ROOT / "docs" / "traces"

    Strategy for loading ExtractionResult inputs:
    Load the 3 committed extraction traces from docs/traces/ as ExtractionResult objects using
    ExtractionResult.model_validate(json.loads(trace_json["final_result"])).
    Fall back to running live extraction if the trace files lack a parseable final_result.

    _build_fixture_draft(extractions: list[ExtractionResult]) -> tuple[ComparisonDraft, dict]:
    Build a deterministic over-optimistic ComparisonDraft (Review Fix 5):
    - Propose "comparable" for EVERY vendor on EVERY dimension (regardless of actual flag states)
    - This is the over-optimistic fixture — the clamp will downgrade it wherever flags dictate
    - Also compute and return real ceilings via _compute_ceilings(extractions)
    # ponytail: fixture draft bypasses the model call; the real clamp runs on real ExtractionResult data.
    # This guarantees has_downgrades == True for any vendor with missing fields. (Review Fix 5)

    _build_comparison_trace(extractions: list[ExtractionResult], rfq) -> dict | None:
    - Computes real ceilings: ceilings = _compute_ceilings(extractions)
    - Builds fixture draft: draft, ceilings = _build_fixture_draft(extractions)
    - Runs real clamp: dimensions, clamp_report = _apply_verdict_clamp(draft, ceilings, vendor_names)
    - Validates clamp_report.has_downgrades BEFORE writing:
        if not clamp_report.has_downgrades:
            print("ERROR: trace has 0 clamp entries. Fix fixture draft or check extraction data.")
            sys.exit(1)  # fail hard — the trace must demonstrate downgrades (Review Fix 5)
    - Builds ComparisonResult from clamped dimensions (code-constructed, same as Wave 3 node)
    - Builds trace dict with required keys:
        input: {vendor_names, rfq_title, extraction_summaries with flag_counts}
        resolved_prompt: {id, version, system_message_excerpt — from load("comparison")}
        raw_model_output: draft.model_dump(mode="json")  # the fixture over-optimistic verdicts
        clamp_step: clamp_report.model_dump(mode="json")
        clarification_step: {flagged_fields_input: [...], note: "fixture mode — no live model call"}
        final_result: clamped_result.model_dump(mode="json")

    _write_comparison_markdown(trace, path):
    - Section 1: Input (vendor names, RFQ title, flag counts per vendor)
    - Section 2: Resolved Prompt (id, version, system message excerpt)
    - Section 3: THE VERDICT-CLAMP DIFF (this is the rubric story — MUST be non-empty)
      | Vendor | Dimension | Model Proposed | Code Ceiling | Clamped To | Ceiling Reason |
      If no entries: FAIL (script already sys.exit(1) above)
    - Section 4: Clarification Questions table (or "fixture mode — no live clarification" note)
    - Section 5: Final Result (dimension matrix table, vendor readiness)
    - Section 6: Fixture Mode Note: "This trace uses a deterministic over-optimistic fixture draft.
      Model proposed 'comparable' for all vendors on all dimensions; code clamped those that violate
      the D-04 ceiling rule. This guarantees a reproducible demonstration of the code-authority
      guarantee without depending on model misbehavior. (Review Fix 5)"
    Markdown must be readable to an Aerchain reviewer.

    main():
    - Load 3 vendor ExtractionResult objects (thorough, cheap, fluff from committed extraction traces)
    - Load rfq.json
    - Run _build_comparison_trace
    - Write comparison_trace_1.json + comparison_trace_1.md

    Step 2 — Run the script to capture the trace:
    cd services/ai && uv run python scripts/capture_comparison_trace.py
    This makes NO live OpenAI calls (fixture mode). The cheap-but-incomplete vendor will trigger
    clamp downgrades on commercial and scope dimensions (known missing pricing from P2 mess spec).
    If the script exits with code 1, the fixture draft needs adjustment — ensure the cheap vendor
    has genuinely missing pricing in the committed extraction traces.

    Step 3 — Make test_comparison_traces_committed GREEN in test_comparison_agent.py:
    Replace NotImplementedError with:
    - traces_dir = pathlib.Path(__file__).parents[3] / "docs" / "traces"
    - json_traces = list(traces_dir.glob("comparison_trace_*.json"))
    - assert len(json_traces) >= 1
    - required_keys = {"input", "resolved_prompt", "raw_model_output", "clamp_step", "final_result"}
    - For each trace: assert required_keys <= set(trace.keys())
    - assert len(trace["clamp_step"]["entries"]) >= 1, "comparison trace must show >=1 verdict downgrade for D-03 rubric (Review Fix 5)"
    - assert trace.get("_fixture_mode", False) is True or "fixture" in str(trace).lower(), "trace must document its fixture-mode provenance"

    Step 4 — Confirm all 20 tests green:
    cd services/ai && uv run pytest tests/test_comparison_agent.py -v
    Expected: 20 passed.
  </action>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_comparison_agent.py -v 2>&1 | tail -25</automated>
  </verify>

  <done>
    - capture_comparison_trace.py runs without import error in fixture mode (no live OpenAI calls)
    - docs/traces/comparison_trace_1.json exists with keys: input, resolved_prompt, raw_model_output, clamp_step (>=1 entry), clarification_step, final_result
    - docs/traces/comparison_trace_1.md exists with the verdict-clamp diff table populated
    - Trace includes a Fixture Mode Note section explaining the deterministic injection (Review Fix 5)
    - All 20 tests in test_comparison_agent.py pass
    - uv run pytest -q shows full suite green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Two full prompts (comparison.v1.md, clarification.v1.md) are authored and committed.
    The comparison.v1.md explicitly requires model_proposed per verdict (Review Fix 3).
    >=1 comparison trace is committed under docs/traces/ (comparison_trace_1.json + comparison_trace_1.md).
    The trace uses fixture-mode deterministic injection — clamp diff is guaranteed (Review Fix 5).
    All 20 comparison agent tests pass.
    The full test suite is green.
  </what-built>

  <how-to-verify>
    1. Review the comparison prompt quality:
       Open services/ai/prompts/comparison.v1.md
       Confirm: role framing is precise, verdict definitions are enum-exact, humility instruction is present,
       no-normalization instruction explicitly forbids currency conversion/bundle splitting/unit price computation,
       >=3 few-shot examples with concrete values, output format section present.
       Specifically verify: the "REQUIRED: model_proposed per verdict" section exists and states
       the field is required for the audit trail (Review Fix 3).
       Ask: "Would an Aerchain reviewer reading this prompt understand exactly how the model should behave?"

    2. Review the clarification prompt quality:
       Open services/ai/prompts/clarification.v1.md
       Confirm: strict-count instruction ("N items → exactly N questions"), REJECTED/ACCEPTED example pair
       is specific and illustrative, role framing focused, ordering instruction present.

    3. Review the comparison trace:
       Open docs/traces/comparison_trace_1.md
       Confirm: the Verdict-Clamp Diff table has >=1 row showing a model_proposed verdict that was
       downgraded by code. The table should show "comparable" in Model Proposed and "not_comparable"
       in Clamped To for the cheap-but-incomplete vendor on commercial or scope.
       Confirm: a "Fixture Mode Note" section explains the deterministic injection approach (Review Fix 5).
       Ask: "Is this trace compelling to an Aerchain reviewer? Does it clearly show code, not the model, decides comparability?"

    4. Confirm all 20 tests pass:
       cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_comparison_agent.py -v

    5. Confirm no regressions:
       cd /Users/aayush/projects/aerchain/services/ai && uv run pytest -q
  </how-to-verify>

  <resume-signal>
    Type "approved" if prompts are high quality (including model_proposed requirement), and trace shows a compelling fixture-mode clamp diff.
    Or describe specific issues (e.g., "comparison prompt missing model_proposed requirement", "trace has 0 downgrade entries") and the executor will address them before resubmitting.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comparison.v1.md prompt → model | Prompt contract must require model_proposed (Review Fix 3) and prevent model from inventing verdicts beyond extraction data; no-normalization instruction is critical |
| clarification.v1.md → model | Strict-count instruction prevents model from inventing flagged fields |
| fixture-mode trace → clamp | Fixture draft is deterministically over-optimistic; real clamp runs on real ExtractionResult — the downgrade is real code behavior, not fabricated (Review Fix 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | comparison.v1.md permits model to normalize values | mitigate | Explicit no-normalization instruction; human checkpoint reviews prompt for this section |
| T-04-04-02 | Tampering | comparison.v1.md omits model_proposed requirement; clamp trace cannot be produced | mitigate | Dedicated "REQUIRED: model_proposed" section; automated verify asserts "model_proposed" in comp_body (Review Fix 3) |
| T-04-04-03 | Tampering | clarification.v1.md allows model to generate questions for unlisted fields | mitigate | Strict-count instruction: "N items → exactly N questions" |
| T-04-04-04 | Information Disclosure | Trace captures full ExtractionResult JSON with vendor pricing data | accept | Prototype only; no PII; trace committed to docs/ as deliverable per D-11 |
| T-04-04-05 | Tampering | Trace committed with 0 clamp entries (no downgrade shown — rubric fails) | mitigate | Fixture mode guarantees has_downgrades; script fails hard with sys.exit(1) if False; test_comparison_traces_committed asserts len >= 1 (Review Fix 5) |
| T-04-04-06 | Tampering | Trace misleads reviewer if fixture nature is hidden (fabricated clamp look) | mitigate | Fixture Mode Note section in Markdown explains deterministic injection; trace JSON includes _fixture_mode flag; human checkpoint reads this section |
| T-04-04-SC | Tampering | No new package installs | accept | Zero new dependencies in this plan |
</threat_model>

<verification>
Phase gate — all criteria must be TRUE before /gsd:verify-work:
1. cd /Users/aayush/projects/aerchain/services/ai && uv run pytest -q → all tests green
2. python -c "from prompts.registry import load; comp=load('comparison'); assert 'model_proposed' in comp.content.lower(), 'model_proposed requirement missing'" → passes (Review Fix 3)
3. python -c "from prompts.registry import load; comp=load('comparison'); assert len(comp.content) > 2000, 'comparison prompt too short'" → passes
4. ls /Users/aayush/projects/aerchain/docs/traces/comparison_trace_1.json → file exists
5. python -c "import json; t=json.loads(open('/Users/aayush/projects/aerchain/docs/traces/comparison_trace_1.json').read()); assert len(t['clamp_step']['entries']) >= 1" → passes
6. Human checkpoint approved (prompt quality + trace readability + fixture mode note)
</verification>

<success_criteria>
- comparison.v1.md: complete prompt with role framing, 3 verdict definitions, model_proposed requirement, field→dimension map, comparability-first instruction, no-normalization instruction, attention points instruction, humility instruction, output format (ComparisonDraft schema), >=3 few-shot examples showing model_proposed
- clarification.v1.md: complete prompt with strict-count instruction and rejected/accepted example pair
- capture_comparison_trace.py: fixture-mode script that fails hard if has_downgrades is False
- docs/traces/comparison_trace_1.json: committed with clamp_step.entries >= 1 and _fixture_mode marker
- docs/traces/comparison_trace_1.md: readable Markdown trace with populated verdict-clamp diff table and Fixture Mode Note
- All 20 test_comparison_agent.py tests pass
- Full suite (uv run pytest -q) green
- Human checkpoint approved on prompt quality and trace readability
</success_criteria>

<output>
Create .planning/phases/04-comparison-agent/04-04-SUMMARY.md when done.
</output>
