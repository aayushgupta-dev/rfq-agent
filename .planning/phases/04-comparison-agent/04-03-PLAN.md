---
phase: 04-comparison-agent
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - services/ai/agents/comparison.py
  - services/ai/api/app.py
autonomous: true
requirements:
  - COMPARE-01
  - COMPARE-02
  - COMPARE-03
  - COMPARE-04
  - COMPARE-05

must_haves:
  truths:
    - "POST /compare/vendors accepts ExtractionResult[] + RFQ and streams SSE events"
    - "Model emits ComparisonDraft; code constructs ComparisonResult — line_item_offers built from ExtractionResult verbatim values, vendor_readiness built in input order, attention_point shells built per code trigger (Review Fix 1+2+6+7)"
    - "Verdict clamp runs server-side before the result event using ComparisonDimension(StrEnum) key — any unmatched or missing (dimension, vendor) pair defaults to not_comparable (fail closed, Review Fix 1)"
    - "The full 6×N matrix is validated and completed before the result event is emitted (Review Fix 1)"
    - "Agent never reads raw vendor text — only ExtractionResult[] (grounding boundary holds transitively)"
    - "Clarification questions are seeded by code-collected flagged fields, count + identity validated against _collect_flagged_fields; model extras rejected (Review Fix 8)"
    - "Clarification call failure surfaces an AttentionPoint(trigger_type='clarification_generation_failed') — not a silent empty list (Review Fix 8)"
    - "Exactly one result SSE event emitted — after clamp + clarification; status/partial for progress (Review Fix 9)"
    - "vendor_readiness list preserves input order; no sort key"
    - "Truncation and refusal both produce safe SSE error events"
    - "_ceiling_for_flags([]) for compliance returns at least 'partially'; for risk is explicitly handled (not fall-through to comparable, Review Fix 4)"
    - "cross_vendor_conflict detector compares differing values across vendors (not per-field conflicting status, Review Fix 10)"
    - "_MAX_VENDORS = 5 is documented as a prototype limit (Review Fix LOW)"
    - "16 of 20 test stubs go GREEN in this wave"
    - "D-01..D-11 decisions each covered by at least one GREEN test"
  artifacts:
    - path: "services/ai/agents/comparison.py"
      provides: "LangGraph StateGraph with align, comparability, compare, clarify nodes; ComparisonDraft model target; code-constructed ComparisonResult; fail-closed clamp; flag collector; offer table builder; RFQ alignment check"
      min_lines: 380
    - path: "services/ai/api/app.py"
      provides: "POST /compare/vendors SSE route + ComparisonRequest with vendor count guard"
      contains: "compare/vendors"
  key_links:
    - from: "services/ai/agents/comparison.py"
      to: "services/ai/schemas/domain.py"
      via: "imports ComparisonResult, ComparisonDraft, ComparisonDimension, ExtractionResult, FlaggedField, ClampReport, ClarificationSet"
      pattern: "from schemas.domain import"
    - from: "services/ai/api/app.py"
      to: "services/ai/agents/comparison.py"
      via: "imports comparison_graph"
      pattern: "from agents.comparison import comparison_graph"
    - from: "services/ai/agents/comparison.py"
      to: "services/ai/grounding/gate.py"
      via: "_collect_flagged_fields mirrors _walk_and_ground read-only pattern"
      pattern: "_collect_flagged_fields"
---

<objective>
Wave 3: Implement the comparison agent (StateGraph) and the POST /compare/vendors SSE route.

Purpose: This plan delivers the core reliability keystones AND closes the consensus BLOCKER fixes from the cross-AI review. The key additions over the pre-review plan: (1) the model emits ComparisonDraft (not ComparisonResult); code constructs the trusted result, (2) the clamp uses ComparisonDimension(StrEnum) and is fail-closed, (3) the full 6×N matrix is validated before emit, (4) empty-ceiling semantics for compliance/risk are explicit, (5) the offer table is code-built from ExtractionResult verbatim, (6) attention points are code-shell + model-phrasing with model extras dropped, (7) clarification identity is validated with failure surfaced as AttentionPoint, (8) exactly one result event, (9) cross-vendor conflict detects differing values not per-field flags. 16 of 20 stubs go GREEN.

Output: services/ai/agents/comparison.py (4-node StateGraph) + POST /compare/vendors in app.py
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-02-SUMMARY.md

<interfaces>
<!-- Exact patterns to replicate. Extracted from codebase. -->

From services/ai/agents/extraction.py lines 1–65 (module structure + chain setup):
  - Module docstring shape
  - Import block: asyncio, json, typing, langchain_core.messages, langchain_core.prompts,
    langgraph.config.get_stream_writer, langgraph.graph.{END, START, StateGraph},
    openai.LengthFinishReasonError, grounding.gate.ground_model, llm.factory.get_llm,
    prompts.registry.load, schemas.domain.*, schemas.events.*
  - Module-level chain: load("extraction") → ChatPromptTemplate.from_messages([SystemMessage(...), ("human", "...")]) → get_llm("reasoning").with_structured_output(Schema, method="json_schema", include_raw=True)

From services/ai/agents/extraction.py lines 88–260 (_run_extraction_impl — apply verbatim, adapt for comparison):
  - assert {"status", "result", "error"} <= set(EVENT_TYPES)  # taxonomy guard
  - Nested try/except shape: outer bare-except, inner LengthFinishReasonError catch
  - Refusal check: raw_msg.additional_kwargs.get("refusal")
  - Parse check: parsed is None or parsing_error is not None
  - Type check: isinstance(parsed, ComparisonDraft)  ← NOTE: ComparisonDraft, not ComparisonResult
  - SUCCESS PATH inside try: code constructs ComparisonResult from ComparisonDraft → clamp → emit result → return state (CR-01 pattern)
  - model_copy(update=...) for immutability (W-R4)

From services/ai/agents/extraction.py lines 257–396 (graph build + sync wrapper):
  - _extraction_node / _build_extraction_graph / extraction_graph / run_extraction pattern
  - Sync wrapper collects last_sse_event for tests

From services/ai/api/app.py lines 101–182 (ExtractionRequest + /extract/vendor route):
  - model_validator(mode="after") for payload guard
  - EventSourceResponse(_generate()) pattern
  - astream(stream_mode="custom") + EventEnvelope(**chunk).model_dump_json()

From services/ai/grounding/gate.py lines 320–366 (_walk_and_ground — adapt read-only):
  - isinstance(value, Field) → read .status
  - isinstance(value, BaseModel) → recurse
  - isinstance(value, list) → iterate with [i] indexing
  Use schemas.envelope.Field as the type to check (same as gate.py uses EnvelopeField).

From services/ai/schemas/domain.py (Wave 2 output):
  - ComparisonDraft: dimensions (list[DimensionComparisonDraft]) + narrative_summary — MODEL TARGET
  - ComparisonResult: full code-constructed result — NOT the model target
  - ComparisonDimension: StrEnum with 6 members — use for fail-closed dimension key lookup
  - ComparabilityVerdict: comparable="comparable", partially="partially", not_comparable="not_comparable"
  - ClarificationSet: questions: list[ClarificationQuestion] — now in domain.py
  - FlaggedField, ClampEntry, ClampReport, DimensionVerdict, DimensionComparison, LineItemOffer, VendorReadiness, AttentionPoint

Field → dimension contribution map (from 04-RESEARCH.md):
  technical:    scope_summary, line_items[*].scope_coverage
  commercial:   pricing_structure, total_price, commercial_terms, line_items[*].pricing
  scope:        line_items[*].scope_coverage (per-item completeness)
  timeline:     timeline
  compliance:   compliance_points (list — EMPTY LIST = ceiling "partially", per Review Fix 4)
  risk:         risks (list — EMPTY LIST = no hard ceiling per RESEARCH A2; explicitly return "comparable" for empty risk; but note in comment this means no-risk-claimed, not risk-free)

Ceiling rule (D-04 + Review Fix 4):
  missing|unsupported on ANY contributing field → not_comparable
  unclear|conflicting on ANY contributing field → at most partially
  all contributing fields present → comparable allowed
  EMPTY LIST for compliance → "partially" (not comparable — empty compliance cannot be verified)
  EMPTY LIST for risk → "comparable" (no risks claimed; see RESEARCH A2 + Review Fix 4 per-dimension explicit branch)

_VERDICT_ORDER: {"comparable": 2, "partially": 1, "not_comparable": 0}
clamp_verdict(model_verdict, code_ceiling) returns the lower of the two using _VERDICT_ORDER.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: comparison.py — 4-node LangGraph agent with draft/result split and fail-closed clamp</name>
  <files>services/ai/agents/comparison.py</files>

  <read_first>
    - services/ai/agents/extraction.py (entire file, lines 1–396) — the exact pattern to mirror; read all of it before writing
    - services/ai/grounding/gate.py (lines 320–366) — _walk_and_ground traversal to adapt as _collect_flagged_fields
    - services/ai/grounding/report.py (lines 1–47) — DowngradeReport/DowngradeEntry shape mirrored by ClampReport/ClampEntry
    - services/ai/schemas/domain.py — all new comparison types from Wave 2; pay special attention to ComparisonDraft vs ComparisonResult and ComparisonDimension StrEnum
    - services/ai/schemas/envelope.py (lines 27–37, 60–95) — FlagStatus enum + Field[T] class to check in flag collector
    - .planning/phases/04-comparison-agent/04-PATTERNS.md (comparison.py section, lines 94–325) — exact import block and patterns to apply
    - .planning/phases/04-comparison-agent/04-RESEARCH.md (Pattern 1–3 and Anti-Patterns) — clamp logic, trigger detection, clarification seeding
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Summary Agreed Concerns 1–11) — all review fixes to implement
  </read_first>

  <action>
    Create services/ai/agents/comparison.py following extraction.py's structure exactly. Key sections:

    MODULE DOCSTRING — describe the 4-node graph, draft/result split, fail-closed clamp, two-path failure detection.
    State: "The model emits ComparisonDraft (proposed verdicts + phrasing only). Code constructs ComparisonResult — the offer table, vendor_readiness, attention_point shells, clarification validation, and clamp_report are ALL code-built. (CLAUDE.md §2 / D-03 / Review Fix 1+2)"
    Include: "comparison_graph, run_comparison, generate_comparison_with_trace" as exports.

    IMPORTS — per 04-PATTERNS.md comparison.py import block. Import:
    - ComparisonDraft, ComparisonDimension, ComparisonResult, ComparabilityVerdict
    - ClarificationSet, ClarificationQuestion, FlaggedField
    - ClampReport, ClampEntry, DimensionVerdict, DimensionComparison, DimensionVerdictDraft
    - LineItemOffer, VendorReadiness, AttentionPoint
    - ExtractionResult, RFQ from schemas.domain
    - Field as EnvelopeField from schemas.envelope (alias to avoid conflict)
    - FlagStatus from schemas.envelope

    MODULE-LEVEL CHAINS — two chains:
    - _comparison_chain: load("comparison") → ChatPromptTemplate([SystemMessage, ("human", "{input}")]) | get_llm("reasoning").with_structured_output(ComparisonDraft, method="json_schema", include_raw=True)
      CRITICAL: with_structured_output(ComparisonDraft, ...) — NOT ComparisonResult.
    - _clarification_chain: load("clarification") → ChatPromptTemplate([SystemMessage, ("human", "{flagged_fields}")]) | get_llm("cheap").with_structured_output(ClarificationSet, method="json_schema", include_raw=True)

    VERDICT CLAMP UTILITIES (add before nodes):

    - _VERDICT_ORDER: dict[str, int] = {"comparable": 2, "partially": 1, "not_comparable": 0}

    - _ceiling_for_flags(flag_statuses: list[FlagStatus], dimension: ComparisonDimension) -> str:
      Implements D-04 ceiling rule WITH explicit per-dimension empty-case handling (Review Fix 4):
      - If any status is missing|unsupported → "not_comparable"
      - If any status is unclear|conflicting → "partially"
      - If flag_statuses is empty:
        - dimension == ComparisonDimension.compliance → return "partially"
          (# ponytail: empty compliance list cannot be verified as compliant — Review Fix 4)
        - dimension == ComparisonDimension.risk → return "comparable"
          (# ponytail: no risks claimed — per RESEARCH A2 empty risk is not auto-blocking; Review Fix 4)
        - Any other dimension with empty contributors → return "not_comparable"
          (# fail-closed: unknown empty dimension — Review Fix 4)
      - All present → "comparable"
      Signature includes `dimension` parameter so the empty-case branch is per-dimension.

    - clamp_verdict(model_verdict: str, code_ceiling: str) -> str:
      Returns min by _VERDICT_ORDER. Downgrade-only; code cannot upgrade.

    - _compute_ceilings(extractions: list[ExtractionResult]) -> dict[ComparisonDimension, dict[str, str]]:
      Returns {ComparisonDimension: {vendor_name: ceiling_str}} for all 6 dimensions.
      Key type is ComparisonDimension (typed), not str — makes downstream key lookup fail-closed.
      For each dimension, collect FlagStatus values of contributing fields from each ExtractionResult, then call _ceiling_for_flags(statuses, dimension).

    - _apply_verdict_clamp(draft: ComparisonDraft, ceilings: dict, vendor_names: list[str]) -> tuple[list[DimensionComparison], ClampReport]:
      FAIL-CLOSED implementation (Review Fix 1):
      1. Build a full 6×N matrix keyed by (ComparisonDimension, vendor_name) with default "not_comparable"
         for any missing (dimension, vendor) pair from the draft.
      2. For each DimensionComparisonDraft in draft.dimensions:
         a. Try ComparisonDimension(dim_draft.dimension) — catch ValueError: unknown dimension → skip, log,
            the default "not_comparable" in the matrix already handles it (fail closed).
         b. For each DimensionVerdictDraft in dim_draft.verdicts:
            - If vendor_name not in vendor_names: skip (unknown vendor, fail closed)
            - code_ceiling = ceilings[dim_enum][vendor_name]
            - clamped = clamp_verdict(vendor_draft.model_proposed, code_ceiling)
            - If clamped != vendor_draft.model_proposed: append ClampEntry to report
            - Update matrix[(dim_enum, vendor_name)] = (clamped, vendor_draft.model_proposed, vendor_draft.reason, code_ceiling)
      3. Build list[DimensionComparison] from the complete matrix (all 6 dimensions × all N vendors).
         For any (dim, vendor) not provided by the model: verdict = not_comparable (fail-closed default),
         model_proposed = not_comparable, reason = "dimension not provided by model", ceiling_reason from ceilings.
      4. Return (list[DimensionComparison], ClampReport).
      This replaces the old _apply_verdict_clamp that walked result.dimensions and could miss model-omitted pairs.

    CODE CONSTRUCTION HELPERS (Review Fix 2+6+7 — these build the ComparisonResult surfaces):

    - _build_offer_table(extractions: list[ExtractionResult], rfq: RFQ) -> list[LineItemOffer]:
      Build the offer table CODE-SIDE from ExtractionResult verbatim values (Review Fix 6 / D-05).
      For each vendor extraction, for each line_item in extraction.line_items:
        LineItemOffer(
          line_item_id=li.line_item_id, line_item_name=li.line_item_name,
          vendor_name=extraction.vendor_name,
          pricing_verbatim=li.pricing.value if li.pricing.status == FlagStatus.present else None,
          pricing_status=li.pricing.status.value,
          scope_verbatim=li.scope_coverage.value if li.scope_coverage.status == FlagStatus.present else None,
          scope_status=li.scope_coverage.status.value,
          non_equivalence_flag=None,  # set by model in compare node via attention point / narrative; table itself is code-built
        )
      # ponytail: offer table is built here from ExtractionResult, never from model output.
      # Model cannot author verbatim values — they come from the extraction directly. (Review Fix 6)

    - _build_vendor_readiness(vendor_names: list[str], dimensions: list[DimensionComparison]) -> list[VendorReadiness]:
      Build in INPUT ORDER (never sorted). For each vendor_name:
        comparable_count = count of DimensionVerdict where verdict == ComparabilityVerdict.comparable
        total_dimensions = 6
        blocked_dims = [d.dimension.value for d in dimensions if any verdict.vendor_name==v and verdict.verdict != comparable]
        descriptor = f"{comparable_count} of {total_dimensions} dimensions comparable" + ("; blocked on " + ", ".join(blocked_dims) if blocked_dims else "")
      # ponytail: list order follows vendor_names input order. No sort key. (D-07)

    - _build_attention_shells(triggers: list[dict]) -> list[AttentionPoint]:
      Build one AttentionPoint shell per code-detected trigger (model fills only summary in compare node).
      Returns AttentionPoint(trigger_type=t["trigger_type"], summary="", vendors_affected=t.get("vendors_affected",[]), dimension_or_field=t.get("dimension_or_field"))
      Shell summary is empty string — the model fills it in the compare node via attention_points_draft.
      Model-invented points (not in trigger list) are dropped in the compare node (Review Fix 7).

    FLAG COLLECTOR (add after clamp utilities):
    - _collect_flagged_fields(extractions: list[ExtractionResult]) -> list[FlaggedField]:
      Read-only variant of gate.py's _walk_and_ground. For each ExtractionResult, recursively walk
      all BaseModel fields. When isinstance(value, EnvelopeField) and value.status != FlagStatus.present,
      append FlaggedField(vendor_name=..., field_path=..., flag_status=value.status.value).
      Walk nested BaseModel fields and list items with [i] indexing.
      Sort result: comparability-blockers (missing|unsupported) first, then unclear|conflicting.

    ATTENTION TRIGGER DETECTION:
    - _detect_attention_triggers(extractions: list[ExtractionResult], ceilings: dict) -> list[dict]:
      Returns list of {"trigger_type", "dimension_or_field", "vendors_affected"} dicts.
      Trigger types:
      a. comparability_blocker: any dimension where ceiling == "not_comparable" for any vendor
      b. missing_pricing: any vendor with pricing_structure.status in (missing, unsupported)
         OR any line_item.pricing.status in (missing, unsupported)
      c. cross_vendor_conflict: same field has DIFFERENT PRESENT VALUES across vendors (Review Fix 10).
         Implementation: for each field path in {timeline, commercial_terms}, collect value strings from all
         vendors where status == FlagStatus.present. If len(set(values)) > 1 → conflict detected.
         This is a cross-vendor value comparison, NOT a check for per-field FlagStatus.conflicting.
         # ponytail: Review Fix 10 — conflicting status is per-vendor; cross-vendor conflict is different values
      d. compliance_gap: all vendors have empty compliance_points list OR all compliance_points are missing/unclear

    RFQ LINE ITEM ALIGNMENT CHECK (Review Fix 11):
    - _check_rfq_alignment(extractions: list[ExtractionResult], rfq: RFQ) -> list[str]:
      Collect rfq_line_item_ids = set of line_item_ids from rfq.line_items.
      For each extraction, collect vendor_line_item_ids = set of line_item_ids from extraction.line_items.
      If vendor_line_item_ids does not cover rfq_line_item_ids: return mismatched vendor names.
      In _run_comparability_impl: if mismatches detected, set state["scope_alignment_issues"] = mismatches.
      In _run_compare_impl: if scope_alignment_issues exist, set scope dimension ceiling to "not_comparable"
      for those vendors with reason "vendor line items do not map to RFQ's line items". Add a comparability_blocker trigger.

    FOUR NODES — each follows the _run_*_impl / _*_node split from extraction.py:

    1. _run_align_impl(state, emit): validates all state["extractions"] are ExtractionResult instances
       (isinstance check — COMPARE-01); if any is not, emit error event + return early.
       Emits status event {"message": "validating inputs", "phase": "align"}.

    2. _run_comparability_impl(state, emit): computes ceilings via _compute_ceilings; checks RFQ alignment
       via _check_rfq_alignment; stores in state["ceilings"] and state["scope_alignment_issues"].
       Detects attention triggers via _detect_attention_triggers; stores in state["triggers"].
       Emits status event {"message": "computing comparability ceilings", "phase": "comparability"}.

    3. _run_compare_impl(state, emit):
       Emits status {"message": "calling comparison model", "phase": "compare"}.
       Calls _comparison_chain with all ExtractionResult data + RFQ + attention trigger list in context.
       Apply full error-handling ladder from extraction.py (LengthFinishReasonError → comparison_truncated;
       refusal → comparison_refused; parsed None → comparison_parse_error; wrong type → comparison_unexpected_type).
       SUCCESS PATH (inside try, CR-01):
       a. _apply_verdict_clamp(parsed_draft, state["ceilings"], vendor_names) → (dimensions, clamp_report)
       b. _build_offer_table(extractions, rfq) → line_item_offers  (code-built, Review Fix 6)
       c. _build_vendor_readiness(vendor_names, dimensions) → vendor_readiness  (code-built, D-07)
       d. Build attention_point shells via _build_attention_shells(state["triggers"])
       e. Filter model's attention_point summaries: intersect against trigger list by trigger_type;
          model-invented types are dropped (only summary text is taken from model, not trigger creation).
          # ponytail: attaches model's phrasing to code-detected shells; extras dropped (Review Fix 7)
       f. Construct ComparisonResult(vendor_names=vendor_names, dimensions=dimensions,
            line_item_offers=line_item_offers, vendor_readiness=vendor_readiness,
            attention_points=attention_points, clarification_questions=[],  # filled in clarify node
            clamp_report=clamp_report)
       g. Store result in state["result"]; store offer_table and attention_points in state for clarify node.
       EMIT result event ONLY ONCE at step g — never emit result before clamp is complete (CR-01 / RESEARCH Pitfall 1 / Review Fix 9).
       Do NOT emit a second result event here.

    4. _run_clarify_impl(state, emit):
       Only runs if state has "result" and no "error".
       Collects flagged fields via _collect_flagged_fields.
       Emits status {"message": "generating clarification questions", "phase": "clarify"}.
       Calls _clarification_chain with flagged fields.
       On success:
       - Validate: assert len(clarification_result.questions) == len(flagged_fields) AND the
         (vendor_name, field_path, flag_status) identity matches (Review Fix 8).
         If mismatch: log warning; use only the questions whose identity matches; drop extras.
       - model_copy the result to add clarification_questions.
       On clarification call failure (any exception):
       - Append AttentionPoint(trigger_type="clarification_generation_failed", summary="...", vendors_affected=[], dimension_or_field=None) to result.attention_points (Review Fix 8).
       - Do NOT abort the whole comparison; continue with result as-is.
       Emit the FINAL (and ONLY) result event HERE with the updated result (Review Fix 9).
       # ponytail: one result event total — this is where it's emitted. The compare node
       # stores the pre-clarification result in state but does NOT emit it. (Review Fix 9)
       Emit done event after result.

    GRAPH BUILD: 4 nodes, 4 edges (START→align→comparability→compare→clarify→END). Compile to comparison_graph.
    NOTE: The compare node does NOT emit the result — it stores in state. The clarify node emits the single final result.
    If the clarify node is unreached (edge condition / error): the compare node's success path must emit result in that case.
    Simplest approach: have compare node emit result ONLY as a fallback if clarify is skipped. Or unconditionally let clarify emit.
    Decision: clarify always runs (no conditional edge — mirrors extraction.py). If compare stored an error, clarify sees no "result" key and skips to done. Result event is emitted by clarify node only.

    SYNC WRAPPER: run_comparison(extractions, rfq) mirrors run_extraction.
    Returns state dict with last_sse_event, result_sse_event (the result SSE event payload for test assertions).

    TRACE SURFACE: generate_comparison_with_trace(extractions, rfq) returns
    (raw_draft, clamped_result, clamp_report, clarification_questions) — used by the trace script in Wave 4.
    raw_draft is the ComparisonDraft before clamping (the model's proposed verdicts).

    IMPORTANT: _MAX_VENDORS = 5
    # ponytail: prototype limit — 5 vendors keeps the single-call context window manageable.
    # Increase if multi-vendor truncation is observed (RESEARCH Pitfall 7 / Review Fix LOW).
    Document this constant with the above comment.
  </action>

  <behavior>
    - clamp_verdict("comparable", "not_comparable") == "not_comparable"
    - clamp_verdict("comparable", "partially") == "partially"
    - clamp_verdict("not_comparable", "comparable") == "not_comparable"
    - _ceiling_for_flags([FlagStatus.missing], ComparisonDimension.commercial) == "not_comparable"
    - _ceiling_for_flags([FlagStatus.unclear], ComparisonDimension.timeline) == "partially"
    - _ceiling_for_flags([FlagStatus.present], ComparisonDimension.technical) == "comparable"
    - _ceiling_for_flags([], ComparisonDimension.compliance) == "partially"  (Review Fix 4)
    - _ceiling_for_flags([], ComparisonDimension.risk) == "comparable"  (Review Fix 4 / RESEARCH A2)
    - _ceiling_for_flags([], ComparisonDimension.technical) == "not_comparable"  (Review Fix 4 fail-closed for others)
    - _collect_flagged_fields([missing_extraction("v1")]) returns len > 0
    - _collect_flagged_fields([present_extraction("v2")]) returns len == 0
    - _build_offer_table([partial_extraction("v")], rfq=...) returns list[LineItemOffer] with pricing_verbatim=None where pricing is missing
    - run_comparison result_sse_event is exactly one event of type "result"
  </behavior>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "
from agents.comparison import clamp_verdict, _ceiling_for_flags, _collect_flagged_fields, comparison_graph
from schemas.domain import ComparisonDimension
from schemas.envelope import FlagStatus
from conftest_comparison import missing_extraction, present_extraction
assert clamp_verdict('comparable', 'not_comparable') == 'not_comparable'
assert _ceiling_for_flags([FlagStatus.missing], ComparisonDimension.commercial) == 'not_comparable'
assert _ceiling_for_flags([FlagStatus.present], ComparisonDimension.technical) == 'comparable'
assert _ceiling_for_flags([], ComparisonDimension.compliance) == 'partially', 'empty compliance must be partially'
assert _ceiling_for_flags([], ComparisonDimension.risk) == 'comparable', 'empty risk: no hard ceiling per RESEARCH A2'
assert _ceiling_for_flags([], ComparisonDimension.technical) == 'not_comparable', 'empty other: fail-closed'
missing_flags = _collect_flagged_fields([missing_extraction('v1')])
present_flags = _collect_flagged_fields([present_extraction('v2')])
assert len(missing_flags) > 0
assert len(present_flags) == 0
print('comparison.py OK')
"</automated>
  </verify>

  <done>comparison.py imports cleanly; clamp_verdict, _ceiling_for_flags (with per-dimension empty-case), _collect_flagged_fields behave correctly; _apply_verdict_clamp is fail-closed; comparison_graph is a compiled StateGraph; generate_comparison_with_trace is exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /compare/vendors route in app.py + 16 test stubs GREEN</name>
  <files>services/ai/api/app.py, services/ai/tests/test_comparison_agent.py</files>

  <read_first>
    - services/ai/api/app.py (lines 101–182) — ExtractionRequest + /extract/vendor route; replicate for /compare/vendors
    - services/ai/tests/test_extraction_agent.py (lines 200–420) — truncation/refusal/SSE taxonomy/traces test patterns to mirror
    - services/ai/tests/test_comparison_agent.py (current stubs) — 16 stubs to make GREEN in this task
    - services/ai/tests/conftest_comparison.py — missing_extraction, present_extraction, partial_extraction builders
    - .planning/phases/04-comparison-agent/04-PATTERNS.md (app.py section, lines 328–380) — ComparisonRequest + route pattern
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Summary Agreed Concerns 1–11) — review assertions to include
  </read_first>

  <action>
    ADD to services/ai/api/app.py:

    Import addition at top (after existing imports):
      from agents.comparison import comparison_graph

    ComparisonRequest class (after ExtractionRequest):
    - extractions: list[ExtractionResult]
    - rfq: RFQ
    - _MAX_VENDORS = 5
    - model_validator(mode="after") _check_vendor_count: raises ValueError if len(self.extractions) > _MAX_VENDORS
      NOTE: FastAPI turns this ValueError into a 422 Unprocessable Entity, NOT 400. The test must assert 422.
      # ponytail: _MAX_VENDORS=5 is a prototype limit — single-call context window constraint (RESEARCH Pitfall 7 / Review Fix LOW).
    - Docstring: note that no char cap needed since ExtractionResult has no raw_text; vendor count guard only.

    POST /compare/vendors route:
    - Same EventSourceResponse(_generate()) pattern as /extract/vendor
    - State dict: {"extractions": req.extractions, "rfq": req.rfq}
    - Every chunk validated through EventEnvelope(**chunk).model_dump_json() before streaming
    - Final "done" event appended after graph completes
    - Docstring: note that clamp runs server-side before result event (D-03); exactly one result event (Review Fix 9).

    MAKE 16 STUBS GREEN in test_comparison_agent.py (replace NotImplementedError with real assertions):

    ---- Carrying forward from original plan (all still needed) ----

    test_clamp_only_downgrades:
    - Assert clamp_verdict("comparable", "not_comparable") == "not_comparable"
    - Assert clamp_verdict("comparable", "partially") == "partially"
    - Assert clamp_verdict("not_comparable", "comparable") == "not_comparable" (downgrade-only)
    - Assert _ceiling_for_flags([FlagStatus.missing], ComparisonDimension.commercial) == "not_comparable"
    - Assert _ceiling_for_flags([FlagStatus.unclear], ComparisonDimension.timeline) == "partially"
    - Assert _ceiling_for_flags([FlagStatus.present], ComparisonDimension.technical) == "comparable"

    test_no_aggregation_over_missing:
    - Assert _ceiling_for_flags([FlagStatus.present, FlagStatus.missing, FlagStatus.present], ComparisonDimension.commercial) == "not_comparable"

    test_attention_points_are_triggered (tightened — Review Fix 7):
    - Build two extractions: one present_extraction, one partial_extraction (pricing missing)
    - Call _detect_attention_triggers([present, partial], ceilings={...})
    - Assert at least one trigger with trigger_type in {"missing_pricing", "comparability_blocker"}
    - Assert no trigger has trigger_type not in {"comparability_blocker", "missing_pricing", "cross_vendor_conflict", "compliance_gap"}
    - ALSO: Mock the model to return attention_points including a fabricated trigger_type="invented_trigger".
      Assert that after _build_attention_shells + model-summary assignment, the "invented_trigger" AttentionPoint is dropped (not in final result.attention_points).
      This is the "no fabricated point" guarantee — Review Fix 7.

    test_clarification_seeded_by_code (tightened — Review Fix 8):
    - Import _collect_flagged_fields
    - Build partial_extraction("cheap")
    - Call _collect_flagged_fields([partial_extraction("cheap")])
    - Assert len(result) > 0; all items are FlaggedField instances; all have vendor_name == "cheap"
    - Assert items with flag_status "missing"/"unsupported" appear before "unclear"/"conflicting" (sorted)
    - ALSO: simulate a model that returns MORE clarification questions than flagged fields → assert the extras are dropped (identity validation — Review Fix 8).

    test_offer_table_code_built (renamed from test_offer_table_verbatim, sharpened — Review Fix 6):
    - Import _build_offer_table
    - Build partial_extraction("v") with one missing_line_item
    - Call _build_offer_table([partial_extraction("v")], rfq=MagicMock(line_items=[]))
    - Assert result is list[LineItemOffer]
    - For the missing line item: assert pricing_verbatim is None and pricing_status == "missing"
    - Assert no LineItemOffer field named "normalized_price", "converted_price", "computed_price"
    - Assert schema: LineItemOffer has fields pricing_verbatim, pricing_status, scope_verbatim, scope_status, non_equivalence_flag

    test_vendor_order_preserved:
    - Build ComparisonResult with vendor_names=["a", "b", "c"], vendor_readiness in same order
    - Serialize to JSON and parse back
    - Assert parsed.vendor_names == ["a", "b", "c"]
    - Assert parsed.vendor_readiness[0].vendor_name == "a"

    test_no_numeric_score:
    - Assert "score" not in ComparisonResult.model_fields
    - Assert "rank" not in ComparisonResult.model_fields
    - Assert "weight" not in ComparisonResult.model_fields
    - Also check VendorReadiness.model_fields has no "score" or "rank" field

    test_truncation_error_event:
    - Patch agents.comparison._comparison_chain; make mock raise LengthFinishReasonError(completion=MagicMock())
    - Call run_comparison; assert state["error"] == "truncated"
    - Assert state["last_sse_event"]["type"] == "error"
    - Assert state["last_sse_event"]["payload"]["recoverable"] is True

    test_refusal_error_event:
    - Patch agents.comparison._comparison_chain; make raw msg have additional_kwargs={"refusal": "cannot process"}
    - Call run_comparison; assert state["error"] == "refusal"
    - Assert state["last_sse_event"]["payload"]["recoverable"] is False

    test_comparison_sse_taxonomy (sequence assertion added — Review Fix 9):
    - Import app from api.app; use FastAPI TestClient
    - Patch _comparison_chain with a canned ComparisonDraft response (all present vendors, no real model call)
    - POST to /compare/vendors with minimal ExtractionResult[] + RFQ body
    - Parse all SSE events from response
    - Assert every event["type"] in EVENT_TYPES
    - Assert "done" event is the last event
    - Assert exactly ONE event with type == "result" appears in the sequence (Review Fix 9 — no double result)

    ---- NEW STUBS from review fixes ----

    test_clamp_applied_to_result (Review Fix 1 — BLOCKER, the e2e clamp test):
    - Build partial_extraction("cheap") with missing pricing (commercial dimension ceiling = not_comparable)
    - Patch agents.comparison._comparison_chain to return a ComparisonDraft where the commercial dimension verdict for "cheap" is proposed as "comparable" (over-optimistic raw draft)
    - Call run_comparison(extractions=[partial_extraction("cheap")], rfq=MagicMock(...))
    - Assert state["result_sse_event"] is not None (a result event was emitted)
    - Parse the emitted result event payload as ComparisonResult
    - Find the commercial DimensionComparison; find the DimensionVerdict for vendor "cheap"
    - Assert verdict == ComparabilityVerdict.not_comparable (code clamped the model's comparable)
    - Assert model_proposed == ComparabilityVerdict.comparable (pre-clamp verdict preserved for trace)
    - Assert len(result.clamp_report.entries) >= 1
    - Assert any(e.vendor_name == "cheap" and e.dimension == "commercial" for e in result.clamp_report.entries)
    This test verifies the clamp touched the ACTUAL EMITTED result event — not just the primitive function.

    test_dimension_enum_fail_closed (Review Fix 1):
    - Import _apply_verdict_clamp, ComparisonDimension from agents.comparison
    - Build a ComparisonDraft with dimension="Commercial" (mis-cased — not a valid ComparisonDimension)
    - Build ceilings = {ComparisonDimension.commercial: {"v1": "not_comparable"}}
    - Call _apply_verdict_clamp(draft, ceilings, ["v1"])
    - Assert result dimensions include commercial with verdict == "not_comparable" for "v1"
      (the mis-cased "Commercial" row is dropped; the fail-closed default for commercial applies)
    - Assert no exception is raised

    test_ceiling_empty_compliance (Review Fix 4):
    - Assert _ceiling_for_flags([], ComparisonDimension.compliance) == "partially"
    - Assert _ceiling_for_flags([], ComparisonDimension.compliance) != "comparable"

    test_ceiling_empty_risks (Review Fix 4):
    - Assert _ceiling_for_flags([], ComparisonDimension.risk) == "comparable"
      (per RESEARCH A2 — no risks claimed is not auto-blocking)
    - Assert _ceiling_for_flags([], ComparisonDimension.technical) == "not_comparable"
      (other dimensions with no contributors: fail-closed)

    test_cross_vendor_conflict_detection (Review Fix 10):
    - Build two extractions with DIFFERENT present timeline values:
        ext1 = present_extraction("v1"); ext1.timeline.value = "8 weeks"
        ext2 = present_extraction("v2"); ext2.timeline.value = "12 weeks"
      (Both statuses are FlagStatus.present — same-vendor "conflicting" is NOT what this tests)
    - Call _detect_attention_triggers([ext1, ext2], ceilings={...})
    - Assert at least one trigger in result with trigger_type == "cross_vendor_conflict"
      and dimension_or_field contains "timeline"

    test_rfq_line_item_alignment (Review Fix 11):
    - Build an ExtractionResult whose line_items have line_item_ids that do NOT match the RFQ
    - Mock rfq.line_items to have a line_item_id the vendor is missing
    - Call _check_rfq_alignment([mismatched_extraction], rfq)
    - Assert the vendor is in the returned mismatch list
    - Assert run_comparison with this extraction marks scope dimension as not_comparable for that vendor

    test_clarification_failure_surfaces_attention_point (Review Fix 8):
    - Patch _clarification_chain to raise an exception
    - Call run_comparison with a partial_extraction
    - Assert the emitted result has an AttentionPoint with trigger_type == "clarification_generation_failed"
    - Assert the result is still emitted (not aborted by clarification failure)

    Leave test_comparison_traces_committed as NotImplementedError — it goes GREEN in Wave 4.
  </action>

  <behavior>
    - POST /compare/vendors with 6 extractions returns 422 (vendor count guard — FastAPI pydantic validator raises ValueError → 422, not 400)
    - POST /compare/vendors with valid payload returns 200 streaming SSE
    - test_clamp_applied_to_result: emitted result event shows not_comparable for vendor with missing pricing; model_proposed preserved as comparable
    - test_dimension_enum_fail_closed: mis-cased "Commercial" dimension → fail-closed to not_comparable, no exception
    - test_ceiling_empty_compliance: empty flags for compliance → "partially"
    - test_clarification_failure_surfaces_attention_point: clarification call failure → AttentionPoint in result
    - test_comparison_sse_taxonomy: exactly one result event in SSE stream
  </behavior>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_comparison_agent.py::test_clamp_only_downgrades tests/test_comparison_agent.py::test_no_aggregation_over_missing tests/test_comparison_agent.py::test_attention_points_are_triggered tests/test_comparison_agent.py::test_clarification_seeded_by_code tests/test_comparison_agent.py::test_offer_table_code_built tests/test_comparison_agent.py::test_vendor_order_preserved tests/test_comparison_agent.py::test_no_numeric_score tests/test_comparison_agent.py::test_truncation_error_event tests/test_comparison_agent.py::test_refusal_error_event tests/test_comparison_agent.py::test_comparison_sse_taxonomy tests/test_comparison_agent.py::test_clamp_applied_to_result tests/test_comparison_agent.py::test_dimension_enum_fail_closed tests/test_comparison_agent.py::test_ceiling_empty_compliance tests/test_comparison_agent.py::test_ceiling_empty_risks tests/test_comparison_agent.py::test_cross_vendor_conflict_detection tests/test_comparison_agent.py::test_rfq_line_item_alignment tests/test_comparison_agent.py::test_clarification_failure_surfaces_attention_point -v 2>&1 | tail -20</automated>
  </verify>

  <done>POST /compare/vendors exists in app.py with 422 vendor count guard and _MAX_VENDORS documented; 17 listed tests pass (including all review-fix tests); test_comparison_traces_committed remains NotImplementedError; full suite green (uv run pytest -q).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /compare/vendors → ExtractionResult[] | Untrusted payload — pydantic validates; vendor count guard blocks oversized arrays (422) |
| compare node → model (ComparisonDraft) | Model proposes ComparisonDraft (verdicts + phrasing only); code constructs ComparisonResult — model cannot self-author clamp_report, offer table, readiness, or attention triggers |
| _apply_verdict_clamp → ComparisonDimension StrEnum | Unknown dimension names fail closed to not_comparable (Review Fix 1) — no KeyError bypass |
| clarify node → model (ClarificationSet) | Model phrases questions for code-supplied flagged fields only; count + identity validated; extras dropped (Review Fix 8) |
| SSE boundary | Exactly one result event crosses the boundary — after clamp + clarification (Review Fix 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Model-proposed comparable verdict over missing field elevated without clamp | mitigate | _apply_verdict_clamp is fail-closed; test_clamp_applied_to_result asserts emitted result downgraded (Review Fix 1) |
| T-04-03-02 | Tampering | Model emits ComparisonResult (not ComparisonDraft) and bypasses code construction | mitigate | with_structured_output(ComparisonDraft) — schema makes it structurally impossible to emit ComparisonResult (Review Fix 1+2); test_schema_shape confirms split |
| T-04-03-03 | Tampering | Free-string dimension key join bypasses clamp for mis-cased dimension | mitigate | ComparisonDimension(StrEnum) coercion in _apply_verdict_clamp; ValueError → not_comparable (fail closed); test_dimension_enum_fail_closed asserts (Review Fix 1) |
| T-04-03-04 | Tampering | Model invents attention points not in code trigger list | mitigate | _build_attention_shells builds shells; model fills summary only; model-invented trigger_types dropped (Review Fix 7); test_attention_points_are_triggered asserts fabricated point dropped |
| T-04-03-05 | Tampering | Model invents clarification questions for un-flagged fields | mitigate | count + (vendor, field_path, flag_status) identity validated; extras dropped; failure → AttentionPoint (Review Fix 8); test_clarification_seeded_by_code asserts extras dropped |
| T-04-03-06 | Tampering | Double result SSE event; pre-clamp result leaks | mitigate | Single result event emitted by clarify node only; compare node stores, does not emit (Review Fix 9); test_comparison_sse_taxonomy asserts exactly one result event |
| T-04-03-07 | Tampering | Offer table contains model-authored (potentially normalized) values | mitigate | _build_offer_table constructs from ExtractionResult.line_items verbatim; model cannot populate this table (Review Fix 6); test_offer_table_code_built asserts code-built, no normalized fields |
| T-04-03-08 | Denial of Service | Oversized ExtractionResult[] payload (>5 vendors) | mitigate | ComparisonRequest._check_vendor_count model_validator rejects >5 vendors with 422 |
| T-04-03-09 | Tampering | Raw vendor text passed to comparison (grounding boundary breach) | mitigate | _run_align_impl asserts isinstance(e, ExtractionResult) for each input; ExtractionResult has no raw_text |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all deps verified in Phases 1–3 |
</threat_model>

<verification>
After Wave 3 plans complete:
cd /Users/aayush/projects/aerchain/services/ai && uv run pytest -q 2>&1 | tail -5
Expected: 19 tests green (17 from test_comparison_agent.py + existing tests); 1 remaining stub (test_comparison_traces_committed) FAILED NotImplementedError; no import errors.

SSE endpoint smoke test (requires running server):
curl -N -X POST http://localhost:8000/compare/vendors -H "Content-Type: application/json" -d '{"extractions":[],"rfq":{...}}' → returns 422 (pydantic validation) or streams SSE events.
</verification>

<success_criteria>
- services/ai/agents/comparison.py exists with comparison_graph, run_comparison, generate_comparison_with_trace exports
- Model target is ComparisonDraft (not ComparisonResult); all code-built surfaces are constructed in comparison.py
- _apply_verdict_clamp is fail-closed: unknown dimensions default to not_comparable; full 6×N matrix validated
- _ceiling_for_flags has explicit per-dimension empty-case handling (compliance=partially, risk=comparable, others=not_comparable)
- _build_offer_table builds from ExtractionResult verbatim — model cannot author offer table
- Exactly one result SSE event emitted (by clarify node)
- POST /compare/vendors route exists in app.py with 422 vendor count guard; _MAX_VENDORS documented
- 17 test stubs GREEN (all review-fix tests included)
- uv run pytest -q shows no regressions in pre-existing tests
</success_criteria>

<output>
Create .planning/phases/04-comparison-agent/04-03-SUMMARY.md when done.
</output>
