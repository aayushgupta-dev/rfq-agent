---
phase: 04-comparison-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai/tests/test_comparison_agent.py
  - services/ai/tests/conftest_comparison.py
autonomous: true
requirements:
  - COMPARE-01
  - COMPARE-02
  - COMPARE-03
  - COMPARE-04
  - COMPARE-05

must_haves:
  truths:
    - "test_comparison_agent.py imports resolve and all stubs collect (not skip) when the module is imported"
    - "Every COMPARE-01..05 requirement maps to >=1 named test function"
    - "Clamp end-to-end test stub exists: test_clamp_applied_to_result mocks model ComparisonDraft, asserts EMITTED result verdict is not_comparable, model_proposed is comparable, ClampEntry exists (Consensus Fix 1)"
    - "Clamp primitive test, no-aggregation test, empty-ceiling tests, and order-preservation test exist as stubs ready to go GREEN in Wave 3"
    - "conftest_comparison.py provides missing_extraction(), present_extraction(), and partial_extraction() builders"
    - "D-01..D-11 decisions each traceable to a named test stub via module docstring mapping"
  artifacts:
    - path: "services/ai/tests/test_comparison_agent.py"
      provides: "RED stub test file — 20 named stubs, all collected (stub count updated per review Fix 13)"
      min_lines: 230
    - path: "services/ai/tests/conftest_comparison.py"
      provides: "ExtractionResult fixture builders for comparison tests"
      min_lines: 40
  key_links:
    - from: "services/ai/tests/test_comparison_agent.py"
      to: "services/ai/schemas/domain.py"
      via: "from schemas.domain import ComparisonResult, ComparisonDraft"
      pattern: "from schemas.domain import"
    - from: "services/ai/tests/conftest_comparison.py"
      to: "services/ai/tests/conftest_extraction.py"
      via: "reuses missing_field/present_field builders"
      pattern: "from conftest_extraction import"
---

<objective>
Wave 1: Create the RED test stubs and conftest fixture builders for the comparison agent.

Purpose: Establish the Nyquist gate before any implementation exists. Every COMPARE-01..05 requirement and every consensus review fix gets a named test stub that imports resolve and collects (pytest can discover them) but fails with NotImplementedError. The key addition vs. the pre-review plan is test_clamp_applied_to_result — the end-to-end clamp test (Consensus Fix 1) that proves the clamp applies to the actual emitted SSE result, not just the primitive clamp_verdict function.

Output: test_comparison_agent.py (20 stubs, all RED) + conftest_comparison.py (fixture builders)
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/04-comparison-agent/04-PATTERNS.md

<interfaces>
<!-- Key types and patterns the executor needs. Read these files first. -->

From services/ai/tests/test_extraction_agent.py (analog — entire structure, lines 1–80):
- Module docstring maps test functions to requirement IDs (one-to-one)
- Imports: json, pathlib, MagicMock, patch, pytest, from schemas.domain, from schemas.envelope, from schemas.events

From services/ai/tests/conftest_extraction.py (analog — entire file, lines 1–67):
- missing_field() → Field(status=FlagStatus.missing)
- present_field(value, snippet, source_id) → Field with status=present + Evidence
- Builder functions (NOT pytest fixtures) so they work in test bodies directly

From services/ai/schemas/domain.py lines 135–174:
- ExtractionResult fields: vendor_name(str), scope_summary(Field[str]), line_items(list[LineItemExtraction]),
  pricing_structure(Field[str]), total_price(Field[str]), commercial_terms(Field[str]),
  timeline(Field[str]), compliance_points(list[Field[str]]), assumptions(list[Field[str]]),
  exclusions(list[Field[str]]), risks(list[Field[str]])
- LineItemExtraction fields: line_item_id(str), line_item_name(str), pricing(Field[str]), scope_coverage(Field[str])
- ComparisonResult stub (2 fields) — Wave 2 will replace this entirely

From services/ai/schemas/envelope.py lines 27–37:
- FlagStatus: present, missing, unclear, conflicting, unsupported

From services/ai/schemas/events.py line 20:
- EVENT_TYPES: ("status", "partial", "result", "error", "done")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conftest_comparison.py — ExtractionResult builders</name>
  <files>services/ai/tests/conftest_comparison.py</files>

  <read_first>
    - services/ai/tests/conftest_extraction.py (entire file, lines 1–67) — replicate the builder-function pattern exactly; do NOT use pytest fixtures
    - services/ai/schemas/domain.py (lines 116–174) — ExtractionResult and LineItemExtraction field shapes to build
    - services/ai/schemas/envelope.py (lines 27–37) — FlagStatus enum values
  </read_first>

  <action>
    Create services/ai/tests/conftest_comparison.py following the conftest_extraction.py builder-function pattern.

    The file must provide these plain functions (not pytest fixtures):

    1. missing_line_item(line_item_id: str, line_item_name: str) -> LineItemExtraction
       Both pricing and scope_coverage are Field(status=FlagStatus.missing).

    2. present_line_item(line_item_id: str, line_item_name: str, pricing_snippet: str, scope_snippet: str, source_id: str = "src") -> LineItemExtraction
       Both pricing and scope_coverage are present Field[str] with Evidence.

    3. missing_extraction(vendor_name: str) -> ExtractionResult
       All Field[T] attributes set to missing; line_items defaults to []; list[Field[str]] lists default to [].
       Use ExtractionResult(vendor_name=vendor_name, scope_summary=missing_field(), ...) directly.
       Import missing_field from conftest_extraction (reuse, do not duplicate).

    4. present_extraction(vendor_name: str) -> ExtractionResult
       All required Field[T] attributes set to present with a sentinel snippet and source_id="src".
       line_items contains one present_line_item for "strategy-creative" / "Strategy & Creative Development".
       Import present_field from conftest_extraction.

    5. partial_extraction(vendor_name: str) -> ExtractionResult
       scope_summary and timeline are present; pricing_structure, total_price, commercial_terms are missing.
       line_items contains one missing_line_item (pricing missing → triggers commercial ceiling to not_comparable).
       This fixture is the primary subject of clamp tests (mirrors cheap-but-incomplete persona).

    Module docstring must state the ponytail rationale: builder functions not pytest fixtures, same reason as conftest_extraction.py.
    Add `# noqa: UP046` if any Generic import is needed; it is not expected for this file.
  </action>

  <behavior>
    - missing_extraction("v1") produces ExtractionResult where scope_summary.status == FlagStatus.missing
    - present_extraction("v2") produces ExtractionResult where scope_summary.status == FlagStatus.present
    - partial_extraction("v3") produces ExtractionResult where pricing_structure.status == FlagStatus.missing AND timeline.status == FlagStatus.present
    - All three return valid ExtractionResult instances (pydantic validation passes)
  </behavior>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "from conftest_comparison import missing_extraction, present_extraction, partial_extraction; me=missing_extraction('v1'); pe=present_extraction('v2'); pa=partial_extraction('v3'); print('OK', me.vendor_name, pe.scope_summary.status, pa.pricing_structure.status)"</automated>
  </verify>

  <done>Import resolves; missing_extraction, present_extraction, partial_extraction return valid ExtractionResult instances without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_comparison_agent.py — 20 RED stubs (review-expanded)</name>
  <files>services/ai/tests/test_comparison_agent.py</files>

  <read_first>
    - services/ai/tests/test_extraction_agent.py (lines 1–80) — module docstring pattern, import block, test naming convention
    - services/ai/tests/test_extraction_agent.py (lines 200–420) — truncation/refusal/SSE/traces test shape to mirror
    - services/ai/schemas/domain.py (lines 163–174) — ComparisonResult stub fields to inspect in test_schema_shape
    - services/ai/schemas/events.py (line 20) — EVENT_TYPES tuple
    - .planning/phases/04-comparison-agent/04-RESEARCH.md (Validation Architecture table) — the original stubs to create
    - services/ai/tests/conftest_comparison.py (Task 1 output) — builders to import
    - .planning/phases/04-comparison-agent/04-REVIEWS.md (Consensus Summary Agreed Concerns) — the new stubs required by review fixes
  </read_first>

  <action>
    Create services/ai/tests/test_comparison_agent.py with 20 named stub functions. Every stub raises NotImplementedError with a message naming the requirement it will cover and the review fix that added it (where applicable).

    Module docstring maps each test to its requirement ID and, for new stubs, the review fix number (mirror test_extraction_agent.py lines 1–25 exactly). Example row:
      test_clamp_applied_to_result  → COMPARE-02 — Review Fix 1 (end-to-end clamp on emitted result)

    Imports needed:
    - json, pathlib, MagicMock, patch, pytest
    - from conftest_comparison import missing_extraction, present_extraction, partial_extraction
    - from schemas.domain import ComparisonResult  # Wave 2 will add ComparisonDraft — stub import is fine for now
    - from schemas.envelope import FlagStatus
    - from schemas.events import EVENT_TYPES
    - from openai import LengthFinishReasonError

    THE 20 STUBS (function names must match exactly — Wave 3 makes them GREEN):

    --- ORIGINAL 13 (carried forward) ---

    1. test_schema_shape — COMPARE-01: ComparisonResult has required fields; no dict[str, Model] shapes; no numeric score/rank field; vendor_names is list[str]; ComparisonDraft is a separate schema
    2. test_no_dict_shapes — COMPARE-01 / PLAT-02: ComparisonResult sub-models use list[BaseModel], not dict[str, BaseModel]
    3. test_clamp_only_downgrades — COMPARE-02: inject model verdict "comparable" over missing field; assert clamped to "not_comparable" (primitive clamp_verdict + _ceiling_for_flags)
    4. test_no_aggregation_over_missing — COMPARE-02: _ceiling_for_flags never produces comparable when any flag is missing
    5. test_attention_points_are_triggered — COMPARE-03: code-triggered only; model-invented point is DROPPED (Review Fix 7 — tightened from "trigger detected" to "fabricated point dropped")
    6. test_clarification_seeded_by_code — COMPARE-03: clarification count + identity matches _collect_flagged_fields; model extras are rejected (Review Fix 8)
    7. test_offer_table_code_built — COMPARE-04: line_item_offers built from ExtractionResult verbatim values + evidence paths; no model-authored values (renamed from test_offer_table_verbatim; Review Fix 6)
    8. test_vendor_order_preserved — COMPARE-05: vendor_readiness list preserves input order after JSON round-trip
    9. test_no_numeric_score — COMPARE-05: ComparisonResult has no field named score, rank, or weight
    10. test_comparison_sse_taxonomy — SSE: all event types from /compare/vendors are in EVENT_TYPES; exactly one result event emitted (Review Fix 9 — adds sequence assertion)
    11. test_truncation_error_event — EXTRACT-05 analog: truncation → safe error event with recoverable=True
    12. test_refusal_error_event — EXTRACT-05 analog: refusal → safe error event with recoverable=False
    13. test_comparison_traces_committed — PROMPT-03 / D-11: >=1 comparison_*.json under docs/traces/ with required keys including clamp_step with >=1 entry; trace uses deterministic fixture draft (Review Fix 5)

    --- NEW 7 STUBS (from consensus review fixes) ---

    14. test_clamp_applied_to_result — COMPARE-02 / Review Fix 1 (BLOCKER):
        Mock the model to return a ComparisonDraft proposing "comparable" on the commercial dimension
        for a vendor with missing pricing. Assert: (a) the EMITTED result event's commercial verdict
        for that vendor == "not_comparable", (b) model_proposed == "comparable", (c) at least one
        ClampEntry in clamp_report matching that (vendor, dimension) pair.
        This is the end-to-end clamp test — proves the clamp touches the actual SSE result.
        Stub: raise NotImplementedError("stub: COMPARE-02 / Fix-1 — e2e clamp on emitted result")

    15. test_dimension_enum_fail_closed — COMPARE-02 / Review Fix 1:
        Make dimension a ComparisonDimension(StrEnum). Assert: _apply_verdict_clamp with a
        DimensionVerdict whose dimension value is "Commercial" (wrong case) or an unknown string
        defaults to not_comparable (fail closed), not a KeyError or bypass.
        Stub: raise NotImplementedError("stub: COMPARE-02 / Fix-1 — StrEnum dimension fail-closed")

    16. test_ceiling_empty_compliance — COMPARE-02 / Review Fix 4:
        Assert _ceiling_for_flags([]) for the compliance dimension returns at least "partially"
        (empty compliance list cannot be "comparable"). Per RESEARCH A2 + Opus review.
        Stub: raise NotImplementedError("stub: COMPARE-02 / Fix-4 — empty compliance ceiling")

    17. test_ceiling_empty_risks — COMPARE-02 / Review Fix 4:
        Assert _ceiling_for_flags([]) for the risk dimension behavior is defined (not fall-through
        to comparable). Per RESEARCH A2: empty risks = no hard ceiling but must be explicitly handled.
        Stub: raise NotImplementedError("stub: COMPARE-02 / Fix-4 — empty risk ceiling defined")

    18. test_cross_vendor_conflict_detection — COMPARE-03 / Review Fix 10:
        Build two extractions with DIFFERENT timeline values (both present, but different).
        Assert _detect_cross_vendor_conflicts returns a trigger for timeline.
        This tests cross-VENDOR comparison of differing values, NOT per-field conflicting status.
        Stub: raise NotImplementedError("stub: COMPARE-03 / Fix-10 — cross-vendor value conflict")

    19. test_rfq_line_item_alignment — COMPARE-04 / Review Fix 11:
        Build an ExtractionResult whose line_items do NOT map to the RFQ's 8 canonical line_item_ids.
        Assert the agent flags scope as not_comparable with a reason naming the mismatch.
        Stub: raise NotImplementedError("stub: COMPARE-04 / Fix-11 — vendor line item RFQ alignment")

    20. test_clarification_failure_surfaces_attention_point — COMPARE-03 / Review Fix 8:
        When the clarification chain call fails (mock raises), assert an AttentionPoint with
        trigger_type=="clarification_generation_failed" appears in the result (not a silent empty list).
        Stub: raise NotImplementedError("stub: COMPARE-03 / Fix-8 — clarification failure → AttentionPoint")

    Each stub body: raise NotImplementedError("stub: {REQ_ID} — {one-line description}")

    Add WAVE_0_COMPLETE = True constant at the top of the file (after imports) so the executor can grep-gate it.

    Note on RED vs full-suite: stubs raise NotImplementedError. The GREEN run for the full suite uses
    `uv run pytest tests/test_comparison_agent.py` targeted, not the full suite, until Wave 3/4 make
    them pass. Do NOT mark stubs xfail — they are genuinely RED by design.
  </action>

  <behavior>
    - pytest discovers all 20 functions
    - All 20 fail with NotImplementedError (not ImportError, not AttributeError)
    - `uv run pytest tests/test_comparison_agent.py --collect-only` lists all 20 test IDs
    - WAVE_0_COMPLETE = True is present in the file
  </behavior>

  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_comparison_agent.py --collect-only -q 2>&1 | grep "test_" | wc -l</automated>
  </verify>

  <done>pytest --collect-only shows >= 20 test IDs in test_comparison_agent.py; all raise NotImplementedError when run (not import errors); WAVE_0_COMPLETE = True is present in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → schemas.domain | Test imports ComparisonResult stub + ComparisonDraft (Wave 2 adds it); schema change in Wave 2 must not break test imports |
| conftest → conftest_extraction | conftest_comparison imports missing_field/present_field; if conftest_extraction changes, both break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | No new npm/pip installs in this plan | accept | No package installs; all deps from Phases 1–3 |
| T-04-01-01 | Tampering | conftest builders could produce invalid ExtractionResult if field shapes change | mitigate | Verify builders against live pydantic validation in Task 1 automated check |
| T-04-01-02 | Tampering | End-to-end clamp test (Fix 1) could be written in a way that mocks too shallowly and passes even when the real clamp is broken | mitigate | test_clamp_applied_to_result must assert on the EMITTED result event (state["result_sse_event"]) not on a return value; Wave 3 executor is directed to the emitted event |
</threat_model>

<verification>
After both tasks complete:
cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_comparison_agent.py -q 2>&1 | grep -E "FAILED|ERROR|passed|error" | head -5
Expected: 20 FAILED (NotImplementedError), 0 errors, 0 import errors.
</verification>

<success_criteria>
- conftest_comparison.py exists with missing_extraction, present_extraction, partial_extraction builders
- test_comparison_agent.py exists with 20 named stubs, all discovered by pytest
- All 20 stubs fail with NotImplementedError (imports resolve cleanly)
- test_clamp_applied_to_result stub exists and names its assertion target (emitted result event)
- Full suite still green: uv run pytest -q (existing tests must not regress)
</success_criteria>

<output>
Create .planning/phases/04-comparison-agent/04-01-SUMMARY.md when done.
</output>
