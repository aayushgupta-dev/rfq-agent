---
phase: 03-extraction-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - services/ai/schemas/domain.py
  - packages/shared-types/index.d.ts
autonomous: true
requirements:
  - EXTRACT-01

must_haves:
  truths:
    - "ExtractionResult covers all 8 RFQ categories: scope, pricing (per-line-item + doc-level bundle), commercial terms, timeline, compliance, assumptions, exclusions, risks (RFQ-aware hybrid, per-line-item Field[T] — D-01; bundled pricing kept doc-level + affected per-item marked unclear, never force-split — D-02)"
    - "vendor_name is plain str (D-05 fix) — grounding gate never attempts to ground it"
    - "All multi-claim categories use list[Field[T]] or list[BaseModel] containing Field[T] — per-claim grounding where natural (D-03); no dict[str, Field] shapes (D-04)"
    - "Codegen regenerates packages/shared-types from the updated schema and drift-check test passes"
    - "Walker coverage test (test_walker_covers_all_fields) is GREEN using the fabricated_field() approach from Plan 01"
  artifacts:
    - path: "services/ai/schemas/domain.py"
      provides: "Full ExtractionResult with LineItemExtraction sub-model"
      contains: "class ExtractionResult, class LineItemExtraction, vendor_name: str"
    - path: "packages/shared-types/index.d.ts"
      provides: "TS contract regenerated from updated pydantic schema"
  key_links:
    - from: "services/ai/schemas/domain.py ExtractionResult"
      to: "services/ai/schemas/envelope.py Field[T]"
      via: "Field[T] attributes on LineItemExtraction + ExtractionResult"
      pattern: "Field\\[.*\\]"
    - from: "services/ai/schemas/domain.py ExtractionResult"
      to: "services/ai/grounding/gate.py _walk_and_ground"
      via: "ground_model(extraction_result, sources) — walker must reach every Field[T]"
      pattern: "ground_model"
---

<objective>
Wave 1 (Schema): flesh out ExtractionResult from its 3-field stub into the full production schema with LineItemExtraction per D-01..D-05. Fix the D-05 vendor_name bug. Run codegen and confirm the drift-check and walker-coverage tests go GREEN.

Purpose: ExtractionResult is the contract everything downstream depends on — the agent (Plan 03), the comparison agent (Phase 4), and the UI (Phase 5). Getting it right before any code touches it avoids rework across those phases.

Output: Updated domain.py + regenerated packages/shared-types/index.d.ts. test_schema_shape and test_walker_covers_all_fields go GREEN.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/03-extraction-agent/03-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/03-extraction-agent/03-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/03-extraction-agent/03-PATTERNS.md

<interfaces>
<!-- Extracted from codebase for executor reference. -->

From services/ai/schemas/envelope.py (Field[T], FlagStatus, Evidence — the building blocks):
```python
class FlagStatus(str, Enum):
    present = "present"
    missing = "missing"
    unclear = "unclear"
    conflicting = "conflicting"
    unsupported = "unsupported"

class Evidence(BaseModel):
    snippet: str
    source_id: str
    char_start: int
    char_end: int

class Field(BaseModel, Generic[T]):  # noqa: UP046
    status: FlagStatus
    value: T | None = None
    evidence: list[Evidence] = pydantic_Field(default_factory=list)
    values: list[ConflictingValue[T]] | None = None
```

From services/ai/schemas/domain.py (RFQ line items — structure extraction maps to):
```python
class LineItem(BaseModel):
    model_config = ConfigDict(extra="forbid")
    id: str
    name: str
    description: str
    deliverables: list[str]
    timeline_weeks: int | None = None
    budget_range_usd: list[int] | None = None

class RFQ(BaseModel):
    line_items: list[LineItem]  # 8 items in the sample
```

From services/ai/schemas/domain.py (current ExtractionResult stub — lines 123-136):
```python
class ExtractionResult(BaseModel):
    model_config = ConfigDict(extra="forbid")
    vendor_name: Field[str] = Field[str](status="missing")  # WRONG — D-05 fix needed
    scope_summary: Field[str] = Field[str](status="missing")
    total_price: Field[Decimal] = Field[Decimal](status="missing")
```

From services/ai/grounding/gate.py (shapes walker handles — lines 320-366):
- Direct Field[T] attribute -> ground_field() called
- Nested BaseModel -> recursive _walk_and_ground()
- list[Field[T]] -> each item grounded
- list[BaseModel] -> each item recursively walked
- dict[str, Field[T]] -> NOT handled (D-04 forbids this shape)

pydantic2ts codegen:
```bash
cd services/ai && uv run python scripts/codegen.py
```
Drift check:
```bash
cd services/ai && uv run pytest tests/test_codegen_drift.py -x
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flesh out ExtractionResult + LineItemExtraction in domain.py (D-01..D-05)</name>
  <files>services/ai/schemas/domain.py</files>
  <read_first>
    - services/ai/schemas/domain.py (full file — current stub at lines 123-136; imports at lines 1-31; LineItem pattern at lines 48-78)
    - services/ai/schemas/envelope.py (Field[T], Evidence, ConflictingValue, FlagStatus — full building blocks)
    - .planning/phases/03-extraction-agent/03-CONTEXT.md (D-01 hybrid; D-02 bundled pricing; D-03 per-claim list; D-04 no dict; D-05 vendor_name)
    - .planning/phases/03-extraction-agent/03-PATTERNS.md (domain.py LineItem pattern; Field[T] usage; # noqa: UP046)
  </read_first>
  <behavior>
    - ExtractionResult.vendor_name is str (not Field[str]) — isinstance(ExtractionResult.model_fields["vendor_name"].annotation, type) and that type is str
    - LineItemExtraction has pricing: Field[Decimal] and scope_coverage: Field[str] — both Field[T]
    - ExtractionResult.line_items is list[LineItemExtraction] (per D-01 RFQ-aware hybrid)
    - ExtractionResult.pricing_structure is Field[str] (doc-level bundle field per D-02)
    - ExtractionResult.assumptions is list[Field[str]] (per-claim per D-03)
    - ExtractionResult.risks is list[Field[str]] (per-claim per D-03)
    - ExtractionResult.exclusions is list[Field[str]] (per-claim per D-03)
    - ExtractionResult.compliance_points is list[Field[str]] (per-claim per D-03)
    - No dict[str, Field] shapes anywhere — only list[Field[T]] or list[BaseModel] (D-04)
    - model_config = ConfigDict(extra="forbid") on all models
    - # noqa: UP046 on any Generic[T] class
  </behavior>
  <action>
    Replace the ExtractionResult stub (lines 123-136) with:

    1. LineItemExtraction BaseModel (insert before ExtractionResult):
       - model_config = ConfigDict(extra="forbid")
       - line_item_id: str  (matches RFQ.line_items[*].id — provenance, not grounded)
       - line_item_name: str  (matches RFQ.line_items[*].name — provenance, not grounded)
       # ponytail: line_item_id and line_item_name are copied from RFQ context at extraction time,
       # not extracted from vendor text — they are scaffold/provenance, not grounded facts.
       - pricing: Field[Decimal]  (vendor's stated price for this item; missing if not bid)
       - scope_coverage: Field[str]  (what the vendor covers for this item; missing if not bid)

    2. ExtractionResult BaseModel (replace stub):
       - model_config = ConfigDict(extra="forbid")
       - vendor_name: str  (D-05: provenance metadata — NOT a grounded Field)
       - scope_summary: Field[str]  (doc-level narrative summary of what vendor is offering)
       - line_items: list[LineItemExtraction]  (D-01: one entry per RFQ line item)
       - pricing_structure: Field[str]  (D-02: verbatim bundle/grand total statement if vendor
         bundled pricing; unclear if not stated; missing if no pricing found)
       - total_price: Field[Decimal]  (D-02: stated grand total if separable; missing if bundled-only)
         # ponytail: Field[Decimal] for total_price — if with_structured_output(method="json_schema")
         # produces schema friction during Plan 03 (model fills it inconsistently or parse errors occur),
         # downgrade to Field[str] here; the gate is value-type-agnostic. Check by running:
         # python -c "from schemas.domain import ExtractionResult; print('ok')" after Plan 03 wires the chain.
       - commercial_terms: Field[str]  (payment terms, milestones, discounts, conditions)
       - timeline: Field[str]  (delivery timeline / project schedule narrative)
       - compliance_points: list[Field[str]]  (D-03: each compliance statement as its own Field)
       - assumptions: list[Field[str]]  (D-03: each vendor assumption as its own Field)
       - exclusions: list[Field[str]]  (D-03: each explicit exclusion as its own Field)
       - risks: list[Field[str]]  (D-03: each identified risk or weak-compliance flag as its own Field)

    Preserve ComparisonResult stub below ExtractionResult unchanged.
    Keep all existing ponytail comments. Update the module docstring to note P3 is complete.

    NOTE: Do NOT add default values to Field[T] fields — the extraction agent always constructs
    real instances, never uses defaults. list[Field[str]] fields default to an empty list via
    pydantic_Field(default_factory=list). Plain str fields (vendor_name, line_item_id,
    line_item_name) have no defaults — they are always provided.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_extraction_agent.py::test_schema_shape tests/test_extraction_agent.py::test_walker_covers_all_fields -x --tb=short</automated>
  </verify>
  <done>
    - ExtractionResult has vendor_name: str (not Field[str])
    - ExtractionResult has all 11 fields as listed (scope_summary, line_items, pricing_structure,
      total_price, commercial_terms, timeline, compliance_points, assumptions, exclusions, risks)
    - LineItemExtraction has pricing: Field[Decimal] and scope_coverage: Field[str]
    - No dict[str, Field] shapes in any model
    - test_schema_shape and test_walker_covers_all_fields both GREEN (walker test uses
      fabricated_field() approach from conftest_extraction — not all-missing)
    - No pydantic import/validation errors on module load:
      python -c "from schemas.domain import ExtractionResult" succeeds
    - W-R1 sanity note: if Field[Decimal] causes structured-output friction in Plan 03,
      change to Field[str] in this file and re-run codegen; note in Plan 03 done criteria
  </done>
</task>

<task type="auto">
  <name>Task 2: Run codegen + confirm drift-check passes</name>
  <files>packages/shared-types/index.d.ts</files>
  <read_first>
    - services/ai/scripts/codegen.py (codegen script — how to invoke; output path)
    - services/ai/tests/test_codegen_drift.py (what it checks; the hash/snapshot mechanism)
  </read_first>
  <action>
    Run the codegen script to regenerate packages/shared-types/index.d.ts from the updated
    ExtractionResult schema:

      cd services/ai && uv run python scripts/codegen.py

    Then run the drift-check test to confirm the regenerated types match what's on disk:

      cd services/ai && uv run pytest tests/test_codegen_drift.py -x

    If drift-check fails with a diff showing the old ExtractionResult stub types (FieldStr /
    FieldDecimal as top-level fields), that is expected — commit the newly generated index.d.ts.
    The drift-check should pass after the commit because the generated file matches the schema.

    If the codegen script itself fails (import error, pydantic2ts error), read the error carefully
    — likely a # noqa: UP046 missing on Generic[T] or an extra= config issue. Fix in domain.py
    and re-run.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_codegen_drift.py -x --tb=short</automated>
  </verify>
  <done>
    - packages/shared-types/index.d.ts reflects LineItemExtraction + updated ExtractionResult
    - test_codegen_drift.py passes GREEN
    - The TS types include LineItemExtraction, ExtractionResult with all fields
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema definition → codegen script | pydantic2ts reads schemas/__init__.py; no user input at this stage |
| ExtractionResult shape → grounding walker | Walker must traverse every Field[T] — D-04 prevents silently skipped dict shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | ExtractionResult.vendor_name | mitigate | Changed from Field[str] to plain str per D-05; grounding gate never attempts to ground provenance metadata |
| T-03-02-02 | Tampering | dict[str, Field] shapes in schema | mitigate | D-04 hard constraint: only list[BaseModel]/list[Field[T]] allowed; walker test (fabricated_field approach) confirms coverage |
| T-03-02-SC | Tampering | codegen script deps | accept | No new packages; codegen uses pydantic2ts already audited in Phase 1 |
</threat_model>

<verification>
uv run pytest tests/test_extraction_agent.py::test_schema_shape tests/test_extraction_agent.py::test_walker_covers_all_fields tests/test_codegen_drift.py -x
Expected: 3 GREEN. Schema shape and walker coverage confirmed. TS types regenerated.
Full suite: uv run pytest tests/ -x
</verification>

<success_criteria>
- ExtractionResult covers all 8 RFQ categories (scope, per-line-item pricing+scope, bundle pricing,
  commercial terms, timeline, compliance, assumptions, exclusions, risks)
- vendor_name is plain str — no Field[str] wrapper
- All multi-claim fields are list[Field[str]] — no dict shapes
- test_schema_shape GREEN, test_walker_covers_all_fields GREEN (fabricated_field approach),
  test_codegen_drift GREEN
- packages/shared-types/index.d.ts contains LineItemExtraction and updated ExtractionResult
- W-R1: Field[Decimal] for total_price documented with the ponytail contingency comment;
  executor checks during Plan 03 and downgrades to Field[str] if structured-output friction occurs
</success_criteria>

<output>
Create `.planning/phases/03-extraction-agent/03-02-SUMMARY.md` when done.
</output>
