---
phase: 03-extraction-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai/tests/test_extraction_agent.py
  - services/ai/tests/conftest_extraction.py
  - docs/traces/.gitkeep
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-05
  - PROMPT-03

must_haves:
  truths:
    - "test_extraction_agent.py exists with RED stubs that import correctly and fail with NotImplementedError or xfail"
    - "docs/traces/ directory is tracked in git (via .gitkeep)"
    - "Every test function in the stub maps 1:1 to a row in the VALIDATION.md Per-Task Verification Map"
  artifacts:
    - path: "services/ai/tests/test_extraction_agent.py"
      provides: "RED stubs for EXTRACT-01/02/03/05, walker coverage, SSE taxonomy, trace file check, refusal detection, missing line item surfacing, live guard"
      contains: "test_schema_shape, test_walker_covers_all_fields, test_evidence_required, test_truncation_raises_error_event, test_refusal_raises_error_event, test_missing_line_items_surface_as_missing, test_sse_event_taxonomy, test_traces_committed, test_truncation_live_guard"
    - path: "services/ai/tests/conftest_extraction.py"
      provides: "Shared fixtures: missing_field(), present_field(), minimal_extraction_result()"
    - path: "docs/traces/.gitkeep"
      provides: "Tracked empty directory for D-13 trace artifacts"
  key_links:
    - from: "services/ai/tests/test_extraction_agent.py"
      to: "services/ai/agents/extraction.py"
      via: "import agents.extraction (will fail RED until Plan 03 ships)"
      pattern: "from agents.extraction import"
    - from: "services/ai/tests/test_extraction_agent.py"
      to: "services/ai/schemas/domain.py"
      via: "import ExtractionResult (schema must exist for shape test)"
      pattern: "from schemas.domain import ExtractionResult"
---

<objective>
Wave 0: create the RED test stubs that define all Phase 3 automated verification gates before any implementation ships. Also track the docs/traces/ directory needed for D-13 trace artifacts.

Purpose: Nyquist compliance requires every task have a runnable automated verify. The stubs fail NOW (RED) and go GREEN as each implementation plan lands. The stubs also lock the exact test function names that VALIDATION.md's Per-Task Verification Map points to.

Output: `tests/test_extraction_agent.py` with 9 stub test functions (all xfail or NotImplementedError), `tests/conftest_extraction.py` with shared fixtures, `docs/traces/.gitkeep`.
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/03-extraction-agent/03-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/03-extraction-agent/03-VALIDATION.md

<interfaces>
<!-- Key patterns from 03-PATTERNS.md for test file authoring. -->

From services/ai/tests/test_grounding_gate.py (class grouping + inline model pattern):
```python
class TestFabricatedSpanDowngrade:
    def test_fabricated_span_is_downgraded(self) -> None: ...
```

From services/ai/tests/test_sse_demo.py (TestClient without lifespan):
```python
client = TestClient(app, raise_server_exceptions=True)
```

SSE parse helper (test_sse_demo.py lines 143-160):
```python
def _parse_sse_events(body: str) -> list[dict]:
    for line in body.splitlines():
        if line.startswith("data: "):
            ...
```

Trace file existence pattern (03-PATTERNS.md):
```python
import pathlib
def test_traces_committed():
    traces_dir = pathlib.Path(__file__).parents[3] / "docs" / "traces"
    json_traces = list(traces_dir.glob("*.json"))
    assert len(json_traces) >= 3
```

gate.py early-return (verified against codebase):
ground_field returns (field, []) immediately when field.status is missing or unsupported —
NO DowngradeEntry is produced for missing fields. Entry-count is NOT a walker-coverage proxy.

Walker coverage approach (B-R1 fix — model_fields enumeration):
Enumerate Field[T]-typed attributes recursively across ExtractionResult.model_fields
(including nested models' model_fields and list element models' model_fields) to build the
expected traversal set. Assert the set the walker visits (tracked via monkeypatching
_walk_and_ground or by comparing the field paths in the DowngradeReport against the
enumerated set) matches this expected set — independent of grounding outcome.

The cleanest implementation: build expected_paths by walking model_fields recursively,
then call ground_model on an instance where every Field is present with a non-locatable
fabricated snippet (e.g. "XYZNOTFOUND"), so each genuinely downgrades and produces a
DowngradeEntry. Assert that the set of field paths in report.entries == expected_paths.
This is outcome-independent of threshold behavior and tests the real walker code path.

SSE taxonomy test mock (B-R4 fix):
test_sse_event_taxonomy MUST patch agents.extraction._chain to return a canned
ExtractionResult so the route exercises status->result->done without a live API call.
Specify this in the stub action:
  with patch("agents.extraction._chain") as mock_chain:
      mock_chain.invoke.return_value = {"raw": mock_msg, "parsed": <canned ExtractionResult>}
      ... post to /extract/vendor via TestClient ...

Live guard stub (B-R2):
test_truncation_live_guard is marked @pytest.mark.live (skipped in CI unless -m live).
It calls the real chain with max_tokens=1 against the largest fixture to confirm
LengthFinishReasonError is raised by the INSTALLED langchain-openai version.
It is a stub in Wave 1 (xfail+live) and goes GREEN in Plan 03/04 execution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test stubs + conftest fixtures + docs/traces/</name>
  <files>services/ai/tests/test_extraction_agent.py, services/ai/tests/conftest_extraction.py, docs/traces/.gitkeep</files>
  <read_first>
    - services/ai/tests/test_grounding_gate.py (class structure + inline model pattern)
    - services/ai/tests/test_sse_demo.py (TestClient without lifespan + SSE parse helper at lines 143-160)
    - services/ai/tests/test_field_envelope.py (imports pattern, standalone function tests)
    - services/ai/grounding/gate.py (ground_field lines 236-317 — confirm early-return on missing/unsupported; _walk_and_ground lines 320-366 — confirm traversal shapes)
    - services/ai/schemas/envelope.py (Field[T], FlagStatus, Evidence — for fixture builders)
    - .planning/phases/03-extraction-agent/03-VALIDATION.md (Per-Task Verification Map — one stub per row)
    - .planning/phases/03-extraction-agent/03-CONTEXT.md (D-05, D-08, D-12..D-15 for test expectations)
    - .planning/phases/03-extraction-agent/03-RESEARCH.md (Pattern 2 — refusal via additional_kwargs.get("refusal"))
  </read_first>
  <action>
    Create services/ai/tests/conftest_extraction.py with three fixture/builder functions (plain functions, not pytest fixtures, so they work inside test bodies too):

      from schemas.envelope import Field, FlagStatus, Evidence
      from decimal import Decimal

      def missing_field(type_=str):
          # ponytail: builder for Field(status=missing) avoids brittle inline construction in tests
          return Field(status=FlagStatus.missing)

      def present_field(value, snippet, source_id="src"):
          return Field(status=FlagStatus.present, value=value,
                       evidence=[Evidence(snippet=snippet, source_id=source_id, char_start=0, char_end=1)])

      def fabricated_field(value="XYZNOTFOUND_VALUE", type_=str):
          # Returns a present field with a snippet guaranteed not to appear in any real source.
          # ground_model on this field ALWAYS produces a DowngradeEntry (used in walker coverage test).
          return Field(status=FlagStatus.present, value=value,
                       evidence=[Evidence(snippet="XYZNOTFOUND_FABRICATED_SNIPPET_123", source_id="src", char_start=0, char_end=1)])

    Create services/ai/tests/test_extraction_agent.py with exactly these 9 stub test functions
    (all marked @pytest.mark.xfail(reason="Plan NN not yet executed", strict=True)):

    1. test_schema_shape — imports ExtractionResult from schemas.domain; asserts vendor_name is
       plain str (not Field[str]); asserts schema has fields for scope_summary, line_items,
       pricing_structure, total_price, commercial_terms, timeline, compliance_points, assumptions,
       exclusions, risks. Use ExtractionResult.model_fields key checks. Mark xfail.

    2. test_walker_covers_all_fields — (B-R1 redesign) builds an ExtractionResult instance where
       every Field[T] attribute (including all LineItemExtraction fields and all list[Field[str]]
       elements) is set to fabricated_field() — a present field with a non-locatable snippet.
       Calls ground_model(result, {"src": "harmless source text that contains nothing"}).
       Builds expected_paths by recursively enumerating Field[T]-typed attributes from
       ExtractionResult.model_fields (including nested model fields and list element model fields).
       Asserts that the set of field_path values in report.entries equals expected_paths — proving
       the walker visited every Field[T] and each produced a DowngradeEntry (since the fabricated
       snippets are unlocalatable). This approach is independent of grounding outcome and tests
       the real _walk_and_ground code path. Mark xfail.
       NOTE: Do NOT use an all-missing ExtractionResult — ground_field early-returns on missing
       with zero entries, so entry-count would be 0 regardless of walker coverage.

    3. test_evidence_required — constructs an ExtractionResult where scope_summary is
       present_field("X", "verbatim quote from source"); calls ground_model(..., {"src": "some text
       verbatim quote from source more text"}); asserts scope_summary.status remains present.
       Mark xfail.

    4. test_truncation_raises_error_event — patches agents.extraction._chain so that
       _chain.invoke raises LengthFinishReasonError; drives the extraction graph synchronously;
       asserts the returned state dict has error=="truncated" and the last SSE chunk had
       type=="error" and payload["recoverable"]==True. Mark xfail.

    5. test_refusal_raises_error_event — patches agents.extraction._chain (include_raw=True path)
       so raw_output["raw"].additional_kwargs["refusal"] is non-None and raw_output["parsed"] is
       None; drives the extraction graph; asserts state has error=="refusal", that no ExtractionResult
       was parsed (state.get("result") is None), and the SSE error event has
       payload["recoverable"]==False. Mark xfail.

    6. test_missing_line_items_surface_as_missing — patches agents.extraction._chain so invoke
       returns a canned ExtractionResult with one line_items entry having pricing=missing_field()
       and scope_coverage=missing_field(); drives the extraction graph; asserts the SSE "result"
       event payload carries that line item's pricing and scope_coverage with status=="missing".
       Mark xfail.

    7. test_sse_event_taxonomy — (B-R4 fix) patches agents.extraction._chain so that
       _chain.invoke returns {"raw": MagicMock(additional_kwargs={}), "parsed": <minimal canned
       ExtractionResult>} where the canned result has vendor_name="test" and all Field[T]s set to
       missing_field() and line_items=[]. Posts to /extract/vendor via TestClient(app) with a
       minimal ExtractionRequest body (import VendorResponse and RFQ from schemas.domain).
       Asserts all emitted event types are in EVENT_TYPES. Mark xfail (route doesn't exist yet).
       Import agents.extraction and api.app inside the test function body to avoid collection errors.

    8. test_traces_committed — checks pathlib.Path(__file__).parents[3] / "docs" / "traces"
       contains >=3 .json files each with keys "input", "resolved_prompt", "raw_model_output",
       "grounding_step", "final_result". Also asserts that at least one trace file has
       grounding_step["downgrade_report"]["entries"] with length > 0 (D-15 machine-verifiable
       genuine downgrade). Mark xfail (traces not yet captured).

    9. test_truncation_live_guard — (B-R2 live guard) marked BOTH @pytest.mark.xfail(strict=True)
       AND @pytest.mark.live so it is skipped in CI (add "live" to pytest markers in pyproject.toml
       if not present). The stub body: imports _chain from agents.extraction; calls
       _chain.invoke({...}, config={"max_tokens": 1}) and asserts LengthFinishReasonError is raised.
       This validates that the INSTALLED langchain-openai raises LengthFinishReasonError on truncation
       (the MEDIUM-confidence RESEARCH.md assumption). Run once during Plan 03/04 real execution
       with -m live to validate against the installed library.
       # ponytail: live guard for RESEARCH.md Pattern 2 MEDIUM-confidence open-bug assumption
       # (#29700/#25510). Skipped in CI. Run with `uv run pytest -m live` to validate against
       # the installed langchain-openai before shipping EXTRACT-05.

    Import block at module level: pytest, pathlib, asyncio, json, unittest.mock (patch, MagicMock),
    conftest_extraction (missing_field, present_field, fabricated_field).
    Import openai.LengthFinishReasonError, schemas.domain.ExtractionResult,
    grounding.gate.ground_model, grounding.report.DowngradeReport, schemas.envelope.Field,
    schemas.envelope.Evidence, schemas.events.EVENT_TYPES at module level only if they exist
    (schemas.domain + schemas.envelope + grounding.* exist; agents.extraction does not yet —
    import it inside test function bodies for tests that need it).

    Create docs/traces/.gitkeep as an empty file.

    Run: uv run pytest tests/test_extraction_agent.py -x --tb=short from services/ai/ to confirm
    all stubs are collected (even if xfail). The live guard stub is skipped by default.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_extraction_agent.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tests/test_extraction_agent.py exists with 9 named test functions: test_schema_shape,
      test_walker_covers_all_fields, test_evidence_required, test_truncation_raises_error_event,
      test_refusal_raises_error_event, test_missing_line_items_surface_as_missing,
      test_sse_event_taxonomy, test_traces_committed, test_truncation_live_guard
    - tests/conftest_extraction.py exists with missing_field(), present_field(), fabricated_field() builders
    - All 9 tests are collected by pytest (no import errors at collection time)
    - 8 tests are xfail; test_truncation_live_guard is xfail+skipped (marked live)
    - test_walker_covers_all_fields uses fabricated_field() approach (NOT all-missing), asserts
      report.entries field_path set == expected_paths from model_fields enumeration
    - test_sse_event_taxonomy patches agents.extraction._chain before posting to /extract/vendor
    - test_refusal_raises_error_event stub asserts error event with recoverable=False AND
      no ExtractionResult parsed
    - test_missing_line_items_surface_as_missing stub asserts missing status propagates through
      SSE result event unchanged
    - test_traces_committed stub asserts both >=3 JSON files AND at least one has non-empty
      downgrade_report.entries (D-15 machine-verifiable)
    - docs/traces/.gitkeep exists
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → schemas | Test file imports schemas.domain — schema must not have side effects on import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W0-01 | Tampering | test_extraction_agent.py import block | accept | Wave 0 is test-only; no user input processed; no trust boundary crossed |
| T-03-W0-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 3 (RESEARCH.md Package Legitimacy Audit confirmed N/A) |
</threat_model>

<verification>
uv run pytest tests/test_extraction_agent.py -v --tb=short
Expected: 8 xfail results + 1 xfail+skipped (live guard), 0 errors, 0 unexpected failures.
Full suite baseline: uv run pytest tests/ -x (existing tests still green)
</verification>

<success_criteria>
- 9 stub test functions exist in test_extraction_agent.py, named exactly as listed in done criteria
- conftest_extraction.py provides missing_field(), present_field(), fabricated_field() builders
- test_walker_covers_all_fields uses fabricated_field() (NOT all-missing) + model_fields enumeration approach
- test_sse_event_taxonomy patches agents.extraction._chain before posting to route
- test_truncation_live_guard is skipped by default (marked live), xfail when run with -m live
- All 9 collected as xfail/skipped by pytest — no collection errors
- test_refusal_raises_error_event asserts recoverable=False AND no parsed ExtractionResult
- test_traces_committed asserts >=3 JSON files AND >=1 has non-empty downgrade_report.entries (D-15)
- docs/traces/ directory tracked via .gitkeep
- Existing test suite (test_grounding_gate, test_field_envelope, test_sse_demo, test_codegen_drift) still passes
</success_criteria>

<output>
Create `.planning/phases/03-extraction-agent/03-01-SUMMARY.md` when done.
</output>
