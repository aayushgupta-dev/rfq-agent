---
phase: 02-grounding-gate-messy-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai/grounding/__init__.py
  - services/ai/grounding/gate.py
  - services/ai/grounding/report.py
  - services/ai/tests/test_grounding_gate.py
  - services/ai/tests/test_sample_fixtures.py
autonomous: true
requirements:
  - EXTRACT-04
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Running the grounding gate test suite exits with ImportError for gate functions (RED — gate not implemented yet)"
    - "The two falsifiability tests (test_fabricated_span_is_downgraded, test_genuine_span_passes_grounding) are written and fail because the gate stub returns NotImplementedError"
    - "test_sample_fixtures.py is written with all DATA-01/02/03 fixture assertions and fails because data/ files do not exist yet"
    - "All module imports in both test files resolve without ImportError (stubs satisfy import graph)"
  artifacts:
    - path: "services/ai/grounding/__init__.py"
      provides: "Package marker + public API re-exports"
    - path: "services/ai/grounding/gate.py"
      provides: "Stubs for ground_field, ground_model raising NotImplementedError"
    - path: "services/ai/grounding/report.py"
      provides: "DowngradeEntry + DowngradeReport pydantic models"
    - path: "services/ai/tests/test_grounding_gate.py"
      provides: "All EXTRACT-04 gate unit tests including falsifiability tests A and B"
    - path: "services/ai/tests/test_sample_fixtures.py"
      provides: "All DATA-01/02/03 fixture existence + messiness assertion tests"
  key_links:
    - from: "tests/test_grounding_gate.py"
      to: "grounding.gate"
      via: "from grounding.gate import ground_field, ground_model"
      pattern: "from grounding"
    - from: "tests/test_sample_fixtures.py"
      to: "schemas.domain"
      via: "from schemas.domain import RFQ, VendorResponse"
      pattern: "from schemas.domain"
---

<objective>
Create the Wave 0 test harness and module stubs that make imports resolve and tests runnable
(in failing/RED state) before any implementation lands. This is the Nyquist Wave 0 requirement
from VALIDATION.md: tests must be written BEFORE implementation so the RED→GREEN progression
is verifiable.

Purpose: Enforce RED-before-GREEN discipline. The falsifiability tests (Test A: fabricated span
is downgraded; Test B: genuine span passes) are the phase-goal gate — they must exist before the
gate is built, not after. Likewise, fixture tests must exist before data/ files are committed,
proving the shape check is independent of the generator.

Output:
- grounding/ package stubs (gate.py + report.py + __init__.py)
- test_grounding_gate.py covering all 9 EXTRACT-04 test cases from VALIDATION.md
- test_sample_fixtures.py covering all DATA-01/02/03 test cases from VALIDATION.md
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/STATE.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md

<interfaces>
From services/ai/schemas/envelope.py (the gate's input/output contract):

class FlagStatus(StrEnum):
    present = "present"
    missing = "missing"
    unclear = "unclear"
    conflicting = "conflicting"
    unsupported = "unsupported"

class Evidence(BaseModel):
    model_config = ConfigDict(extra="forbid")
    snippet: str
    char_start: int
    char_end: int
    source_id: str

class ConflictingValue(BaseModel, Generic[T]):  # noqa: UP046
    model_config = ConfigDict(extra="forbid")
    value: T | None = None
    evidence: list[Evidence] = pydantic_Field(default_factory=list)

class Field(BaseModel, Generic[T]):  # noqa: UP046
    model_config = ConfigDict(extra="forbid")
    status: FlagStatus
    value: T | None = None
    evidence: list[Evidence] = pydantic_Field(default_factory=list)
    values: list[ConflictingValue[T]] | None = None

From services/ai/schemas/domain.py (current stubs — will be fleshed in plan 02-03):
class RFQ(BaseModel):   # stub — real fields land in plan 02-03
class VendorResponse(BaseModel):   # stub — real fields land in plan 02-03

From services/ai/scripts/codegen.py:
def repo_root() -> Path: ...  # exported for reuse in test_sample_fixtures.py

From services/ai/tests/test_field_envelope.py (structural analog for gate tests):
# class-per-concern grouping, Evidence/Field construction patterns, -> None annotations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module stubs — grounding package (gate.py + report.py + __init__.py)</name>
  <files>services/ai/grounding/__init__.py, services/ai/grounding/gate.py, services/ai/grounding/report.py</files>
  <read_first>
    - services/ai/schemas/envelope.py — Field[T], Evidence, ConflictingValue, FlagStatus; the gate's I/O contract
    - services/ai/schemas/events.py — ConfigDict(extra="forbid") + pydantic_Field pattern for report models
    - services/ai/agents/__init__.py — bare __init__.py pattern
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §grounding/__init__.py, §grounding/gate.py, §grounding/report.py
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Architecture Patterns (project structure diagram)
  </read_first>
  <action>
    Create services/ai/grounding/ as a Python package with three files.

    grounding/__init__.py: Re-export the public API so callers write
    "from grounding import ground_field, ground_model, DowngradeEntry, DowngradeReport".
    Use the re-export pattern from PATTERNS.md §grounding/__init__.py. __all__ must list
    all four names.

    grounding/report.py: Define DowngradeEntry and DowngradeReport as pydantic BaseModel
    subclasses (not dataclasses — consistent with the codebase). Follow the ConfigDict
    pattern from PATTERNS.md §grounding/report.py exactly:
    - DowngradeEntry fields: field_path: str, original_status: str, failed_evidence: Evidence,
      reason: str. Import Evidence from schemas.envelope.
    - DowngradeReport fields: entries: list[DowngradeEntry] with pydantic_Field(default_factory=list).
      Add a @property has_downgrades -> bool.
    - Both models: ConfigDict(extra="forbid").
    - Module docstring: one-line role, purpose note "Consumed by tests now; passed to SSE trace in P3."
    - "from __future__ import annotations" at top (project-wide pattern).
    - Alias: "from pydantic import Field as pydantic_Field" (avoids collision with envelope Field).

    grounding/gate.py: Stub only — declare function signatures that raise NotImplementedError so
    imports resolve and tests can be written against real identifiers. Do NOT implement the logic yet
    (that is plan 02-02). The stub must define the exact signatures that plan 02-02 will implement:
    - Module-level constants (comment them as stubs): FUZZY_THRESHOLD: float = 90.0, MIN_SNIPPET_LEN: int = 15
      with # ponytail: comments per PATTERNS.md.
    - _normalize_with_map(text: str) -> tuple[str, list[int]]:
      raise NotImplementedError("_normalize_with_map not implemented")
    - _match_exact(norm_snippet: str, norm_source: str, orig_indices: list[int]) -> tuple[int, int] | None:
      raise NotImplementedError("_match_exact not implemented")
    - _match_fuzzy(norm_snippet: str, norm_source: str, threshold: float, orig_indices: list[int]) -> tuple[int, int] | None:
      raise NotImplementedError("_match_fuzzy not implemented")
    - ground_field(field: EnvelopeField, sources: dict[str, str], field_path: str = "") -> tuple[EnvelopeField, list[DowngradeEntry]]:
      raise NotImplementedError("ground_field not implemented")
    - ground_model(obj: BaseModel, sources: dict[str, str]) -> tuple[BaseModel, DowngradeReport]:
      raise NotImplementedError("ground_model not implemented")
    - Import EnvelopeField alias: "from schemas.envelope import Field as EnvelopeField"
      and add "# ponytail: class named Field shadows pydantic.Field — alias import" (per PATTERNS.md).
    - Import pydantic BaseModel for the ground_model signature.
    - Do NOT import rapidfuzz yet (it may not be installed; importing it in stubs would fail).
    - Module docstring must mention D-01 (search & recompute, never trust model offsets) and D-06
      (pure function, returns new object + downgrade report).
    - Include the function name in each NotImplementedError message for faster debugging during development.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "from grounding import ground_field, ground_model, DowngradeEntry, DowngradeReport; print('imports OK')"</automated>
  </verify>
  <done>
    "from grounding import ground_field, ground_model, DowngradeEntry, DowngradeReport" succeeds
    without ImportError. gate.py stubs raise NotImplementedError("ground_field not implemented")
    (or equivalent named message) when called. report.py instantiates DowngradeReport() with entries=[].
  </done>
</task>

<task type="auto">
  <name>Task 2: test_grounding_gate.py — all EXTRACT-04 unit tests (RED)</name>
  <files>services/ai/tests/test_grounding_gate.py</files>
  <read_first>
    - services/ai/tests/test_field_envelope.py — exact structural analog (docstring, imports, class-per-concern, -> None, Evidence/Field construction patterns)
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Validation Architecture (full test list) and §The Falsifiability Tests (verbatim test A + B implementations)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §test_grounding_gate.py
    - .planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md (per-task verification map)
    - services/ai/schemas/envelope.py — Evidence, Field, FlagStatus, ConflictingValue constructors
    - services/ai/grounding/gate.py (the stubs from Task 1 above — test against these identifiers)
    - services/ai/grounding/report.py (DowngradeEntry, DowngradeReport — asserted in tests)
  </read_first>
  <action>
    Write services/ai/tests/test_grounding_gate.py with ALL nine test cases from VALIDATION.md
    §Per-Task Verification Map for EXTRACT-04. Tests must be in RED (failing with NotImplementedError
    from the stubs, not ImportError) after this task.

    RED test discipline (CRITICAL — do not deviate): call gate functions NORMALLY in every test
    body. Do NOT wrap test bodies in pytest.raises(NotImplementedError). The tests are RED because
    the stubs raise NotImplementedError on call, which causes pytest to report a FAILED test (the
    correct RED state). Wrapping in pytest.raises would invert the RED→GREEN invariant — those tests
    would PASS against stubs and FAIL after implementation, breaking the Nyquist workflow.

    File structure (follow test_field_envelope.py exactly):
    - "from __future__ import annotations" at top
    - File-level docstring (see PATTERNS.md §test_grounding_gate.py for template)
    - imports: pytest, Decimal, Evidence, Field, FlagStatus, ConflictingValue from schemas.envelope;
      ground_field, ground_model from grounding.gate; DowngradeEntry, DowngradeReport from grounding.report

    Test classes (comment-separated, one class per concern):
    1. TestFabricatedSpanDowngrade — contains test_fabricated_span_is_downgraded() per RESEARCH.md
       §Falsifiability Tests (Test A). Copy the exact source/evidence/field construction verbatim.
       Assert: grounded.status == FlagStatus.unsupported, grounded.value is None,
       grounded.evidence == [], len(report) == 1.
    2. TestGenuineSpanPasses — contains test_genuine_span_passes_grounding() per RESEARCH.md
       §Falsifiability Tests (Test B). Assert: grounded.status == FlagStatus.present, len(report) == 0,
       source[ev.char_start:ev.char_end] == "$15,000 for strategy and creative".
       Also add test_offsets_are_recomputed_not_trusted() that provides intentionally wrong char_start=0
       char_end=1 and asserts the gate overwrites them correctly.
    3. TestConflictingField — contains test_conflicting_field_grounded_per_value(): create a Field with
       status=conflicting, values=[ConflictingValue(value="X", evidence=[genuine_ev]),
       ConflictingValue(value="Y", evidence=[fabricated_ev])].
       Expected behavior (document with a docstring): any failed evidence in any ConflictingValue
       downgrades the whole Field to unsupported — this is an intentional conservative tradeoff (D-05);
       the gate never partially preserves conflicting values when grounding fails.
       Assert: returned field.status == FlagStatus.unsupported.
    4. TestFuzzyMatching — contains:
       - test_fuzzy_match_above_threshold_grounds(): source text has "Vendor A proposes $15,000";
         snippet is "vendor a proposes  $15,000" (extra space, lowercase — minor normalization diff).
         Assert grounds successfully.
       - test_fuzzy_match_below_threshold_downgrades(): snippet is completely unrelated text.
         Assert returns unsupported.
    5. TestNFKCLigature — contains test_nfkc_ligature_offset_mapping(): source = "Our ﬁrm proposes $15,000";
       snippet = "firm proposes $15,000" (fi ligature → fi). Assert grounds and offsets point to correct
       region of original source text.
    6. TestShortSnippetGuard — contains test_short_snippet_guard(): snippet shorter than MIN_SNIPPET_LEN
       (e.g. "Q3") must be downgraded even if it coincidentally appears in the source.
    7. TestWalker — contains test_walker_grounds_nested_fields(): create an inline pydantic BaseModel
       with two Field[str] attributes (one genuine, one fabricated). Call ground_model() and assert
       the fabricated one is downgraded and the genuine one survives.

    All test functions use "-> None" return type. Do NOT use pytest.raises anywhere in this file
    (see RED test discipline note above). All assertions run unconditionally — the NotImplementedError
    from stubs surfaces as a test FAILURE (red), which is the correct state before implementation.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_grounding_gate.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>
    All tests collect without ImportError. Tests fail with NotImplementedError (not ImportError
    or SyntaxError) — this is the correct RED state. The exact test names from VALIDATION.md exist:
    test_fabricated_span_is_downgraded, test_genuine_span_passes_grounding,
    test_offsets_are_recomputed_not_trusted, test_conflicting_field_grounded_per_value,
    test_fuzzy_match_above_threshold_grounds, test_fuzzy_match_below_threshold_downgrades,
    test_nfkc_ligature_offset_mapping, test_short_snippet_guard, test_walker_grounds_nested_fields.
    The output shows "FAILED" lines, not "PASSED" lines (confirming RED state, not inverted passes).
  </done>
</task>

<task type="auto">
  <name>Task 3: test_sample_fixtures.py — all DATA-01/02/03 fixture tests (RED)</name>
  <files>services/ai/tests/test_sample_fixtures.py</files>
  <read_first>
    - services/ai/tests/test_codegen_drift.py — repo_root() import pattern + fixture path resolution
    - services/ai/tests/test_field_envelope.py — pydantic model_validate_json pattern
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §test_sample_fixtures.py
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Validation Architecture
    - services/ai/scripts/codegen.py — repo_root() export
    - services/ai/schemas/domain.py — current RFQ + VendorResponse stubs (imports must resolve)
  </read_first>
  <action>
    Write services/ai/tests/test_sample_fixtures.py with all DATA-01/02/03 tests from VALIDATION.md.
    Tests must be RED because data/rfq.json and data/vendor_*.json don't exist yet.

    Follow test_codegen_drift.py for repo_root() + path construction pattern exactly.
    "from __future__ import annotations" at top.

    Module-level constants:
    _DATA_DIR = repo_root() / "data"
    FIXTURE_FILENAMES = {
        "thorough-but-pricey": "vendor_thorough.json",
        "cheap-but-incomplete": "vendor_cheap.json",
        "polished-fluff": "vendor_fluff.json",
    }
    # D-09/D-13: explicit persona→filename map; filenames are authoritative.
    # generate_samples.py must write exactly these names. Do not derive from persona.replace("-","_").

    Fixture test assertion order (CRITICAL): assert file existence FIRST, then deserialize.
    This ensures a pre-02-03 run fails with a clear "file not found" AssertionError rather than
    an AttributeError on a missing schema field. Pattern:
      fixture_path = _DATA_DIR / "rfq.json"
      assert fixture_path.exists(), f"Missing fixture: {fixture_path}"
      rfq = RFQ.model_validate_json(fixture_path.read_text())

    Test functions (all -> None):
    - test_rfq_fixture_valid(): assert (_DATA_DIR / "rfq.json").exists() [FIRST, before model_validate];
      RFQ.model_validate_json(content); assert isinstance(rfq, RFQ); assert len(rfq.line_items) == 8.
      Note: exactly 8 is the test-enforced contract (test-only, no model_validator needed for the
      prototype); the rfq-gen prompt specifies 8 named line items.
    - test_vendor_fixtures_exist_and_valid(): parametrize over list(FIXTURE_FILENAMES.values())
      OR write three separate tests. Each: assert (_DATA_DIR / filename).exists() [FIRST];
      VendorResponse.model_validate_json(); assert isinstance(v, VendorResponse);
      assert v.raw_text is not None and len(v.raw_text) > 200.
    - test_vendor_fixture_messiness(): load all three vendors using FIXTURE_FILENAMES;
      thorough has "bundle" OR "all-inclusive" OR "package" (case-insensitive) in raw_text;
      cheap has a missing price marker (no "$" for at least one line item section, OR "TBD"
      OR "price not provided"); fluff has two contradictory terms (e.g. "weeks" appear with
      conflicting numbers, or timeline is stated differently in two places).
    - test_cheap_incomplete_has_missing_price(): load vendor_cheap.json via FIXTURE_FILENAMES;
      assert file exists FIRST; assert "tbd" in raw.lower() or "not provided" in raw.lower()
      or "no price" in raw.lower() or a line-item section has no dollar amount. Use a regex
      or simple string search — not an LLM call (D-13).
    - test_polished_fluff_has_conflict(): load vendor_fluff.json via FIXTURE_FILENAMES;
      assert file exists FIRST; assert raw_text contains at least two timeline or scope statements
      that contradict (look for two different week counts or two different "will" / "won't"
      statements near the same line item using simple string patterns).

    The exact assertions for messiness are intentionally flexible (string-search, not structural)
    because the committed fixture text is raw prose. Mark the messiness check constants with
    comments referencing D-09/D-13 so the executor knows why the assertions are keyword-based
    rather than schema-based. If the model generates unexpected phrasing (e.g. "price to be
    confirmed" vs "TBD"), the fix is in the vendor-gen prompt, not the test assertion.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_sample_fixtures.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>
    Pytest collects all tests without ImportError or SyntaxError. Running the suite fails with
    FileNotFoundError or AssertionError on the "assert file exists" line (data/rfq.json not found)
    — not AttributeError or ImportError. The exact test names from VALIDATION.md exist:
    test_rfq_fixture_valid, test_vendor_fixtures_exist_and_valid,
    test_vendor_fixture_messiness, test_cheap_incomplete_has_missing_price, test_polished_fluff_has_conflict.
    FIXTURE_FILENAMES constant is defined and uses the authoritative short names
    (vendor_thorough.json, vendor_cheap.json, vendor_fluff.json).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → module stubs | Tests call stub functions that raise NotImplementedError; no external input crosses here in this plan |
| test harness → domain stubs | test_sample_fixtures.py imports RFQ/VendorResponse stubs; schema is code-defined, no external data yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | gate.py stub imports | accept | Stubs raise NotImplementedError — no code path that trusts model output exists yet; real mitigation is in plan 02-02 |
| T-02-02 | Tampering | test_sample_fixtures.py path construction | mitigate | Use repo_root() from codegen.py (already tested); never construct paths from user-supplied strings |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (rapidfuzz added in 02-02); all imports are from existing deps |
</threat_model>

<verification>
After completing all three tasks:
- uv run pytest tests/test_grounding_gate.py -x -q — fails with NotImplementedError (not ImportError); output shows "FAILED" lines
- uv run pytest tests/test_sample_fixtures.py --collect-only -q — collects all 5 tests
- uv run pytest tests/test_sample_fixtures.py -x -q — fails with AssertionError on file-existence check (not AttributeError)
- uv run pytest tests/test_grounding_gate.py tests/test_sample_fixtures.py -q 2>&1 | grep -v '^$' — no ImportError lines, only NotImplementedError/AssertionError failures
- python -c "from grounding import ground_field, ground_model, DowngradeEntry, DowngradeReport; print('OK')" — prints OK
- grep -n "pytest.raises" services/ai/tests/test_grounding_gate.py — should return no matches (no pytest.raises usage)
</verification>

<success_criteria>
- grounding/__init__.py, gate.py (stubs), report.py all exist and import cleanly
- gate.py stub NotImplementedError messages include function name (e.g. "ground_field not implemented")
- test_grounding_gate.py: 9 test cases from VALIDATION.md collected, failing RED with NotImplementedError; no pytest.raises wrappers
- test_sample_fixtures.py: 5 test cases from VALIDATION.md collected, failing RED with AssertionError on file-existence check (not AttributeError); FIXTURE_FILENAMES defined with authoritative short filenames
- No ImportError anywhere in the test collection output
- Full test suite run (uv run pytest -x -q) shows all existing tests (test_field_envelope, test_codegen_drift, test_prompt_registry, test_llm_factory) still passing
</success_criteria>

<output>
Create .planning/phases/02-grounding-gate-messy-data/02-01-SUMMARY.md when done.
</output>
