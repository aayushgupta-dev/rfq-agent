---
phase: 02-grounding-gate-messy-data
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - services/ai/grounding/gate.py
  - services/ai/grounding/report.py
  - services/ai/pyproject.toml
autonomous: true
requirements:
  - EXTRACT-04

must_haves:
  truths:
    - "A fabricated evidence span (not in source text) is downgraded to status=unsupported; value and evidence are suppressed — no model-asserted verified flag can promote it (D-01: gate ignores model-supplied offsets, searches the source and recomputes)"
    - "A genuine evidence span present in the source text passes grounding and keeps its value (no over-rejection)"
    - "Recomputed char_start/char_end are verified: source_text[char_start:char_end] == the original snippet"
    - "A Field[T] with status=conflicting has each ConflictingValue.evidence grounded independently (D-05: single-field core handles the conflicting values[] case)"
    - "A snippet with minor whitespace/normalization diff grounds above threshold; a completely unrelated snippet is downgraded (D-02: moderate normalization — NFKC/casefold/smart-quotes, symbols kept; D-03: rapidfuzz partial_ratio fuzzy fallback at ~90)"
    - "NFKC ligature (ﬁ → fi) is matched correctly and offsets map to the original source span"
    - "Snippets shorter than MIN_SNIPPET_LEN are downgraded unconditionally (Pitfall 3 guard)"
    - "The recursive walker finds and re-grounds every Field[T] in a nested pydantic model (D-05: schema-agnostic walker, works without knowing ExtractionResult's shape)"
    - "ground_field/ground_model are pure functions: they return a NEW object (model_copy) plus a structured downgrade report, never mutating input; sources are supplied as dict[str, str] keyed by Evidence.source_id (D-06: pure function + report; D-07: sources keyed by source_id)"
  artifacts:
    - path: "services/ai/grounding/gate.py"
      provides: "Full grounding gate: _normalize_with_map, _match_exact, _match_fuzzy, ground_field, ground_model"
    - path: "services/ai/grounding/report.py"
      provides: "DowngradeEntry + DowngradeReport (may be unchanged from plan 02-01 if already correct)"
    - path: "services/ai/pyproject.toml"
      provides: "rapidfuzz added to [project.dependencies]"
  key_links:
    - from: "grounding/gate.py"
      to: "schemas/envelope.py"
      via: "from schemas.envelope import Field as EnvelopeField, Evidence, FlagStatus"
      pattern: "EnvelopeField"
    - from: "grounding/gate.py"
      to: "rapidfuzz"
      via: "from rapidfuzz.fuzz import partial_ratio_alignment"
      pattern: "partial_ratio_alignment"
    - from: "grounding/gate.py"
      to: "unicodedata"
      via: "import unicodedata"
      pattern: "unicodedata.normalize"
---

<objective>
Implement the grounding gate — the reliability keystone that "disproves the model" (D-01).
Replace the NotImplementedError stubs in gate.py with the full two-stage normalization,
exact matching, fuzzy fallback, downgrade logic, conflicting-field branch, and recursive walker.
All 9 EXTRACT-04 tests from plan 02-01 must turn GREEN.

Purpose: CLAUDE.md §8 — grounding enforced in CODE. The gate ignores model-supplied offsets
entirely (D-01), recomputes REAL offsets via the two-stage normalization map (D-04), and
downgrades any fact whose evidence cannot be located to status=unsupported (D-06).
No LLM is involved — this is pure deterministic Python.

Output:
- services/ai/grounding/gate.py (complete implementation)
- services/ai/pyproject.toml updated with rapidfuzz dependency
- All 9 EXTRACT-04 unit tests GREEN
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-01-SUMMARY.md

<interfaces>
From services/ai/schemas/envelope.py (the gate's I/O contract — read this file first):

class FlagStatus(StrEnum):
    present = "present"; missing = "missing"; unclear = "unclear"
    conflicting = "conflicting"; unsupported = "unsupported"

class Evidence(BaseModel):
    snippet: str; char_start: int; char_end: int; source_id: str

class ConflictingValue(BaseModel, Generic[T]):  # noqa: UP046
    value: T | None = None
    evidence: list[Evidence] = pydantic_Field(default_factory=list)

class Field(BaseModel, Generic[T]):  # noqa: UP046
    status: FlagStatus; value: T | None = None
    evidence: list[Evidence] = pydantic_Field(default_factory=list)
    values: list[ConflictingValue[T]] | None = None

Key semantic rule: status=unsupported → model_validator enforces value=None, evidence=[].
Construct unsupported downgrade as: EnvelopeField(status=FlagStatus.unsupported)
The validator sets value=None and evidence=[] automatically.

Offset recompute: model_copy(update={"evidence": new_evidence_list}) with
Evidence(snippet=ev.snippet, char_start=recomputed_start, char_end=recomputed_end,
source_id=ev.source_id) — recomputed offsets replace original ones.

From services/ai/grounding/report.py (already correct from plan 02-01):
class DowngradeEntry(BaseModel): field_path: str; original_status: str; failed_evidence: Evidence; reason: str
class DowngradeReport(BaseModel): entries: list[DowngradeEntry] = []; has_downgrades: bool property
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install rapidfuzz and implement _normalize_with_map + _match_exact + _match_fuzzy (with offset-map correctness fix)</name>
  <files>services/ai/pyproject.toml, services/ai/grounding/gate.py, services/ai/tests/test_grounding_gate.py</files>
  <read_first>
    - services/ai/pyproject.toml — current [project.dependencies] block; add rapidfuzz there
    - services/ai/schemas/envelope.py — import patterns and alias guard
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Pattern 2 (Two-Stage Normalization With Dual Mapping) — the exact algorithm with stage1_to_orig + surviving_positions composition; use verbatim
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Pattern 3 (partial_ratio_alignment — Offset Recovery) — exact rapidfuzz call with ScoreAlignment.dest_start/dest_end semantics; use verbatim
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Common Pitfalls §Pitfall 1 (single-pass normalization breaks map) and §Pitfall 2 (dest_end is exclusive)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §grounding/gate.py (module docstring, constants, alias import guard, ponytail comments)
    - services/ai/tests/test_grounding_gate.py — the test file from plan 02-01 (add the new _normalize_with_map unit test here)
  </read_first>
  <action>
    Step 1 — install rapidfuzz:
    From services/ai/, run "uv add rapidfuzz" to add it to pyproject.toml and the uv.lock.
    Confirm the package legitimacy audit in RESEARCH.md §Package Legitimacy Audit confirms it
    as [VERIFIED: PyPI + official GitHub] before installing.

    Step 2 — implement the three helper functions in gate.py, replacing the NotImplementedError stubs.
    Keep the module docstring, FUZZY_THRESHOLD, MIN_SNIPPET_LEN constants, and alias imports from
    the stubs. Add "import unicodedata", "import re" (for whitespace collapse if needed), and
    "from rapidfuzz.fuzz import partial_ratio_alignment".

    _normalize_with_map(text: str) -> tuple[str, list[int]]:
    Implement the two-stage algorithm from RESEARCH.md Pattern 2 exactly.

    OFFSET-MAP CORRECTNESS (Codex review concern): avoid .strip() on normalized text alone, as
    stripping after building orig_indices shifts the mapping. Instead, trim leading/trailing
    whitespace characters out of the surviving_positions list together with the normalized string:
    after stage 2 produces (normalized_str, surviving_positions), find leading_spaces =
    len(normalized_str) - len(normalized_str.lstrip()) and trailing_spaces =
    len(normalized_str) - len(normalized_str.rstrip()); slice both together:
      normalized = normalized_str[leading_spaces : len(normalized_str) - trailing_spaces or None]
      surviving_positions = surviving_positions[leading_spaces : len(surviving_positions) - trailing_spaces or None]
    Then compose: orig_indices = [stage1_to_orig[s1_i] for s1_i in surviving_positions[:len(normalized)]].
    This guarantees orig_indices[i] is the original-text position for normalized[i] with no index shift.

    Stage 1: iterate char-by-char; for each original index orig_i, call
    unicodedata.normalize("NFKC", ch) then apply smart-quote/dash replacements
    (D-02: replace curly apostrophes → straight, curly quotes → straight, en/em dash → hyphen),
    then .casefold(); extend stage1_to_orig with [orig_i] * len(normalized_char).
    Stage 2: iterate stage1 characters; collapse runs of whitespace to a single space, tracking
    surviving_positions (the stage1 index of each surviving char). Apply the trim-together step above.
    Return (normalized, orig_indices).

    Add a code comment on the NFD→NFC conservative-offset behavior: "# NFD decomposed source
    (e.g. 'e' + combining acute) is composed to 'é' by NFKC. The resulting offset points to
    the base character position in the original string — this is intentionally conservative and
    acceptable for grounding (snippet highlights the composed character region)."

    Add "# ponytail: two-stage normalization map exists because NFKC expansion and whitespace
    collapse both change string length; single-pass assumption breaks offset recovery (D-04)."

    _match_exact(norm_snippet: str, norm_source: str, orig_indices: list[int]) -> tuple[int, int] | None:
    Guard: if not norm_snippet or not norm_source: return None.
    Use norm_source.find(norm_snippet). If -1, return None. Otherwise:
    norm_start = found_index; norm_end_excl = norm_start + len(norm_snippet).
    char_start = orig_indices[norm_start]; char_end = orig_indices[norm_end_excl - 1] + 1.
    Return (char_start, char_end).

    _match_fuzzy(norm_snippet: str, norm_source: str, threshold: float, orig_indices: list[int]) -> tuple[int, int] | None:
    Guard: if not norm_snippet or not norm_source: return None.
    Implement per RESEARCH.md Pattern 3. Call partial_ratio_alignment(norm_snippet, norm_source,
    score_cutoff=threshold). If None, return None.
    char_start = orig_indices[result.dest_start]; char_end = orig_indices[result.dest_end - 1] + 1.
    (Pitfall 2: dest_end is exclusive — use dest_end - 1 for the last char index.)
    Return (char_start, char_end).

    Step 3 — add a dedicated unit test for _normalize_with_map to test_grounding_gate.py:
    Add class TestNormalizeWithMap to the existing test file (which already has TestNFKCLigature
    etc.). Add one test method: test_nfkc_ligature_offset_roundtrip() -> None:
      source = "Our ﬁrm offers services"
      normalized, orig_indices = _normalize_with_map(source)
      # "fi" from ligature ﬁ should appear in normalized text
      fi_pos = normalized.find("fi")
      assert fi_pos >= 0, "NFKC ligature ﬁ must normalize to 'fi'"
      # Round-trip: original text sliced via orig_indices must equal source chars at that region
      orig_start = orig_indices[fi_pos]
      orig_end = orig_indices[fi_pos + 1] + 1   # +1 since ﬁ is one char in source
      assert source[orig_start:orig_end] == "ﬁ", f"Expected 'ﬁ', got {source[orig_start:orig_end]!r}"
    Also add: test_normalize_strips_leading_trailing_consistently() -> None that asserts
    len(_normalize_with_map("  hello  ")[1]) == len(_normalize_with_map("  hello  ")[0])
    (mapping array length must equal normalized string length after trim-together).
    Import _normalize_with_map at the top of the test file (add to existing import line from grounding.gate).
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run python -c "from grounding.gate import _normalize_with_map, _match_exact, _match_fuzzy; t='Vendor A proposes ﬁrm'; n,m=_normalize_with_map(t); print('norm:', n); i=n.find('firm'); print('firm at norm:', i, '-> orig:', m[i] if i >= 0 else 'miss'); assert len(n)==len(m), 'map length mismatch'"</automated>
  </verify>
  <done>
    "uv add rapidfuzz" succeeds and pyproject.toml lists rapidfuzz in dependencies.
    _normalize_with_map handles the ﬁ ligature: normalizing "Vendor A proposes ﬁrm" produces
    "vendor a proposes firm" and the orig_indices array has the same length as the normalized string.
    _normalize_with_map("  hello  ") returns normalized of length 5 and an orig_indices of length 5
    (trim-together applied). No NotImplementedError on the three helper functions.
    TestNormalizeWithMap tests collect in the test file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ground_field (including conflicting branch and original_status fix) and ground_model walker; calibrate FUZZY_THRESHOLD</name>
  <files>services/ai/grounding/gate.py, services/ai/tests/test_grounding_gate.py</files>
  <read_first>
    - services/ai/schemas/envelope.py — full file; especially the model_validator rules for unsupported (value=None, evidence=[]) and the conflicting field shape (values[], empty top-level evidence)
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Code Examples §Grounding Gate: Core Logic Sketch — the ground_field skeleton with _ground_evidence helper
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Common Pitfalls §Pitfall 5 (ConflictingValue fields — evidence is INSIDE each ConflictingValue, not at top level)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §grounding/gate.py §Conflicting field branch and §Downgrade to unsupported
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Pattern 4 (Schema-Agnostic Recursive Field[T] Walker) — the _walk_and_ground implementation
    - services/ai/grounding/report.py (DowngradeEntry constructor — needs field_path, original_status, failed_evidence, reason)
    - services/ai/tests/test_grounding_gate.py — add new tests for source-id-missing and non-mutation (add to existing file)
  </read_first>
  <action>
    Implement ground_field and ground_model in gate.py, replacing their NotImplementedError stubs.

    Internal helper _ground_evidence_item(ev: Evidence, source_text: str, field_path: str,
    field_status: str) -> tuple[Evidence | None, DowngradeEntry | None]:
    The field_status parameter carries the ENCLOSING FIELD'S real status (FlagStatus value as str)
    and MUST be passed in from ground_field so DowngradeEntry.original_status is accurate.
    Do NOT hardcode original_status="present" or use ev.source_id as the status value.

    1. Guard: if not ev.snippet: return None, DowngradeEntry(field_path=field_path,
       original_status=field_status, failed_evidence=ev, reason="empty snippet").
    2. Guard: if not source_text: return None, DowngradeEntry(field_path=field_path,
       original_status=field_status, failed_evidence=ev, reason="empty source text").
    3. If len(ev.snippet) < MIN_SNIPPET_LEN: return None, DowngradeEntry(field_path=field_path,
       original_status=field_status, failed_evidence=ev,
       reason=f"snippet too short ({len(ev.snippet)} < {MIN_SNIPPET_LEN})").
    4. norm_source, orig_src_indices = _normalize_with_map(source_text)
    5. norm_snippet, _ = _normalize_with_map(ev.snippet)
    6. Try _match_exact → if hit, recompute offsets, return new Evidence with recomputed offsets, None.
    7. Try _match_fuzzy → if hit, recompute offsets, return new Evidence with recomputed offsets, None.
    8. Return None, DowngradeEntry(field_path=field_path, original_status=field_status,
       failed_evidence=ev, reason="snippet not locatable in source text").

    ground_field(field, sources, field_path="") -> tuple[EnvelopeField, list[DowngradeEntry]]:
    Per D-07, sources is dict[str, str] keyed by Evidence.source_id.
    - If field.status in (FlagStatus.missing, FlagStatus.unsupported): return field, [] immediately.
    - If field.status == FlagStatus.conflicting (Pitfall 5): iterate field.values; for each cv, ground
      each Evidence in cv.evidence against sources.get(ev.source_id). If source_id not found, add
      DowngradeEntry(original_status=field.status.value, reason="source_id not in sources").
      Collect new_values. If any downgrade → return EnvelopeField(status=FlagStatus.unsupported), entries.
      Add comment: "# ponytail: any failed evidence across all ConflictingValues downgrades the
      WHOLE Field to unsupported — intentional conservative tradeoff (D-05). Partial preservation
      of conflicting values would create ambiguous grounding state; the gate prefers to surface the
      conflict as unsupported and let the buyer see a flag rather than partial facts."
      Else return field.model_copy(update={"values": new_values}), [].
    - For present/unclear: iterate field.evidence; for each ev, check sources.get(ev.source_id).
      If None, add DowngradeEntry(field_path=field_path, original_status=field.status.value,
      failed_evidence=ev, reason="source_id not in sources"). Else call
      _ground_evidence_item(ev, source_text, field_path, field_status=field.status.value).
      If any DowngradeEntry accumulated → return EnvelopeField(status=FlagStatus.unsupported), entries.
      Else return field.model_copy(update={"evidence": new_evidence_list}), [].

    ground_model(obj: BaseModel, sources: dict[str, str]) -> tuple[BaseModel, DowngradeReport]:
    Implement the _walk_and_ground recursive function from RESEARCH.md Pattern 4 inline or as a
    private helper. Iterate obj.model_fields.items(); getattr(obj, field_name) to get value.
    - isinstance(value, EnvelopeField) → call ground_field
    - isinstance(value, BaseModel) → recurse
    - isinstance(value, list) → iterate items, recurse on EnvelopeField and BaseModel items
    Use obj.model_copy(update=updates) per D-06 (never mutate in place).
    Return (new_obj, DowngradeReport(entries=all_entries)).

    Add to test_grounding_gate.py — new test class TestSourceIdMissing:
    - test_missing_source_id_downgrades(): construct a Field with status=present and
      Evidence with source_id="v_unknown"; call ground_field with sources={"v_other": "some text"}.
      Assert returned field.status == FlagStatus.unsupported and report has one entry.
    Add to TestWalker (existing class):
    - test_ground_model_does_not_mutate_input(): construct a model with a genuine Field;
      call ground_model(); assert the ORIGINAL field object is unchanged (input object identity
      preserved via model_copy, not in-place mutation). Compare original_field.status before
      and after ground_model() call.
    Add docstring to test_conflicting_field_grounded_per_value (already written in plan 02-01)
    if not already present: "Conservative grounding tradeoff: any failed evidence across any
    ConflictingValue downgrades the whole Field to unsupported. This is intentional — partial
    preservation of conflicting values would create ambiguous grounding state (see gate.py ponytail comment)."

    FUZZY_THRESHOLD CALIBRATION (after implementing the gate and confirming 9 tests pass):
    Run the gate against the test data in the existing test suite and log fuzzy scores.
    Execute: cd services/ai && uv run pytest tests/test_grounding_gate.py -x -q -s 2>&1 | grep -i fuzzy
    Manually verify that:
    - test_fuzzy_match_above_threshold_grounds passes (minor-whitespace snippet scores above 90)
    - test_fuzzy_match_below_threshold_downgrades passes (unrelated snippet scores below 90)
    If test_fuzzy_match_above_threshold_grounds fails with the default threshold (genuine span
    rejected), lower FUZZY_THRESHOLD in gate.py in increments of 2 (88, 86...) until it passes.
    If test_fuzzy_match_below_threshold_downgrades fails (fabricated span slips through), raise
    FUZZY_THRESHOLD until it fails. Document the calibrated value as a comment:
    "# FUZZY_THRESHOLD calibrated against test fixtures — genuine minor-whitespace diffs score
    ~{measured_score}; set to {calibrated_value} to accept genuine fuzzy matches while
    rejecting fabricated spans."
    Starting point 90.0 is expected to work; calibrate only if a test fails.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_grounding_gate.py -x -q 2>&1</automated>
  </verify>
  <done>
    All 9 EXTRACT-04 tests in test_grounding_gate.py pass GREEN, plus the new TestNormalizeWithMap
    and TestSourceIdMissing tests. In particular:
    test_fabricated_span_is_downgraded: PASS (Test A — phase goal success criterion 1).
    test_genuine_span_passes_grounding: PASS (Test B — phase goal success criterion 2).
    test_offsets_are_recomputed_not_trusted: PASS (source[char_start:char_end] == snippet).
    test_nfkc_ligature_offset_roundtrip: PASS (ﬁ ligature maps correctly).
    test_normalize_strips_leading_trailing_consistently: PASS (map array length == normalized length).
    test_missing_source_id_downgrades: PASS (missing source_id → unsupported).
    test_ground_model_does_not_mutate_input: PASS (original object unchanged).
    DowngradeEntry.original_status reflects the real field status, not a hardcoded "present".
    FUZZY_THRESHOLD calibrated (default 90.0 or adjusted) with a comment documenting measured scores.
    Full test output shows all new + original tests passed, 0 failed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → ground_model | Caller supplies source texts as dict[str, str]; gate treats them as untrusted strings |
| Model output → gate input | Model-supplied char_start/char_end are IGNORED entirely (D-01); gate recomputes from scratch |
| Vendor text → normalization | Vendor text may contain adversarial Unicode; NFKC normalization + casefold happens before matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | Model-supplied char_start/char_end offsets | mitigate | D-01: gate ignores model offsets entirely; all char_start/char_end are recomputed from scratch via _normalize_with_map + _match_exact/_match_fuzzy |
| T-02-04 | Spoofing | Model-asserted verified/grounded flag | mitigate | No such flag in the schema; gate never reads a "grounded" field — only the raw snippet vs source text comparison determines grounding |
| T-02-05 | Spoofing | Fabricated snippet scoring high on partial_ratio | mitigate | MIN_SNIPPET_LEN guard (Pitfall 3): snippets < 15 chars are unconditionally downgraded; plus FUZZY_THRESHOLD (calibrated, default 90.0) rejects partial matches below threshold |
| T-02-06 | Tampering | Prompt injection via vendor text into normalization | accept | Gate is LLM-free; vendor text is processed by unicodedata + string comparison only — no code execution surface; accept |
| T-02-07 | Tampering | source_id used as filesystem path | accept | sources is dict[str, str] in caller-controlled scope — source_id is a dict key, never a filesystem path; accept |
| T-02-SC | Tampering | pip install rapidfuzz | mitigate | rapidfuzz is [VERIFIED: PyPI + official GitHub] per RESEARCH.md Package Legitimacy Audit — MIT, 6-year history, 25M/week downloads; no blocking checkpoint needed for this verified package |
</threat_model>

<verification>
After both tasks complete:
- cd services/ai && uv run pytest tests/test_grounding_gate.py -x -q → all tests pass (9 original + new normalize/source-id/non-mutation tests)
- Test A: test_fabricated_span_is_downgraded PASS (fabricated $99 snippet → unsupported, value None, evidence [])
- Test B: test_genuine_span_passes_grounding PASS (genuine $15,000 snippet survives, recomputed offsets confirmed)
- test_nfkc_ligature_offset_roundtrip PASS (source[orig_start:orig_end] == "ﬁ")
- test_normalize_strips_leading_trailing_consistently PASS (map array length == normalized length)
- test_missing_source_id_downgrades PASS
- test_ground_model_does_not_mutate_input PASS
- Full suite: cd services/ai && uv run pytest -x -q → all existing tests still green
- python -c "from grounding import ground_field, ground_model; from schemas.envelope import Field, FlagStatus, Evidence; e=Evidence(snippet='$15,000 for strategy', char_start=0, char_end=1, source_id='v1'); f=Field[str](status=FlagStatus.present, value='15000', evidence=[e]); r,report=ground_field(f, {'v1': 'Vendor A proposes $15,000 for strategy and creative'}); print(r.status, r.evidence[0].char_start, r.evidence[0].char_end)"
- Verify DowngradeEntry.original_status == "present" (not "v1" or hardcoded string) in fabricated-span test
</verification>

<success_criteria>
- rapidfuzz listed in services/ai/pyproject.toml [project.dependencies]
- All test_grounding_gate.py tests GREEN (0 failures) including new normalize, source-id, non-mutation tests
- Falsifiability test A: fabricated span → unsupported, value=None, evidence=[]
- Falsifiability test B: genuine span → present, source[start:end] == snippet text
- NFKC ligature unit test: _normalize_with_map("Our ﬁrm") → orig_indices[fi_pos] maps to ﬁ position
- _normalize_with_map trim-together: len(normalized) == len(orig_indices) for text with leading/trailing spaces
- DowngradeEntry.original_status == field.status.value (the actual field status, not hardcoded)
- Conflicting-field downgrade-all behavior documented in code comment (conservative tradeoff, D-05)
- FUZZY_THRESHOLD calibrated and commented with measured score evidence
- NFD→NFC conservative-offset behavior documented in code comment
</success_criteria>

<output>
Create .planning/phases/02-grounding-gate-messy-data/02-02-SUMMARY.md when done.
</output>
