---
phase: 02-grounding-gate-messy-data
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - services/ai/agents/rfq_gen.py
  - services/ai/agents/vendor_gen.py
  - services/ai/scripts/generate_samples.py
  - services/ai/api/app.py
  - data/rfq.json
  - data/rfq.md
  - data/vendor_thorough.json
  - data/vendor_cheap.json
  - data/vendor_fluff.json
  - docs/prompts/data-generation.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - PROMPT-04

must_haves:
  truths:
    - "data/rfq.json exists, deserializes to a valid RFQ with exactly 8 line_items"
    - "data/vendor_thorough.json, vendor_cheap.json, vendor_fluff.json exist and deserialize to valid VendorResponse instances with non-empty raw_text"
    - "Each vendor fixture exhibits its declared mess spec issues: thorough has bundled/over-scoped pricing, cheap has missing price markers, fluff has conflicting statements (D-09: mess specs are hand-authored in code so each issue type is assertable; D-10: exactly 3 complementary personas — thorough-but-pricey, cheap-but-incomplete, polished-fluff)"
    - "All DATA-01/02/03 fixture tests in test_sample_fixtures.py pass GREEN (D-13: assertions run against the COMMITTED fixtures, not live LLM regen)"
    - "GET /data/rfq returns a valid JSON RFQ; POST /data/vendor-gen returns a VendorResponse JSON for a given persona (accepts optional rfq_text; generates a fresh RFQ only when rfq_text is omitted)"
    - "At least one documented prompt failure example + fix is captured in docs/prompts/data-generation.md alongside versioning/eval notes (PROMPT-04) (D-14: real failure harvested from authoring rfq-gen/vendor-gen if one occurred; if none occurred, labeled honestly as an anticipated failure-mode + mitigation)"
  artifacts:
    - path: "services/ai/agents/rfq_gen.py"
      provides: "generate_rfq() -> RFQ via rfq-gen prompt + with_structured_output"
    - path: "services/ai/agents/vendor_gen.py"
      provides: "generate_vendor_response(rfq_text, persona, mess_spec) -> VendorResponse; MESS_SPECS dict; FIXTURE_FILENAMES dict"
    - path: "services/ai/scripts/generate_samples.py"
      provides: "CLI that writes data/rfq.json + rfq.md + 3 vendor JSON files using FIXTURE_FILENAMES"
    - path: "services/ai/api/app.py"
      provides: "GET /data/rfq and POST /data/vendor-gen endpoints (DATA-04 live regen)"
    - path: "data/rfq.json"
      provides: "Committed sample RFQ fixture"
    - path: "data/vendor_thorough.json"
      provides: "Committed sample vendor fixture — thorough-but-pricey persona"
    - path: "data/vendor_cheap.json"
      provides: "Committed sample vendor fixture — cheap-but-incomplete persona"
    - path: "data/vendor_fluff.json"
      provides: "Committed sample vendor fixture — polished-fluff persona"
    - path: "docs/prompts/data-generation.md"
      provides: "PROMPT-04: per-prompt docs for rfq-gen/vendor-gen/messy-data-gen + ≥1 failure example + versioning notes"
  key_links:
    - from: "services/ai/api/app.py"
      to: "services/ai/agents/rfq_gen.py"
      via: "from agents.rfq_gen import generate_rfq"
      pattern: "generate_rfq"
    - from: "services/ai/scripts/generate_samples.py"
      to: "services/ai/agents/vendor_gen.py"
      via: "from agents.vendor_gen import generate_vendor_response, MESS_SPECS, FIXTURE_FILENAMES"
      pattern: "FIXTURE_FILENAMES"
    - from: "data/rfq.json"
      to: "services/ai/schemas/domain.py"
      via: "RFQ.model_validate_json()"
      pattern: "line_items"
---

<objective>
Build the generation agents (rfq_gen.py, vendor_gen.py), run them to produce the committed sample
data fixtures, wire the live-regen API endpoints, and document the Prompt Pack with the PROMPT-04
failure example + versioning notes.

This plan closes the DATA-04 loop: sample data exists in data/ AND is regenerable live in-app.
It also closes PROMPT-04: the data-generation prompts are documented with what/why/how-handles-unreliable-info
plus the mandatory failure example + fix (captured honestly from the authoring process in plan 02-03).

Purpose: Without committed fixtures, the DATA-01/02/03 tests from plan 02-01 can't go GREEN.
Without live regen, DATA-04 is incomplete. Both streams converge here.

Output:
- rfq_gen.py + vendor_gen.py (agents that call the prompts)
- generate_samples.py (CLI to write data/)
- Committed data/ fixtures (5 files: rfq.json, rfq.md, vendor_thorough.json, vendor_cheap.json, vendor_fluff.json)
- api/app.py updated with /data/rfq and /data/vendor-gen endpoints
- docs/prompts/data-generation.md (PROMPT-04 documentation)
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-02-SUMMARY.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-03-SUMMARY.md

<interfaces>
From services/ai/schemas/domain.py (fleshed by plan 02-03):
class MessSpecItem(BaseModel): line_item: str, issue_type: str, instruction: str
class LineItem(BaseModel): id, name, description, deliverables, timeline_weeks, budget_range_usd
class RFQ(BaseModel): title, client_name, issue_date, response_deadline, scope_summary,
    line_items: list[LineItem], commercial_expectations, questionnaire, compliance_requirements, budget_total_usd
class VendorResponse(BaseModel): vendor_name, persona, mess_spec: list[MessSpecItem], source_id, format_label, raw_text: str

From services/ai/agents/_demo.py (structural analog — read before writing rfq_gen.py/vendor_gen.py):
# module docstring, from __future__ import annotations, get_llm() call pattern

From services/ai/llm/factory.py:
def get_llm(tier: Literal["reasoning", "cheap"]): ...  # always via tier, never model id

From services/ai/prompts/registry.py:
def load(prompt_id: str) -> frontmatter.Post: ...  # always via load(), never open()

From services/ai/scripts/codegen.py:
def repo_root() -> Path: ...  # reuse for generate_samples.py path resolution

From services/ai/api/app.py (existing route pattern to match):
@app.get("/stream/demo") async def stream_demo() -> EventSourceResponse: ...
# New endpoints are synchronous GET/POST returning dict, no SSE needed (RESEARCH.md Open Question 3)

From services/ai/tests/test_sample_fixtures.py (written in plan 02-01):
FIXTURE_FILENAMES = {
    "thorough-but-pricey": "vendor_thorough.json",
    "cheap-but-incomplete": "vendor_cheap.json",
    "polished-fluff": "vendor_fluff.json",
}
# FIXTURE_FILENAMES in vendor_gen.py must export IDENTICAL values to match what test_sample_fixtures.py expects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rfq_gen.py + vendor_gen.py agents + generate_samples.py CLI (with API pre-check and capped iteration) + run to commit fixtures</name>
  <files>services/ai/agents/rfq_gen.py, services/ai/agents/vendor_gen.py, services/ai/scripts/generate_samples.py, data/rfq.json, data/rfq.md, data/vendor_thorough.json, data/vendor_cheap.json, data/vendor_fluff.json</files>
  <read_first>
    - services/ai/agents/_demo.py — module docstring, from __future__ import annotations, LangGraph/chain pattern to adapt
    - services/ai/llm/factory.py — get_llm() signature, "reasoning" tier
    - services/ai/prompts/registry.py — load() signature and usage
    - services/ai/schemas/domain.py — RFQ, VendorResponse, LineItem, MessSpecItem field names (from plan 02-03)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §rfq_gen.py, §vendor_gen.py, §generate_samples.py
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Pattern 5 (LangChain structured output for RFQ), §Pattern 6 (plain-text call for vendor), §Architecture Patterns (system diagram for both paths)
    - .planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md §D-08 (one-pass), §D-09 (hand-authored mess specs), §D-10 (3 personas), §D-12 (format diversity)
    - services/ai/scripts/codegen.py — repo_root() export; write pattern (repo_root() / "data")
    - services/ai/tests/test_sample_fixtures.py — FIXTURE_FILENAMES constant (vendor_gen.py must export matching values)
  </read_first>
  <action>
    Write services/ai/agents/rfq_gen.py:
    - Module docstring: role, "single structured LangChain call — no LangGraph", references D-11.
    - generate_rfq() -> RFQ: load("rfq-gen"), ChatPromptTemplate.from_messages([("system", post.content),
      ("human", "Generate the RFQ now.")]), get_llm("reasoning").with_structured_output(RFQ, method="json_schema"),
      chain.invoke({}). Return the validated RFQ instance.
    - render_rfq_md(rfq: RFQ) -> str: format the RFQ as a readable Markdown document. Use
      f-string template with a "# {rfq.title}" heading, client/date section, scope summary, a table
      or bullet list of line items (id, name, description, deliverables, timeline_weeks, budget_range_usd),
      commercial expectations, questionnaire as numbered list, compliance as bullet list.
      Pure string formatting — no LLM call.

    Write services/ai/agents/vendor_gen.py:
    - Module docstring: role, "one-pass generation — see D-08", 3 personas per D-10.
    - FIXTURE_FILENAMES: dict[str, str] = {
        "thorough-but-pricey": "vendor_thorough.json",
        "cheap-but-incomplete": "vendor_cheap.json",
        "polished-fluff": "vendor_fluff.json",
      }
      This map is the authoritative source for output filenames. generate_samples.py imports and
      uses it; test_sample_fixtures.py defines an identical local copy for test path resolution.
      Do NOT derive filenames from persona.replace("-","_") — the short names (vendor_thorough.json
      not vendor_thorough_but_pricey.json) are the committed names that fixture tests expect.
    - MESS_SPECS: dict[str, list[MessSpecItem]] with three keys: "thorough-but-pricey",
      "cheap-but-incomplete", "polished-fluff". Each value is a list of MessSpecItem instances
      (not list[dict] — use MessSpecItem(line_item=..., issue_type=..., instruction=...)).
      Import MessSpecItem from schemas.domain.
      Thorough-but-pricey: bundle pricing across line items (e.g. "strategy + TVC bundled as one fee"),
      over-scope (add unrequested services), high-anchored pricing per item.
      Cheap-but-incomplete: omit price for at least 3 line items (instruction: "Leave the fee TBD"),
      vague timelines ("Q3" not weeks), leave compliance section blank.
      Polished-fluff: give two different timelines for the same item in different sections (internal
      conflict), use "best-in-class" / "award-winning" without evidence (marketing fluff),
      vague compliance claim ("we follow all relevant regulations").
    - FORMAT_LABELS: dict[str, str] mapping personas to format labels (thorough→"tabular_proposal",
      cheap→"letter_email", fluff→"deck_bullets").
    - generate_vendor_response(rfq_text: str, persona: str, mess_spec: list[MessSpecItem]) -> VendorResponse:
      load("vendor-gen"), ChatPromptTemplate with {rfq_text}, {persona}, {mess_spec} inputs
      (convert mess_spec to dicts via [m.model_dump() for m in mess_spec] before passing to template),
      get_llm("reasoning").invoke(), result.content as raw_text,
      VendorResponse(vendor_name=persona, persona=persona, mess_spec=mess_spec,
      source_id=f"vendor_{FIXTURE_FILENAMES[persona].removesuffix('.json')}",
      format_label=FORMAT_LABELS[persona], raw_text=raw_text).

    Write services/ai/scripts/generate_samples.py:
    - Module docstring + usage comment "uv run python scripts/generate_samples.py".
      Document: "Requires OPENAI_API_KEY in .env. CI validates committed fixtures only (D-13);
      this script is a developer tool for regenerating them."
    - Imports: repo_root from scripts.codegen, generate_rfq + render_rfq_md from agents.rfq_gen,
      generate_vendor_response + MESS_SPECS + FIXTURE_FILENAMES from agents.vendor_gen,
      json, Path, schemas.domain.

    - API PRE-CHECK before main() content: define _check_api_access() -> None that calls
      get_llm("reasoning").invoke("ping") with a minimal prompt. Catch any exception (AuthenticationError,
      APIConnectionError, or generic Exception). On failure, print a clear message:
      "ERROR: Cannot reach OpenAI API. Check OPENAI_API_KEY in .env. Details: {e}" and sys.exit(1).
      Do NOT log the API key value in the error message. Call _check_api_access() at the start of main().

    - main() function:
      1. _check_api_access()
      2. rfq = generate_rfq()
      3. data_dir = repo_root() / "data"; data_dir.mkdir(exist_ok=True)
      4. (data_dir/"rfq.json").write_text(rfq.model_dump_json(indent=2))
      5. rfq_md = render_rfq_md(rfq); (data_dir/"rfq.md").write_text(rfq_md)
      6. For each persona in list(FIXTURE_FILENAMES.keys()):
         vendor = generate_vendor_response(rfq_md, persona, MESS_SPECS[persona])
         filename = FIXTURE_FILENAMES[persona]
         (data_dir / filename).write_text(vendor.model_dump_json(indent=2))
         print(f"  wrote {filename}")
         Note: filenames come from FIXTURE_FILENAMES — never derived via persona.replace("-","_").
      7. Print "Done — fixtures written to data/"
    - if __name__ == "__main__": main()

    Run the script to generate the fixtures:
    cd services/ai && uv run python scripts/generate_samples.py
    This makes live LLM calls (OpenAI API key required). The pre-check fails fast if the key
    is missing. The script must succeed and produce the 5 data/ files.

    After the fixtures are written, verify test_sample_fixtures.py goes GREEN:
    cd services/ai && uv run pytest tests/test_sample_fixtures.py -x -q

    MESSINESS ITERATION CAP: If any messiness assertion fails (test_cheap_incomplete_has_missing_price
    or test_polished_fluff_has_conflict), iterate: adjust the mess spec instruction in MESS_SPECS
    or add a stronger instruction in the vendor-gen prompt, then regenerate and re-test.
    Do NOT relax the test assertion to make it pass — the test must pass because the data is
    genuinely messy (D-13). Cap at 3 iteration attempts. If after 3 attempts a test still fails,
    stop iterating and document the failure as a "known limitation" in the plan SUMMARY and in
    docs/prompts/data-generation.md §Prompt Failure Example — note which mess spec instruction
    the model consistently ignores and what workaround is needed. This is a valid outcome that
    demonstrates real AI reliability challenges; it does not block shipping the committed fixtures
    (manually edit the fixture text to satisfy the assertion if the LLM cannot, but document the
    limitation honestly).
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_sample_fixtures.py -x -q</automated>
  </verify>
  <done>
    All 5 DATA-01/02/03 tests in test_sample_fixtures.py pass GREEN.
    data/rfq.json exists with 8 line_items. data/vendor_thorough.json, vendor_cheap.json,
    vendor_fluff.json exist (using FIXTURE_FILENAMES short names, not persona.replace("-","_")).
    All three vendor files have non-empty raw_text. test_cheap_incomplete_has_missing_price PASS.
    test_polished_fluff_has_conflict PASS. test_vendor_fixture_messiness PASS.
    MESS_SPECS uses MessSpecItem instances (not list[dict]).
  </done>
</task>

<task type="auto">
  <name>Task 2: Live-regen API endpoints with rfq_text caching (DATA-04) + PROMPT-04 documentation</name>
  <files>services/ai/api/app.py, docs/prompts/data-generation.md</files>
  <read_first>
    - services/ai/api/app.py — full file; existing lifespan + GET /stream/demo pattern to follow
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §api/app.py (new synchronous GET endpoint pattern, import additions)
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Architecture Patterns (Live-regen API path diagram) and §Open Questions Q3 (GET endpoints, synchronous, no SSE)
    - .planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md §D-14 (PROMPT-04 failure example — real failure preferred, honestly labeled if not)
    - CLAUDE.md §7 (Prompt Pack docs: what/why/how-handles-unreliable) and §14 (docs/ folder structure, docs/prompts/)
    - docs/assignment.md §22 (prompt quality rubric criteria — what the docs must prove)
    - services/ai/agents/rfq_gen.py + vendor_gen.py (just written — read the actual functions to document them accurately)
    - services/ai/prompts/rfq-gen.v1.md, vendor-gen.v1.md, messy-data-gen.v1.md (the actual prompts — docs must match reality)
    - .planning/phases/02-grounding-gate-messy-data/02-03-SUMMARY.md — check whether a real authoring failure was observed (D-14)
  </read_first>
  <action>
    Modify services/ai/api/app.py to add two synchronous endpoints for live data regeneration (DATA-04).
    Keep the existing lifespan and /stream/demo route unchanged.

    Add to imports (after "from agents._demo import demo_graph"):
    from agents.rfq_gen import generate_rfq, render_rfq_md
    from agents.vendor_gen import generate_vendor_response, MESS_SPECS

    Add two new route handlers:

    GET /data/rfq:
    @app.get("/data/rfq")
    async def get_rfq() -> dict:
        """Live-regenerate the marketing-services RFQ via rfq-gen prompt (DATA-04).
        Returns: JSON-serializable RFQ dict."""
        rfq = generate_rfq()
        return rfq.model_dump(mode="json")

    POST /data/vendor-gen:
    The endpoint accepts an optional rfq_text body parameter so callers can pass in a previously-
    generated RFQ (ensuring all vendors respond to the SAME RFQ for a valid comparison). If rfq_text
    is omitted, a fresh RFQ is generated inline.

    Define a request body model (inline in app.py):
      class VendorGenRequest(BaseModel):
          persona: str
          rfq_text: str | None = None

    @app.post("/data/vendor-gen")
    async def post_vendor_gen(req: VendorGenRequest) -> dict:
        """Live-regenerate a vendor response for the given persona (DATA-04).
        req.persona: one of "thorough-but-pricey", "cheap-but-incomplete", "polished-fluff".
        req.rfq_text: optional RFQ Markdown text; if omitted, a fresh RFQ is generated.
        Accepts rfq_text to ensure all vendors in a comparison see the same RFQ.
        Returns: JSON-serializable VendorResponse dict."""
        if req.persona not in MESS_SPECS:
            from fastapi import HTTPException
            raise HTTPException(status_code=400, detail=f"Unknown persona: {req.persona}. Must be one of {list(MESS_SPECS)}")
        rfq_text = req.rfq_text
        if rfq_text is None:
            rfq = generate_rfq()
            rfq_text = render_rfq_md(rfq)
        vendor = generate_vendor_response(rfq_text, req.persona, MESS_SPECS[req.persona])
        return vendor.model_dump(mode="json")

    Note: use model_dump(mode="json") throughout — this is the correct pydantic v2 pattern
    for JSON-serializable dicts (avoids json.loads(model_dump_json()) round-trip).

    Write docs/prompts/data-generation.md — PROMPT-04 documentation:
    This is the Prompt Pack documentation for the three data-generation prompts. CLAUDE.md §7 and
    §14 define what it must contain. Structure:

    # Data Generation Prompts — Prompt Pack Documentation

    ## rfq-gen (version 1)
    - **What it does:** One-sentence description.
    - **Why it is structured this way:** Explain the persona framing (procurement manager), why
      8 named line items are specified, why compliance clauses are explicit, why the anti-hallucination
      instruction is needed.
    - **How it handles unreliable / missing information:** RFQ is our own artifact — no missing
      info is expected. But explain why anti-hallucination rules prevent the model from inventing
      context not in the prompt.
    - **Model tier:** reasoning (gpt-5.4) — explain why (structured output + domain richness).

    ## vendor-gen (version 1)
    - **What it does:** One-sentence description.
    - **Why it is structured this way:** Explain D-08 (one-pass generation, not clean-then-vandalize),
      why mess_spec instructions are passed as structured input (D-09 — deterministic, testable),
      why format diversity matters (letter vs. table vs. deck) for extraction coverage.
    - **How it handles unreliable / missing information:** The prompt INSTRUCTS the model to produce
      unreliable info deliberately — this is by design. Explain the paradox: we need the model to
      produce "bad" output (missing fields, conflicts) on command, which requires explicit instruction
      (not hoping for organic badness).
    - **Model tier:** reasoning.

    ## messy-data-gen (version 1)
    - **What it does:** Serves as the issue-type taxonomy reference embedded in vendor-gen.
    - **Why it is structured this way:** Explain D-08 — dedicated taxonomy prompt keeps the Prompt
      Pack slot meaningful and documents the issue types for reviewers, without adding an extra LLM call.
    - **Model tier:** cheap (taxonomy reference, not a generation call).

    ## Prompt Failure Example + Fix (PROMPT-04 — D-14)
    Read the 02-03-SUMMARY.md to determine whether a real authoring failure was observed.

    IF a real failure was observed: document it as-is.
    - **What went wrong:** Description of the actual bad output.
    - **Why it happened:** Root cause.
    - **Fix applied:** What instruction was added/changed.
    - **Documented:** "Real failure observed during authoring on [date]."

    IF no real failure was observed during authoring: document an anticipated failure-mode instead.
    Label it explicitly: "Anticipated failure-mode (no real failure occurred during authoring)."
    - **Anticipated failure:** E.g., vendor-gen may include a price despite "omit fee" instruction
      (the model over-polishes against the mess spec). This is a known LLM reliability risk for
      instruction-following on deliberate omissions.
    - **Mitigation already in prompt:** The vendor-gen prompt includes an explicit instruction
      "do not clean up the issues in the mess spec" and specifies the forbidden behavior by example.
    - **Why this matters:** If vendors consistently over-polish, the extraction agent sees fewer
      missing/unclear flags, reducing the rubric score for absence-is-first-class.
    - **What would trigger a v2 prompt:** Persistent over-polishing despite the instruction above.
    Do NOT invent a failure that contradicts the actual prompt bodies or fixture contents.

    In both cases, add a Versioning/Eval Notes section:
    - **Current version:** v1; what would trigger a bump to v2.
    - **Eval criteria:** How to tell the prompt is working (messiness tests pass, fixtures have
      the right flag types, extraction agent sees missing/unclear/conflicting fields).
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_prompt_registry.py -x -q && python -c "from api.app import app; routes=[r.path for r in app.routes]; print(routes); assert '/data/rfq' in routes; assert '/data/vendor-gen' in routes; print('routes OK')"</automated>
  </verify>
  <done>
    api/app.py has GET /data/rfq and POST /data/vendor-gen routes.
    POST /data/vendor-gen accepts a JSON body with persona (required) and rfq_text (optional);
    uses rfq_text if provided, generates a fresh RFQ only when omitted.
    Both endpoints use model_dump(mode="json") — no json.loads(model_dump_json()) pattern.
    docs/prompts/data-generation.md exists with sections for all three prompts (what/why/how)
    plus ≥1 failure example + fix + versioning note (PROMPT-04 D-14). Failure section is
    labeled honestly: "Real failure observed" OR "Anticipated failure-mode (no real failure occurred)".
    test_prompt_registry.py still PASS (no frontmatter broken).
    Full suite: uv run pytest -x -q → all tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full test suite GREEN + VALIDATION.md updated</name>
  <files>services/ai/tests/test_sample_fixtures.py, .planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md — current validation contract (all test status rows)
    - services/ai/tests/test_sample_fixtures.py — fixture tests written in plan 02-01; may need minor updates to match actual VendorResponse field name (raw_text) after plan 02-03 fleshed the schema
    - services/ai/tests/test_grounding_gate.py — all gate tests (from plan 02-02)
  </read_first>
  <action>
    Run the full test suite and confirm all tests are GREEN:
    cd services/ai && uv run pytest -x -q

    Expected green tests:
    - test_grounding_gate.py (9 original + new normalize/source-id/non-mutation tests from 02-02 — all EXTRACT-04 and correctness)
    - test_sample_fixtures.py (5 tests — DATA-01/02/03)
    - test_codegen_drift.py (DATA-04 codegen — from plan 02-03)
    - test_prompt_registry.py (PROMPT-04 load path)
    - test_field_envelope.py, test_llm_factory.py, test_sse_demo.py (Phase 1 regressions)

    If test_sample_fixtures.py fails due to a schema field name mismatch (e.g., VendorResponse
    gained a new field in plan 02-03 that the fixture test doesn't reference), fix the test — not
    the schema. The schema is the contract (D-12).

    If any messiness assertion is failing because the test pattern doesn't match the actual fixture
    prose (e.g., looking for "TBD" but the model wrote "price to be confirmed"), update the test
    assertion to match the actual fixture text while preserving the semantic intent: the assertion
    must still prove messiness, not become trivially-pass.

    After all tests pass, update .planning/phases/02-grounding-gate-messy-data/02-VALIDATION.md:
    - Set nyquist_compliant: true in frontmatter
    - Set wave_0_complete: true in frontmatter
    - Update each test row's Status column from ⬜ pending to ✅ green
    - Set "Approval: complete" at the bottom
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    "uv run pytest -x -q" exits 0 with all tests passing (0 failures, 0 errors).
    VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true.
    All requirement IDs in VALIDATION.md have status ✅ green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /data/vendor-gen | persona and rfq_text parameters arrive in JSON body; persona validated against MESS_SPECS keys before use; rfq_text is a plain string passed to the LLM — no filesystem or code-execution surface |
| LLM output → VendorResponse.raw_text | Model output stored as raw text — never re-injected into another LLM call in P2 |
| data/ fixtures → test assertions | Committed fixture files are read-only at test time; no external modification surface |
| OPENAI_API_KEY → generate_samples.py | Pre-check verifies key is accessible; key is never logged in error output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | POST /data/vendor-gen — persona parameter | mitigate | Validate persona against MESS_SPECS dict keys before use; return HTTP 400 for unknown values; persona is a dict key, never used as a path or LLM instruction fragment directly |
| T-02-12 | Information Disclosure | OPENAI_API_KEY in environment at generation time | mitigate | Key is in .env (gitignored); factory.py never logs it; generate_samples.py pre-check error message must not print the key value (enforced in action above) |
| T-02-13 | Spoofing | Model-generated vendor text asserting ungrounded claims | accept | Vendor text is the TEST DATA for the grounding gate — it's supposed to contain fabricated claims; the gate (plan 02-02) validates evidence spans before display; data generation itself is not a trust boundary |
| T-02-14 | Denial of Service | /data/rfq or /data/vendor-gen generating slow responses | accept | Synchronous LLM calls are intentional for this prototype (RESEARCH.md Open Q3); no production SLA requirement; acceptable for 5-day prototype scope |
| T-02-15 | Tampering | rfq_text parameter injecting malicious content into vendor-gen LLM call | accept | rfq_text is caller-supplied context to an LLM — for this prototype the caller is the developer or the app's own /data/rfq endpoint; no external untrusted party has access; accept for prototype scope |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan; all dependencies already present |
</threat_model>

<verification>
After all three tasks complete:
- cd services/ai && uv run pytest -x -q → 0 failures, all suites GREEN
- test_grounding_gate.py: all PASS (EXTRACT-04 gate fully verified, including new normalize/source-id/non-mutation tests)
- test_sample_fixtures.py: 5 PASS (DATA-01/02/03 fixtures verified)
- test_codegen_drift.py: PASS (DATA-04 schema contract holds)
- test_prompt_registry.py: PASS (PROMPT-04 load path holds)
- data/rfq.json: exists, RFQ.model_validate_json() succeeds, len(rfq.line_items) == 8
- data/vendor_*.json: vendor_thorough.json, vendor_cheap.json, vendor_fluff.json (FIXTURE_FILENAMES short names — NOT vendor_thorough_but_pricey.json etc.)
- GET /data/rfq route: python -c "from api.app import app; assert any(r.path == '/data/rfq' for r in app.routes)"
- POST /data/vendor-gen route: python -c "from api.app import app; assert any(r.path == '/data/vendor-gen' for r in app.routes)"
- POST /data/vendor-gen accepts rfq_text in request body: confirm VendorGenRequest model has rfq_text: str | None
- docs/prompts/data-generation.md: exists and contains "failure" section (PROMPT-04 D-14) with honest labeling
- VALIDATION.md: nyquist_compliant: true
- Verify FIXTURE_FILENAMES in vendor_gen.py matches FIXTURE_FILENAMES in test_sample_fixtures.py exactly
</verification>

<success_criteria>
- Five data/ fixture files committed: rfq.json, rfq.md, vendor_thorough.json, vendor_cheap.json, vendor_fluff.json
- FIXTURE_FILENAMES exported from vendor_gen.py with authoritative short names; generate_samples.py uses it (not persona.replace)
- API pre-check in generate_samples.py fails fast without logging the key if OPENAI_API_KEY is missing
- Messiness iteration capped at 3 attempts; iteration failure documented honestly if it occurs
- All phase 2 automated tests GREEN (16+ total across grounding gate + fixtures + codegen + prompt registry)
- GET /data/rfq and POST /data/vendor-gen endpoints wired in api/app.py; POST accepts optional rfq_text
- Both endpoints use model_dump(mode="json") — not json.loads(model_dump_json())
- docs/prompts/data-generation.md exists with per-prompt docs + ≥1 failure example + fix + versioning note; D-14 labeled honestly
- VALIDATION.md: nyquist_compliant: true, wave_0_complete: true
- Full suite uv run pytest -x -q exits 0
</success_criteria>

<output>
Create .planning/phases/02-grounding-gate-messy-data/02-04-SUMMARY.md when done.
</output>
