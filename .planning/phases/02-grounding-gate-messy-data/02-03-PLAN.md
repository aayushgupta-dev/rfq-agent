---
phase: 02-grounding-gate-messy-data
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - services/ai/schemas/domain.py
  - packages/shared-types/index.d.ts
  - services/ai/prompts/rfq-gen.v1.md
  - services/ai/prompts/vendor-gen.v1.md
  - services/ai/prompts/messy-data-gen.v1.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - PROMPT-04

must_haves:
  truths:
    - "RFQ schema has real fields: title, client_name, issue_date, response_deadline, scope_summary, line_items (list[LineItem] with ≥8 field minimum), commercial_expectations, questionnaire, compliance_requirements (D-11: RFQ = structured pydantic + rendered Markdown, plain types not Field[T] — our own clean artifact, never grounded)"
    - "VendorResponse schema carries raw_text + provenance: vendor_name, persona, mess_spec, source_id, format_label — NOT pre-extracted Field[T] fields (D-12: raw messy text + provenance is the extraction input we ground against)"
    - "pydantic2ts drift-check test passes after schema changes (TS types regenerated)"
    - "rfq-gen, vendor-gen, and messy-data-gen prompts load from registry without error and have non-empty bodies"
    - "All three prompts have required frontmatter: id, version, intent, model_tier, failure_handling"
  artifacts:
    - path: "services/ai/schemas/domain.py"
      provides: "Fleshed RFQ + VendorResponse; ExtractionResult/ComparisonResult remain P3/P4 stubs"
      contains: "class LineItem"
    - path: "packages/shared-types/index.d.ts"
      provides: "Regenerated TS contract from updated schemas"
    - path: "services/ai/prompts/rfq-gen.v1.md"
      provides: "Full RFQ generation prompt (not a TODO stub)"
    - path: "services/ai/prompts/vendor-gen.v1.md"
      provides: "Full vendor response generation prompt with mess spec instructions"
    - path: "services/ai/prompts/messy-data-gen.v1.md"
      provides: "Taxonomy/reference prompt for issue types (D-08)"
  key_links:
    - from: "services/ai/agents/rfq_gen.py (plan 02-04)"
      to: "services/ai/schemas/domain.py"
      via: "from schemas.domain import RFQ, LineItem"
      pattern: "class RFQ"
    - from: "services/ai/agents/vendor_gen.py (plan 02-04)"
      to: "services/ai/schemas/domain.py"
      via: "from schemas.domain import VendorResponse"
      pattern: "class VendorResponse"
    - from: "packages/shared-types/index.d.ts"
      to: "services/ai/schemas/domain.py"
      via: "pydantic2ts codegen (scripts/codegen.py)"
      pattern: "export interface RFQ"
---

<objective>
Flesh out the RFQ and VendorResponse pydantic schemas (currently P1 stubs), regenerate the TS
contract, and author the three data-generation prompts (rfq-gen, vendor-gen, messy-data-gen)
so the generation agents in plan 02-04 have both a working schema and a quality prompt to call.

Purpose: The schema is the contract — changing domain.py propagates through shared-types to the
Phase 5 UI. The prompts are the highest-weighted deliverable (30% of grade); these three are
the data-generation half of the Prompt Pack and must be authored with care, not left as TODO stubs.
D-11: RFQ uses plain Python types (not Field[T] — it's our own artifact, not a grounded extraction).
D-12: VendorResponse carries raw_text + provenance only (no pre-extracted fields).

Output:
- domain.py with LineItem + fleshed RFQ + fleshed VendorResponse (ExtractionResult/ComparisonResult unchanged)
- packages/shared-types/index.d.ts regenerated
- Three prompt files authored (replacing TODO stubs)
</objective>

<execution_context>
@/Users/aayush/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aayush/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aayush/projects/aerchain/.planning/PROJECT.md
@/Users/aayush/projects/aerchain/.planning/ROADMAP.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md
@/Users/aayush/projects/aerchain/.planning/phases/02-grounding-gate-messy-data/02-01-SUMMARY.md

<interfaces>
From services/ai/schemas/envelope.py (DO NOT use Field[T] in RFQ/VendorResponse — they are our own artifacts):
class Field(BaseModel, Generic[T]): ...  # only used in ExtractionResult/ComparisonResult (P3/P4)

From services/ai/scripts/codegen.py:
def repo_root() -> Path: ...   # already in use; codegen.py runs via uv run python scripts/codegen.py

From services/ai/tests/test_codegen_drift.py (the test that must pass after schema change):
# test_codegen_drift.py runs codegen and diffs against committed index.d.ts
# Run: cd services/ai && uv run pytest tests/test_codegen_drift.py -x -q

From services/ai/prompts/registry.py:
def load(prompt_id: str) -> frontmatter.Post: ...
# Required frontmatter keys asserted by test_prompt_registry.py:
# id (must match filename stem), version, intent, model_tier ("reasoning" or "cheap"), failure_handling

From services/ai/tests/test_prompt_registry.py:
# Asserts: all 7 prompt stubs load without error; id matches filename stem; required keys present
# Run: cd services/ai && uv run pytest tests/test_prompt_registry.py -x -q
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out RFQ + VendorResponse schemas in domain.py + regenerate TS contract</name>
  <files>services/ai/schemas/domain.py, packages/shared-types/index.d.ts</files>
  <read_first>
    - services/ai/schemas/domain.py — current stub (the P1 placeholder with title/budget_total stub fields)
    - services/ai/schemas/envelope.py — ConfigDict(extra="forbid"), pydantic_Field alias, from __future__ import annotations patterns
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Pattern 7 (RFQ Schema Design — LineItem sketch, constraints, no tuple type)
    - .planning/phases/02-grounding-gate-messy-data/02-RESEARCH.md §Common Pitfalls §Pitfall 4 (pydantic2ts drift) and §Pitfall 6 (RFQ structured output constraints — no tuple, no Decimal, use list[int] for ranges)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §domain.py (ConfigDict pattern, pydantic_Field alias, ponytail comments, codegen impact note)
    - packages/shared-types/index.d.ts — current content (to understand what will change)
  </read_first>
  <action>
    Modify services/ai/schemas/domain.py to replace the RFQ and VendorResponse stubs with real fields.
    Keep ExtractionResult and ComparisonResult exactly as they are (P3/P4 stubs — do not touch them).
    Preserve and update the P1 module docstring to reflect what changed.

    New LineItem model (new class, before RFQ):
    - ConfigDict(extra="forbid"), from __future__ import annotations at top
    - Fields: id: str, name: str, description: str, deliverables: list[str],
      timeline_weeks: int | None = None, budget_range_usd: list[int] | None = None
      (D-12/Pitfall 6: list[int] with 2-element convention, not tuple[int,int] — OpenAI structured output constraint)

    Fleshed RFQ model:
    - Remove the old stub fields (title: Field[str], budget_total: Field[Decimal])
    - Replace with plain Python types (D-11 — RFQ is our own artifact, NOT grounded extraction):
      title: str, client_name: str, issue_date: str, response_deadline: str,
      scope_summary: str, line_items: list[LineItem],
      commercial_expectations: str,
      questionnaire: list[str] = pydantic_Field(default_factory=list),
      compliance_requirements: list[str] = pydantic_Field(default_factory=list),
      budget_total_usd: int | None = None
    - ConfigDict(extra="forbid")
    - Update docstring to remove "stub" language and reference D-11

    Fleshed VendorResponse model:
    - Remove stub fields (vendor_name: Field[str], proposed_total: Field[Decimal], response_completeness_score: Field[int])
    - Replace with raw text + provenance (D-12):
      vendor_name: str, persona: str,
      mess_spec: list[dict] = pydantic_Field(default_factory=list),
      source_id: str, format_label: str, raw_text: str
    - ConfigDict(extra="forbid")
    - Update docstring to reference D-12 ("raw text + provenance — NOT pre-extracted fields")

    Remove the "from decimal import Decimal" import if it is no longer needed after removing
    Field[Decimal] stubs. Keep "from schemas.envelope import Field" only if ExtractionResult/
    ComparisonResult still use it.

    After saving domain.py, run codegen:
    cd services/ai && uv run python scripts/codegen.py
    This writes packages/shared-types/index.d.ts. Commit that file alongside domain.py.
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_codegen_drift.py -x -q</automated>
  </verify>
  <done>
    test_codegen_drift.py passes GREEN. domain.py has LineItem + fleshed RFQ + fleshed VendorResponse.
    RFQ has no Field[T] wrappers (plain str/int/list types). VendorResponse has raw_text: str and
    source_id: str. packages/shared-types/index.d.ts contains "export interface RFQ" and
    "export interface VendorResponse" with the new field shapes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author rfq-gen, vendor-gen, and messy-data-gen prompts (PROMPT-04 foundation)</name>
  <files>services/ai/prompts/rfq-gen.v1.md, services/ai/prompts/vendor-gen.v1.md, services/ai/prompts/messy-data-gen.v1.md</files>
  <read_first>
    - services/ai/prompts/rfq-gen.v1.md — current stub (frontmatter is correct; body is a TODO)
    - services/ai/prompts/vendor-gen.v1.md — current stub
    - services/ai/prompts/messy-data-gen.v1.md — current stub
    - .planning/phases/02-grounding-gate-messy-data/02-CONTEXT.md §decisions D-08 through D-14 (one-pass generation, mess spec taxonomy, 3 personas, PROMPT-04 failure capture)
    - .planning/phases/02-grounding-gate-messy-data/02-PATTERNS.md §Prompt files (frontmatter schema, required keys, body style)
    - docs/assignment.md §22 rubric (Prompt quality 30%), §24 anti-patterns (no clean samples, no heavy normalization, never ignore missing/contradictory info)
    - CLAUDE.md §1 (evidence over assertion, absence first-class), §7 (Prompt Pack traceability — what/why/how-it-handles-unreliable)
    - services/ai/schemas/domain.py (the fleshed RFQ schema — the rfq-gen prompt must match these field names)
  </read_first>
  <action>
    Replace the TODO body of all three prompt files with real content. Preserve the frontmatter
    exactly (the registry test asserts all four frontmatter keys). Add a real body below the "---".

    rfq-gen.v1.md — System prompt for RFQ generation (DATA-01):
    The prompt generates a JSON-structured RFQ via with_structured_output(RFQ) so it must
    produce output that maps to the LineItem/RFQ schema. Instructions:
    - Establish persona: "You are a senior procurement manager at a mid-size consumer goods brand
      running a competitive marketing-services bid."
    - Specify scope: 8 line items by name (strategy & creative, TVC development, TVC production,
      social organic, paid media planning, paid media buying, kids advertising & claims compliance,
      launch program management).
    - Realism bar: specific client_name (not "ACME"), real campaign context (product launch or
      rebranding), concrete dates and timelines (not "TBD"), named deliverables per line item,
      explicit budget range, real compliance clauses (COPPA for kids advertising, regulatory
      claim substantiation, media spend transparency).
    - Questionnaire: ≥8 questions covering pricing model, team composition, previous clients,
      exclusivity, timeline risk, compliance accreditations.
    - Anti-hallucination instruction: "Do not invent technology vendors, award shows, or client
      names not established in this prompt."
    - Output instruction: "Respond ONLY with the JSON object matching the RFQ schema. No prose."

    vendor-gen.v1.md — System prompt for vendor response generation (DATA-02/D-08):
    The prompt generates raw messy vendor prose via plain .invoke() (not structured output).
    Instructions:
    - Persona context: passed as input parameter {persona} (thorough-but-pricey / cheap-but-incomplete / polished-fluff)
    - Mess spec: passed as input parameter {mess_spec} (list of {line_item, issue_type, instruction})
    - Embedded messy-data-gen taxonomy (D-08): include the issue-type taxonomy inline so the model
      knows what each issue type means. Reference the taxonomy: bundled_scope, missing_line_item,
      vague_timeline, unclear_tax, partial_scope, internal_conflict, weak_compliance, marketing_fluff.
    - Format diversity: persona 1 writes a formal letter/email; persona 2 uses a tabular proposal;
      persona 3 writes deck-style bullet points.
    - Realism instruction: "Respond exactly as the persona would — do not clean up the issues in
      the mess spec. If the spec says omit pricing for a line item, omit it. If it says give
      conflicting timelines, give two different timelines in different sections."
    - Anti-hallucination: "Do not mention specific clients, awards, or technologies unless the RFQ
      established them."
    - Output instruction: "Respond with raw vendor proposal text only. No JSON. No commentary."

    messy-data-gen.v1.md — Issue-type taxonomy / reference prompt (D-08):
    This prompt is the taxonomy reference embedded by vendor-gen, not a standalone generation call.
    Body should be the definitive issue-type taxonomy:
    - Define each of 8 issue types with: name, description, example in vendor text, why it causes
      problems for buyers.
    - Types: bundled_scope, missing_line_item, vague_timeline, unclear_tax_and_currency,
      partial_scope, internal_conflict, weak_compliance_claim, marketing_fluff.
    - Include a "how this stresses the extraction agent" column (educates PROMPT-04 reviewers).
    - Update the frontmatter intent to: "Issue-type taxonomy used as a reference embedded in
      vendor-gen; defines the 8 mess types that stress extraction and comparison agents."
    - Update model_tier to "cheap" (this is a reference document, not a model call).

    Keep all existing frontmatter keys intact. Do not change id, version, or the failure_handling
    field (the registry test checks all four keys exist and are non-empty strings).
  </action>
  <verify>
    <automated>cd /Users/aayush/projects/aerchain/services/ai && uv run pytest tests/test_prompt_registry.py -x -q</automated>
  </verify>
  <done>
    test_prompt_registry.py passes GREEN (all 7 prompts load, all have required frontmatter).
    rfq-gen.v1.md body is not a TODO stub — it contains a real system prompt with persona,
    8 line item names, compliance clauses, and anti-hallucination instructions.
    vendor-gen.v1.md body contains mess spec instructions and format diversity per persona.
    messy-data-gen.v1.md defines 8 issue types with descriptions and examples.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt file → registry.load | Prompt files are author-controlled; registry validates id/frontmatter in code |
| pydantic schema → codegen | domain.py fields define the TS contract; codegen script runs in the repo, not from external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | prompt file bodies | accept | Prompts are committed to source control; they contain no secrets, no API keys; accept |
| T-02-09 | Tampering | domain.py schema change propagating incorrectly to shared-types | mitigate | test_codegen_drift.py verifies TS output matches regenerated output after every schema change (PLAT-02 control) |
| T-02-10 | Spoofing | vendor-gen prompt instructing model to hallucinate commercial claims | mitigate | Prompt includes explicit anti-hallucination instruction: "Do not invent clients, awards, or technologies not in RFQ." D-09 mess specs are hand-authored so injected issues are known and detectable in tests |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all dependencies already in pyproject.toml |
</threat_model>

<verification>
After both tasks complete:
- cd services/ai && uv run pytest tests/test_codegen_drift.py -x -q → PASS
- cd services/ai && uv run pytest tests/test_prompt_registry.py -x -q → PASS (all 7 prompts)
- python -c "from schemas.domain import RFQ, VendorResponse, LineItem; print(RFQ.model_fields.keys())" → includes line_items, questionnaire, compliance_requirements
- python -c "from schemas.domain import VendorResponse; print(VendorResponse.model_fields.keys())" → includes raw_text, source_id, persona, mess_spec
- grep -c "TODO P2" services/ai/prompts/rfq-gen.v1.md → 0 (TODO body replaced)
- grep -c "TODO P2" services/ai/prompts/vendor-gen.v1.md → 0
- grep -c "TODO P2" services/ai/prompts/messy-data-gen.v1.md → 0
- Full suite: cd services/ai && uv run pytest -x -q → all existing tests pass
</verification>

<success_criteria>
- domain.py: LineItem class exists; RFQ has line_items: list[LineItem], questionnaire, compliance_requirements; VendorResponse has raw_text: str, source_id: str, persona: str
- No Field[T] wrappers in RFQ or VendorResponse (plain types per D-11/D-12)
- packages/shared-types/index.d.ts regenerated; test_codegen_drift.py PASS
- All three prompts have non-TODO bodies; test_prompt_registry.py PASS
- ExtractionResult and ComparisonResult unchanged (P3/P4 stubs preserved)
</success_criteria>

<output>
Create .planning/phases/02-grounding-gate-messy-data/02-03-SUMMARY.md when done.
</output>
