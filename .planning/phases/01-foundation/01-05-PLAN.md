---
phase: 01-foundation
plan: "05"
type: execute
wave: 3
depends_on: [01-01-monorepo-scaffold]
files_modified:
  - apps/web/package.json
  - apps/web/next.config.mjs
  - apps/web/tsconfig.json
  - apps/web/eslint.config.mjs
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/lib/utils.ts
  - apps/web/components/ui/button.tsx
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - pnpm-lock.yaml
autonomous: false
requirements: [FOUND-UPGRADE]
user_setup: []

must_haves:
  truths:
    - "apps/web runs on Next.js 16.2.9 + React 19.2.7 with the existing flat ESLint config still loading"
    - "Tailwind v4 (CSS-first, no config file) is wired and processes through PostCSS"
    - "shadcn/ui substrate exists: components.json, cn() helper, oklch token system in globals.css"
    - "A single Tailwind-styled shadcn Button renders on the existing page as the end-to-end proof"
    - "The full backend test suite and all web quality gates (tsc, eslint, build, prettier, ruff) stay green"
  artifacts:
    - path: "apps/web/postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin wiring"
      contains: "@tailwindcss/postcss"
    - path: "apps/web/components.json"
      provides: "shadcn/ui project config (new-york, neutral, rsc, @/* aliases)"
    - path: "apps/web/lib/utils.ts"
      provides: "cn() class-merge helper"
      exports: ["cn"]
    - path: "apps/web/components/ui/button.tsx"
      provides: "the one proof shadcn component"
    - path: "apps/web/app/globals.css"
      provides: "Tailwind v4 import + shadcn oklch token system"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/globals.css"
      via: "import"
      pattern: "import.*globals\\.css"
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/ui/button.tsx"
      via: "import"
      pattern: "from \"@/components/ui/button\""
---

<objective>
Upgrade `apps/web` from Next.js 15 / React 19.0 to Next.js 16.2.9 / React 19.2.7, and add the
UI substrate (Tailwind CSS v4 CSS-first + shadcn/ui init) plus exactly ONE proof component
(`Button`) rendered on the existing page.

Purpose: Get the frontend onto the current framework + the agreed UI toolchain (CLAUDE.md §5)
BEFORE the real buyer screens land in Phase 5 — so screen work starts on a proven, modern substrate
instead of a 15.x shell. The Button render is the integration proof, mirroring how 01-01 imported
`FlagStatus` to prove the workspace link.

Output: Pinned-version `apps/web` on Next 16 + React 19.2, Tailwind v4 wired, shadcn substrate
(components.json, cn(), token system), and one visibly-styled Button on the page. No buyer screens,
no other shadcn components, no theme customization — those are Phase 5.

Scope is LOCKED: substrate + one proof component only. Do NOT add any shadcn component besides
Button. Do NOT build any layout, screen, dark-mode toggle UI, or theme work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-05-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Current apps/web state the executor is upgrading. Read these; do not re-discover. -->

apps/web/package.json (BEFORE — versions are ranges, must become EXACT pins):
- dependencies: "next": "^15.3.3", "react": "^19.0.0", "react-dom": "^19.0.0",
  "@aerchain/shared-types": "workspace:*"
- devDependencies: "eslint-config-next": "^15.3.3", "@types/react": "^19.0.0",
  "@types/react-dom": "^19.0.0", "eslint": "^9.0.0", "@eslint/eslintrc": "^3.3.5",
  "@types/node": "^22.0.0", "typescript": "^5.7.0"
- scripts.lint is currently "next lint" (REMOVED in Next 16 — must become "eslint .")

apps/web/tsconfig.json — ALREADY has compilerOptions.paths {"@/*":["./*"]} (shadcn init
prerequisite is satisfied; the plan still VERIFIES it before running init).

apps/web/next.config.mjs — has transpilePackages: ["@aerchain/shared-types"] (MUST survive the
upgrade; Turbopack supports it).

apps/web/eslint.config.mjs — flat config via FlatCompat bridging eslint-config-next (MUST survive;
Next 16 defaults to flat config — only the package.json lint-script entry point changes).

apps/web/app/layout.tsx — minimal RootLayout, does NOT yet import a globals.css (the plan adds the
import).

apps/web/app/page.tsx — imports type FlagStatus from @aerchain/shared-types (workspace-link proof);
renders a single <p>. The Button render is ADDED here; keep the FlagStatus import intact (do not
regress the existing workspace-link proof).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Next.js 15 → 16.2.9 + React 19.2.7 (exact pins, lint-script migration)</name>
  <files>apps/web/package.json, apps/web/next.config.mjs, apps/web/tsconfig.json, apps/web/eslint.config.mjs, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json (current ranges + "next lint" script)
    - apps/web/next.config.mjs (transpilePackages must survive)
    - apps/web/eslint.config.mjs (FlatCompat flat config must survive)
    - .planning/phases/01-foundation/01-05-RESEARCH.md §"Pinned versions" and §"Next 16 migration"
  </read_first>
  <action>
    Run the Next codemod from `apps/web` as cwd: `pnpm dlx @next/codemod@canary upgrade latest`
    (the runner stays pinned `@canary` even when upgrading to `latest`; it auto-handles the
    `next lint` → ESLint CLI migration, the turbopack key move, and `unstable_` prefix removal).
    Then enforce EXACT version pins (not ranges) in `apps/web/package.json`: `next` `16.2.9`,
    `react` `19.2.7`, `react-dom` `19.2.7`, `eslint-config-next` `16.2.9`, and bump
    `@types/react`/`@types/react-dom` to latest 19.x. React/react-dom MUST be exact strings
    (no `^`) to avoid 19.1.x resolution per the research doc pitfall. Re-run `pnpm install` so
    `pnpm-lock.yaml` reflects the pins; commit the lockfile.

    Change `package.json` `scripts.lint` from `next lint` to `eslint .` (the codemod's
    `next-lint-to-eslint-cli` does this — verify it landed; set it manually if not). `next build`
    no longer lints, so the lint script is now the only ESLint entry point.

    Keep `transpilePackages: ['@aerchain/shared-types']` in `next.config.mjs` (Turbopack supports it;
    Turbopack is now the default for dev/build and we have no webpack config, so this is clean — do
    NOT add a webpack escape hatch). Do NOT delete or rewrite `eslint.config.mjs` — the FlatCompat
    flat config survives because Next 16 defaults to flat config and `eslint-config-next@16` still
    publishes; only the lint entry point moved. Leave `tsconfig.json` paths and the `next` plugin
    intact. This is a foundation tech-debt upgrade (FOUND-UPGRADE), not mapped to a PLAT/PROMPT req.
  </action>
  <verify>
    <automated>grep -q '"next": "16.2.9"' apps/web/package.json && grep -q '"react": "19.2.7"' apps/web/package.json && grep -q '"react-dom": "19.2.7"' apps/web/package.json && grep -q '"eslint-config-next": "16.2.9"' apps/web/package.json && grep -q '"lint": "eslint \."' apps/web/package.json && ! grep -q 'next lint' apps/web/package.json && grep -q "transpilePackages" apps/web/next.config.mjs && pnpm --filter @aerchain/web exec tsc --noEmit && pnpm --filter @aerchain/web exec eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` pins `next` `16.2.9`, `react` `19.2.7`, `react-dom` `19.2.7`,
      `eslint-config-next` `16.2.9` as EXACT strings (no `^`/range).
    - `scripts.lint` is exactly `eslint .`; the string `next lint` appears nowhere in package.json.
    - `transpilePackages: ['@aerchain/shared-types']` still present in `next.config.mjs`.
    - `apps/web/eslint.config.mjs` still loads (FlatCompat config intact).
    - `pnpm --filter @aerchain/web exec tsc --noEmit` exits 0.
    - `pnpm --filter @aerchain/web exec eslint .` exits 0.
    - `pnpm-lock.yaml` updated to the new pins and committed.
  </acceptance_criteria>
  <done>Next 16.2.9 + React 19.2.7 pinned exactly, lint script migrated to `eslint .`, flat config and transpilePackages survive, tsc and eslint both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind v4 + shadcn/ui substrate + one Button proof</name>
  <files>apps/web/postcss.config.mjs, apps/web/app/globals.css, apps/web/app/layout.tsx, apps/web/components.json, apps/web/lib/utils.ts, apps/web/components/ui/button.tsx, apps/web/app/page.tsx, apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/tsconfig.json (CONFIRM compilerOptions.paths has {"@/*":["./*"]} BEFORE running init — init errors without it; it is already present, but verify)
    - apps/web/app/layout.tsx (must gain the globals.css import)
    - apps/web/app/page.tsx (keep the FlagStatus import; add the Button render)
    - .planning/phases/01-foundation/01-05-RESEARCH.md §"Tailwind v4 setup" and §"shadcn init"
  </read_first>
  <action>
    All commands run with `apps/web` as cwd (NOT repo root, NOT `--monorepo` — the research doc
    flags a pnpm-workspace detection gotcha if run at root).

    1. Tailwind v4 (CSS-first — there is NO `tailwind.config.js` in v4, do not create one):
       `pnpm add tailwindcss@4.3.1 @tailwindcss/postcss@4.3.1 postcss`. Create
       `apps/web/postcss.config.mjs` exporting `{ plugins: { "@tailwindcss/postcss": {} } }`
       (no autoprefixer, no content array). Create `apps/web/app/globals.css` containing
       `@import "tailwindcss";`. Add `import "./globals.css";` to `apps/web/app/layout.tsx` so the
       stylesheet is loaded (it is not imported today).

    2. shadcn init (substrate only): first VERIFY `apps/web/tsconfig.json` has
       `compilerOptions.paths {"@/*":["./*"]}` (init errors otherwise — it is already present).
       Run `pnpm dlx shadcn@latest init`; select base color `neutral` and style `new-york` (the
       old default style is deprecated). This writes `components.json` (rsc:true, tsx:true,
       iconLibrary lucide, cssVariables:true, `@/*` aliases), `lib/utils.ts` exporting `cn()`
       (= twMerge(clsx(...))), and REWRITES `globals.css` with the v4 token system (oklch `:root`
       + `.dark` vars, `@theme inline`, `@custom-variant dark`, and `@import "tw-animate-css"` —
       use tw-animate-css, NOT the deprecated tailwindcss-animate). It installs
       class-variance-authority, clsx, tailwind-merge, tw-animate-css, lucide-react. Do NOT pass
       `--legacy-peer-deps`/`--force` — pnpm needs no peer flags here.

    3. ONE proof component: `pnpm dlx shadcn@latest add button` (generates
       `components/ui/button.tsx`). Add NO other component.

    4. Render the proof: edit `apps/web/app/page.tsx` to import `Button` from
       `@/components/ui/button` and render a single styled `<Button>` (e.g. label "Bid Desk").
       KEEP the existing `FlagStatus` type import and its usage intact — that is the 01-01
       workspace-link proof and must not regress. This Button is the end-to-end UI-substrate proof,
       nothing more — do not build any layout, grid, or screen around it.

    Commit `package.json` + `pnpm-lock.yaml` changes from the installs.
  </action>
  <verify>
    <automated>test -f apps/web/postcss.config.mjs && grep -q "@tailwindcss/postcss" apps/web/postcss.config.mjs && test -f apps/web/components.json && test -f apps/web/lib/utils.ts && grep -q "export function cn\|export const cn" apps/web/lib/utils.ts && test -f apps/web/components/ui/button.tsx && grep -q '@import "tailwindcss"' apps/web/app/globals.css && grep -q "globals.css" apps/web/app/layout.tsx && grep -q 'from "@/components/ui/button"' apps/web/app/page.tsx && grep -q "FlagStatus" apps/web/app/page.tsx && pnpm --filter @aerchain/web exec tsc --noEmit && pnpm --filter @aerchain/web exec eslint . && pnpm --filter @aerchain/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/postcss.config.mjs` exists wiring `@tailwindcss/postcss`; no `tailwind.config.js` exists.
    - `apps/web/components.json` exists (new-york style, neutral base, rsc:true, `@/*` aliases).
    - `apps/web/lib/utils.ts` exists and exports `cn`.
    - `apps/web/components/ui/button.tsx` exists; NO other file under `components/ui/`.
    - `apps/web/app/globals.css` contains `@import "tailwindcss"` and the shadcn oklch token system
      (uses tw-animate-css, not tailwindcss-animate).
    - `apps/web/app/layout.tsx` imports `./globals.css`.
    - `apps/web/app/page.tsx` imports `Button` from `@/components/ui/button` and renders it; the
      original `FlagStatus` import/usage is still present.
    - `pnpm --filter @aerchain/web exec tsc --noEmit` exits 0.
    - `pnpm --filter @aerchain/web exec eslint .` exits 0.
    - `pnpm --filter @aerchain/web build` (Turbopack) completes with no errors.
  </acceptance_criteria>
  <done>Tailwind v4 + shadcn substrate wired, one Button renders, all web gates (tsc/eslint/build) pass, workspace-link proof preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `apps/web` upgraded to Next 16.2.9 + React 19.2.7 with Tailwind v4 + shadcn/ui substrate and a
    single Tailwind-styled shadcn `<Button>` rendered on the existing page as the end-to-end UI
    integration proof. No buyer screens, no other components.
  </what-built>
  <how-to-verify>
    Per CLAUDE.md §11 (Playwright end-to-end pass on the running app — not just curl/unit checks):
    1. From `apps/web`, start the dev server: `pnpm --filter @aerchain/web dev` (Turbopack default).
    2. Drive the page in a real browser with the Playwright browser tool: navigate to the local URL
       (default http://localhost:3000), wait for render, and read the DOM.
    3. Confirm a Tailwind-styled shadcn Button is visibly rendered (rounded, themed background per
       the new-york/neutral tokens — not an unstyled native button), and the existing
       "workspace link verified (missing)" text/FlagStatus proof is still present.
    4. Capture a screenshot of the rendered page for the demo/write-up (store under docs/demo or
       attach to the summary).
    Expected outcome: the page shows a properly styled Button — Tailwind v4 + shadcn tokens are
    flowing end-to-end, proving the UI substrate works on Next 16.
  </how-to-verify>
  <resume-signal>Type "approved" once the styled Button renders correctly in the browser, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/pnpm dependency install → build | New devtime/UI dependencies (Tailwind v4, shadcn deps, Next 16) enter the build at install time. This is the only new surface; the change is pure build-time/UI tooling and adds NO runtime network call, secret, auth path, or user-input boundary. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-SC | Tampering | pnpm installs (next 16.2.9, react/react-dom 19.2.7, tailwindcss + @tailwindcss/postcss 4.3.1, shadcn-installed cva/clsx/tailwind-merge/tw-animate-css/lucide-react) | mitigate | EXACT version pins in package.json + committed `pnpm-lock.yaml` (integrity-hash locked). All packages are mainstream, GA, actively maintained (Next.js/Vercel, Meta React, Tailwind Labs, shadcn) — RESEARCH.md verified all versions live against the npm registry on 2026-06-27. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages; no `--force`/`--legacy-peer-deps`. |
| T-01-05-INFO | Information Disclosure | UI substrate + build config | accept | No secrets, env vars, API keys, or PII touched. Tailwind v4 is CSS-first build tooling; shadcn components are local source. No new network/trust surface introduced. Low-value, build-time-only. |
| T-01-05-DOS | Denial of Service | Turbopack build (now default) | accept | No custom webpack config exists, so Turbopack's fail-fast on stray config does not apply. `next build --webpack` is the documented escape hatch if a future build breaks; not needed here. Build success is gated in Task 2 verification. |
</threat_model>

<verification>
Phase-level checks (run from repo root unless noted):
- `grep '"next"\|"react"\|"react-dom"\|"eslint-config-next"' apps/web/package.json` shows exact pins
  (16.2.9 / 19.2.7 / 19.2.7 / 16.2.9), and `scripts.lint` is `eslint .` (no `next lint`).
- `pnpm --filter @aerchain/web exec tsc --noEmit` exits 0.
- `pnpm --filter @aerchain/web exec eslint .` exits 0 (flat config still loads).
- `pnpm --filter @aerchain/web build` (Turbopack) completes with no errors.
- `apps/web/components.json`, `apps/web/lib/utils.ts` (exports `cn`), and
  `apps/web/components/ui/button.tsx` all exist; `apps/web/app/globals.css` contains
  `@import "tailwindcss"`; `apps/web/app/layout.tsx` imports `./globals.css`.
- `apps/web/app/page.tsx` imports and renders `Button` from `@/components/ui/button` AND still
  imports `FlagStatus` (no regression of the 01-01 workspace-link proof).
- Prettier clean on apps/web:
  `pnpm exec prettier --check 'apps/web/**/*.{ts,tsx,css,json,mjs}'`
  (exempt generated/shadcn files only if Prettier conflicts with shadcn's own formatting — keep the
  exemption minimal and noted).
- No backend regression: `cd services/ai && uv run pytest` still reports 90 passed, and
  `cd services/ai && uv run ruff check .` still clean.
- Render proof: a Tailwind-styled shadcn Button is visibly rendered on the running page, confirmed
  via Playwright (CLAUDE.md §11), with a screenshot captured.
</verification>

<success_criteria>
- `apps/web` runs on Next.js 16.2.9 + React 19.2.7 (exact pins), lint migrated to `eslint .`, flat
  ESLint config and `transpilePackages` intact.
- Tailwind v4 (CSS-first, no config file) wired through PostCSS; `globals.css` imported in layout.
- shadcn/ui substrate present: components.json (new-york/neutral), `cn()` helper, oklch token system
  using tw-animate-css.
- Exactly ONE shadcn component (Button) added and rendered as the end-to-end proof; the workspace-
  link proof is preserved.
- All web gates (tsc, eslint, build, prettier) pass; backend pytest (90) + ruff stay green.
- Playwright confirms the styled Button renders on the running app.
- Scope held: no other shadcn components, no buyer screens, no theme customization.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done.
</output>
