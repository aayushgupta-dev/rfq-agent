---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/ai/prompts/__init__.py
  - services/ai/prompts/registry.py
  - services/ai/prompts/rfq-gen.v1.md
  - services/ai/prompts/vendor-gen.v1.md
  - services/ai/prompts/messy-data-gen.v1.md
  - services/ai/prompts/ui-ux-gen.v1.md
  - services/ai/prompts/extraction.v1.md
  - services/ai/prompts/comparison.v1.md
  - services/ai/prompts/clarification.v1.md
  - services/ai/tests/test_prompt_registry.py
autonomous: true
requirements: [PROMPT-01]
must_haves:
  truths:
    - "The Prompt Pack registry exists in services/ai/prompts/ as first-class versioned .md source (not inline strings buried in code)."
    - "All 7 prompts (rfq-gen, vendor-gen, messy-data-gen, ui-ux-gen, extraction, comparison, clarification) exist as stubs with full YAML frontmatter + placeholder bodies + TODO markers."
    - "The registry loads a prompt by id and resolves the latest version by filename suffix (extraction.v1.md)."
  artifacts:
    - path: "services/ai/prompts/registry.py"
      provides: "load(prompt_id) resolving latest-version-by-filename .md via python-frontmatter"
      contains: "def load"
    - path: "services/ai/prompts/extraction.v1.md"
      provides: "One of the 7 prompt stubs with frontmatter id/version/intent/model_tier/failure_handling"
      contains: "model_tier"
    - path: "services/ai/tests/test_prompt_registry.py"
      provides: "Asserts all 7 load by id and resolve latest version"
  key_links:
    - from: "services/ai/prompts/registry.py"
      to: "services/ai/prompts/*.v*.md"
      via: "glob {id}.v*.md, parse frontmatter, return highest version"
      pattern: "frontmatter.load"
---

<objective>
Build the Prompt Pack registry skeleton with all 7 prompt stubs (D-11, D-12, D-13, PROMPT-01). Each prompt is a `.md` file = YAML frontmatter (id, version, intent, model_tier, failure_handling) + markdown body, loaded by a small Python registry that resolves the latest version by filename suffix. Create all 7 stubs (rfq-gen, vendor-gen, messy-data-gen, ui-ux-gen, extraction, comparison, clarification) with full frontmatter + placeholder bodies + TODO markers.

Purpose: The Prompt Pack is 30% of the grade and a first-class versioned source tree (CLAUDE.md §7). Phase 1 proves the registry loads every prompt and locks the structure; the bodies get fleshed out in their owning phases (P2/P3/P4/P5).

Output: `services/ai/prompts/registry.py`, 7 `.md` stubs, and a test asserting all 7 load and resolve latest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Registry shape VERIFIED in RESEARCH.md "Code Examples" -> Prompt registry. ~30 lines, stdlib + python-frontmatter.
     Use this exact approach - no exploration: -->

load(prompt_id: str) -> frontmatter.Post   # .metadata (dict) + .content (str body)
  glob f"{prompt_id}.v*.md" in the prompts dir
  parse filename suffix .vN.md -> int version
  return frontmatter.load(highest-version file)
  raise KeyError(f"no prompt '{prompt_id}'") if none found
  import: import frontmatter  (the python-frontmatter package, installed in Plan 01)

Frontmatter keys per stub (D-11): id, version, intent, model_tier, failure_handling
model_tier values map to the LLM factory tiers: "reasoning" | "cheap"
</interfaces>

<notes>
- 7 prompts (D-13): rfq-gen, vendor-gen, messy-data-gen, ui-ux-gen, extraction, comparison, clarification. ALL created as stubs now - full bodies are fleshed out later (rfq-gen/vendor-gen/messy-data-gen in P2, extraction in P3, comparison/clarification in P4, ui-ux-gen in P5). Each body is a TODO placeholder marking its owning phase.
- model_tier per prompt (sensible defaults, Claude's discretion): the reasoning-heavy generation/extraction/comparison prompts -> "reasoning"; lighter clarification-question drafting -> "cheap" (CLAUDE.md §5 tier guidance). Set a reasonable tier per stub.
- Versioning (D-12): filename suffix is the version of record (extraction.v1.md); new version = new file; registry resolves latest. Frontmatter ALSO carries `version` for metadata. Phase 1 only ships v1 of each.
- Use `python-frontmatter` (installed in Plan 01), NOT a manual `text.split("---")` (RESEARCH Don't Hand-Roll - fragile on `---` in body).
- This plan shares NO files with Plans 02 or 03 (it owns prompts/ entirely + its own test file) - fully parallel in Wave 2.
- The registry is intentionally minimal (~30 lines). Do NOT add caching, hot-reload, or a prompt-rendering/templating engine - none requested (PonyTail: YAGNI; CLAUDE.md §2). Mark the latest-resolution with a `# ponytail:` note that it exists for D-12.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt registry + all 7 versioned stubs</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Code Examples" -> Prompt registry + the extraction.v1.md stub example; "Don't Hand-Roll" frontmatter row)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 .md+frontmatter loaded by id, D-12 filename-suffix versioning, D-13 all 7 stubs)
    - services/ai/pyproject.toml (confirm python-frontmatter installed; from Plan 01)
    - CLAUDE.md §7 (Prompt Pack: the 7 prompts, per-prompt intent/rationale/failure-handling), §2 (minimal - no over-engineering)
  </read_first>
  <behavior>
    - load("extraction") returns a Post whose .metadata has keys id, version, intent, model_tier, failure_handling and .content is the (placeholder) body.
    - load(<each of the 7 ids>) succeeds and returns metadata with a non-empty intent and a model_tier in {"reasoning","cheap"}.
    - load("does-not-exist") raises KeyError.
    - When two version files exist for an id (e.g. a temporary foo.v1.md + foo.v2.md), load("foo") returns the v2 metadata (latest-by-suffix).
  </behavior>
  <action>
    Create `services/ai/prompts/registry.py` with `load(prompt_id: str)` per the verified RESEARCH pattern: glob the `{prompt_id}.v*.md` files in the prompts dir, parse the `.vN.md` suffix to an int, return `frontmatter.load()` of the highest-version file, raise `KeyError(f"no prompt '{prompt_id}'")` if none. Add `services/ai/prompts/__init__.py` re-exporting `load`.

    Create all 7 stub files (D-13), each with YAML frontmatter keys `id`, `version: 1`, `intent`, `model_tier`, `failure_handling`, and a placeholder body with a `TODO:` marker naming the owning phase:
      - `rfq-gen.v1.md` (model_tier: reasoning; intent: generate one realistic marketing-services RFQ; TODO body -> P2)
      - `vendor-gen.v1.md` (reasoning; generate a vendor response; TODO -> P2)
      - `messy-data-gen.v1.md` (reasoning; inject per-vendor mess spec complexity; TODO -> P2)
      - `ui-ux-gen.v1.md` (reasoning; buyer UI structure/sections/copy; TODO -> P5)
      - `extraction.v1.md` (reasoning; per-vendor structured extraction with evidence + flags; failure_handling: never fill missing info, offsets validated in code per P2 grounding gate; TODO -> P3)
      - `comparison.v1.md` (reasoning; comparability-before-ranking over ExtractionResult[]; TODO -> P4)
      - `clarification.v1.md` (cheap; draft clarification/exception questions for missing/unclear/conflicting info; TODO -> P4)
    Each `failure_handling` line states how the prompt handles unreliable/missing/conflicting info (CLAUDE.md §7) - even as a stub.

    Write `services/ai/tests/test_prompt_registry.py` covering the <behavior> cases, including a loop asserting all 7 ids load with a valid model_tier, and a latest-version test (create v1+v2 temp files in a tmp dir, or parametrize the suffix parse). Run `uv run pytest` - green.
  </action>
  <verify>
    <automated>cd services/ai && uv run pytest tests/test_prompt_registry.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls services/ai/prompts/*.v*.md` lists exactly 7 prompt files, each with YAML frontmatter keys id/version/intent/model_tier/failure_handling.
    - `uv run python -c "from prompts import load; [load(i) for i in ['rfq-gen','vendor-gen','messy-data-gen','ui-ux-gen','extraction','comparison','clarification']]"` exits 0 (all 7 load by id).
    - `load("does-not-exist")` raises KeyError.
    - The latest-version test passes: given two version files for one id, `load` returns the higher version's metadata.
    - `uv run pytest tests/test_prompt_registry.py` exits 0.
    - Each stub's `model_tier` is one of "reasoning"/"cheap" and each `failure_handling` value is non-empty.
  </acceptance_criteria>
  <done>The prompt registry loads any of the 7 prompts by id and resolves latest-by-filename; all 7 stubs exist as first-class versioned .md source with full frontmatter + TODO placeholder bodies; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt `.md` files -> registry | first-party authored source files parsed as data (no external/untrusted input in Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | YAML frontmatter parsing of `.md` prompts | mitigate | Use `python-frontmatter` (RESEARCH "Approved", installed Plan 01) which uses safe YAML loading; prompt files are first-party source committed to the repo, not user-supplied uploads. |
| T-04-02 | Information Disclosure | prompt bodies/frontmatter | accept | Phase 1 stubs contain only intent/TODO placeholders - no secrets, no PII. Prompts are intended to be documented publicly (the Prompt Pack deliverable). |

No high-severity threats for a static, first-party prompt registry skeleton parsing committed source files.
</threat_model>

<verification>
- `ls services/ai/prompts/*.v*.md` shows exactly 7 stubs, each with full frontmatter.
- All 7 load by id via the registry; latest-version resolution works; missing id raises KeyError.
- `uv run pytest tests/test_prompt_registry.py` green.
</verification>

<success_criteria>
- ROADMAP success criterion 5 made TRUE: the Prompt Pack registry exists in services/ai/prompts/ with all 7 stubs as versioned source (skeleton, not inline strings).
- The registry is minimal and resolves latest-by-filename per D-12.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
