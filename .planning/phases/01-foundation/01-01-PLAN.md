---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - main.py
  - .python-version
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .prettierrc.json
  - .prettierignore
  - services/ai/pyproject.toml
  - services/ai/.python-version
  - services/ai/__init__.py
  - packages/shared-types/package.json
  - packages/shared-types/index.d.ts
  - apps/web/package.json
  - apps/web/.eslintrc.json
  - apps/web/next.config.mjs
  - apps/web/tsconfig.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
autonomous: true
requirements: [PLAT-02]
must_haves:
  truths:
    - "The repo root is a pnpm+turbo JS workspace; the Python project lives entirely in services/ai with its own pyproject.toml and uv env."
    - "apps/web is a Next.js App Router shell that imports from @aerchain/shared-types, proving the workspace link resolves."
    - "ruff lints+formats services/ai; prettier+eslint lint apps/web; both run clean on the scaffold."
  artifacts:
    - path: "services/ai/pyproject.toml"
      provides: "Relocated Python project (name=aerchain-ai) with ruff+pytest config"
      contains: "name = \"aerchain-ai\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package globs (apps/*, packages/* — NOT services/*)"
      contains: "packages/*"
    - path: "packages/shared-types/package.json"
      provides: "The @aerchain/shared-types contract package consumed by apps/web"
      contains: "@aerchain/shared-types"
    - path: "apps/web/app/page.tsx"
      provides: "Next.js shell that imports a type from @aerchain/shared-types"
  key_links:
    - from: "apps/web"
      to: "@aerchain/shared-types"
      via: "pnpm workspace dependency (workspace:*)"
      pattern: "workspace:\\*"
    - from: "repo root"
      to: "services/ai"
      via: "Python project relocated; root main.py + pyproject deleted"
      pattern: "aerchain-ai"
---

<objective>
Stand up the full monorepo skeleton (D-01, D-02, D-03) and the dev tooling spine (D-17, D-18). Relocate the root Python project into `services/ai/`, delete the placeholder `main.py`, make the repo root a pnpm+turbo JS workspace, create the `apps/web` Next.js App Router shell and the `packages/shared-types` contract package, and wire ruff/pytest (Python) + prettier/eslint (TS).

Purpose: Every downstream Phase 1 plan (schemas+codegen, LLM+SSE, prompt registry) depends on this directory layout and tooling existing first. This is Wave 1 — nothing else can start until the skeleton lands.

Output: A working JS workspace root, a relocated Python service, a Next.js shell proving the shared-types link, and configured linters/formatters on both sides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<notes>
- RESEARCH.md "Recommended Project Structure" is the authoritative layout. `pnpm-workspace.yaml` lists `apps/*` and `packages/*` ONLY — turbo orchestrates JS; uv owns `services/ai` entirely (turbo must NOT run over the Python service).
- `packages/shared-types/index.d.ts` is a GENERATED artifact (filled by Plan 02's codegen). In THIS plan it is created as a minimal hand-stubbed placeholder ONLY so the pnpm link + apps/web import resolves; Plan 02 overwrites it via codegen and the drift test then owns it. Mark the placeholder content with a comment that codegen will replace it.
- Environment verified available: uv 0.9.26, pnpm 10.28.1, node 25.3.0, python3 3.12.12, ruff 0.15.13, pytest 9.0.2. turbo + json-schema-to-typescript are NOT installed yet (this plan installs them at the root).
- Versions to pin (RESEARCH.md Standard Stack): `turbo` latest 2.x, `next` latest 15.x, `prettier` latest, `json-schema-to-typescript@15` (15.0.4). Pin exact resolved versions at install time.
- `.env` decision (RESEARCH Open Question 1): keep ONE `.env` at repo root; `services/ai` loads it from one level up. Document this in README in Plan 03 (verify_access references it). Do NOT create a second `.env`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate Python project into services/ai and install JS workspace root</name>
  <read_first>
    - pyproject.toml (root — name=aerchain, empty deps, to be relocated/renamed)
    - main.py (root — placeholder hello-world, to be DELETED per D-02)
    - .python-version (root — 3.12, moves with the project)
    - .gitignore (confirm node_modules/.venv/__pycache__ already ignored before adding deps)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Recommended Project Structure", "Runtime State Inventory", Installation block)
    - CLAUDE.md §5 (monorepo architecture), §10 (uv + pnpm dev), §15 (turbo orchestrates JS only)
  </read_first>
  <action>
    Create `services/ai/` and move the Python project there per D-02: create `services/ai/pyproject.toml` with `name = "aerchain-ai"`, `requires-python = ">=3.12"`; move `.python-version` (3.12) into `services/ai/`; add an empty `services/ai/__init__.py` (or package-dir config) so the service is importable for codegen (RESEARCH Pitfall 1). DELETE the root `main.py` and the root `pyproject.toml` (root becomes JS-only). Add Python deps via uv from inside `services/ai/`: runtime `pydantic langgraph langchain langchain-openai fastapi uvicorn sse-starlette python-frontmatter`, dev `pydantic-to-typescript==2.0.0 ruff pytest`. Configure ruff (lint + format) and pytest in `services/ai/pyproject.toml` (sensible defaults: line length 100, target py312 — Claude's discretion per D-17).

    At the repo root, initialize the JS workspace (D-03): create `package.json` (private, name `aerchain`, packageManager pnpm), `pnpm-workspace.yaml` listing exactly `apps/*` and `packages/*` (NOT `services/*` — RESEARCH anti-pattern), and a minimal `turbo.json` pipeline (lint/build tasks only; depth deferred to P5 per Deferred Ideas). Install root devDependencies via pnpm: `turbo` (latest 2.x), `prettier` (latest), and `json-schema-to-typescript@15` (15.0.4 — REQUIRED by Plan 02 codegen, RESEARCH Pitfall 2). Add root `.prettierrc.json` and `.prettierignore` that EXEMPTS `packages/shared-types/index.d.ts` (generated artifact, D-18).

    Run `uv run ruff check` and `uv run ruff format --check` from `services/ai/` to confirm clean.
  </action>
  <verify>
    <automated>test -f services/ai/pyproject.toml && ! test -f main.py && ! test -f pyproject.toml && grep -q 'aerchain-ai' services/ai/pyproject.toml && grep -q 'packages/\*' pnpm-workspace.yaml && (cd services/ai && uv run ruff check .) && test -d node_modules/json-schema-to-typescript</automated>
  </verify>
  <acceptance_criteria>
    - `services/ai/pyproject.toml` exists with `name = "aerchain-ai"`; root `main.py` and root `pyproject.toml` no longer exist.
    - `uv run ruff check .` from `services/ai/` exits 0; `uv run pytest --co` (collect-only) exits 0 (no tests yet is fine).
    - `pnpm-workspace.yaml` contains `apps/*` and `packages/*` and does NOT contain `services/*`.
    - `node_modules/.bin/json2ts` exists (json-schema-to-typescript@15 installed at root) and `node_modules/.bin/turbo` exists.
    - `.prettierignore` lists `packages/shared-types/index.d.ts`.
  </acceptance_criteria>
  <done>Python project relocated to services/ai with deps+ruff+pytest; root is a pnpm+turbo workspace with turbo, prettier, and json-schema-to-typescript@15 installed; generated TS path is prettier-exempt.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared-types package and the Next.js App Router shell that imports it</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Recommended Project Structure" — packages/shared-types and apps/web layout)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 apps/web shell renders nothing real but proves the link; D-03 pnpm links web → shared-types)
    - CLAUDE.md §5 (apps/web is a thin client, no AI SDKs, no business logic), §15 (shared-types is the contract, mechanically generated)
    - package.json (root — created in Task 1; confirm workspace globs)
  </read_first>
  <action>
    Create `packages/shared-types/package.json` with name `@aerchain/shared-types`, `types`/`main` pointing at `index.d.ts`, private. Create a PLACEHOLDER `packages/shared-types/index.d.ts` exporting one trivial type (e.g. `export type FlagStatus = 'present' | 'missing' | 'unclear' | 'conflicting' | 'unsupported';`) with a header comment `// GENERATED by services/ai/scripts/codegen.py — do not hand-edit (Plan 02 overwrites this)`. This placeholder exists ONLY so the workspace link + apps/web import resolves now; Plan 02's codegen replaces it and the drift test owns it thereafter.

    Create the `apps/web` Next.js App Router shell (D-01): `apps/web/package.json` (name `@aerchain/web`, depends on `"@aerchain/shared-types": "workspace:*"`, next latest 15.x as a dependency), `next.config.mjs`, `tsconfig.json` (App Router defaults, transpile the workspace package as needed), `.eslintrc.json` extending the Next baseline + reconciling with Prettier (D-18), and minimal `app/layout.tsx` + `app/page.tsx`. The page MUST import the `FlagStatus` type from `@aerchain/shared-types` and reference it (e.g. type-annotate a const) to PROVE the link — it renders nothing real (D-01).

    Run `pnpm install` at root to link the workspace, then `pnpm --filter @aerchain/web exec tsc --noEmit` to confirm the cross-package type import type-checks.
  </action>
  <verify>
    <automated>grep -q '@aerchain/shared-types' packages/shared-types/package.json && grep -q 'workspace:\*' apps/web/package.json && grep -q "@aerchain/shared-types" apps/web/app/page.tsx && (cd apps/web && pnpm exec tsc --noEmit) && pnpm exec prettier --check 'apps/web/**/*.{ts,tsx}'</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared-types/index.d.ts` exists with the GENERATED header comment and exports `FlagStatus` as the 5-state union.
    - `apps/web/app/page.tsx` imports a type from `@aerchain/shared-types` and uses it; `pnpm exec tsc --noEmit` in apps/web exits 0 (the cross-package link resolves and type-checks).
    - `apps/web/package.json` declares `"@aerchain/shared-types": "workspace:*"`.
    - `pnpm exec prettier --check 'apps/web/**/*.{ts,tsx}'` exits 0 and `pnpm exec eslint apps/web` exits 0.
  </acceptance_criteria>
  <done>shared-types package exists with a clearly-marked generated placeholder; apps/web shell imports and type-uses a shared-types type, proving the pnpm workspace link end-to-end; eslint+prettier clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-time → repo | npm + PyPI dependency resolution introduces third-party code into the workspace (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm/PyPI dependency installs (turbo, prettier, json-schema-to-typescript, pydantic/langchain stack) | mitigate | All packages are version-pinned from RESEARCH.md "Package Legitimacy Audit" (long-established, mandated, registry-verified 2026-06-27). Pin exact resolved versions in lockfiles (pnpm-lock.yaml, uv.lock) and commit them so installs are reproducible. |
| T-01-02 | Information Disclosure | `.gitignore` coverage of new dep/env dirs | accept | `.gitignore` already covers node_modules/.venv/__pycache__ (verified). No secrets handled in this plan; `.env` stays empty until Plan 03. Low risk. |
| T-01-SC | Tampering | npm/pip/uv installs | mitigate | slopcheck unavailable in research env, but all packages are CLAUDE.md-mandated/long-established with registry-verified versions — RESEARCH disposition is "Approved", none [SUS]/[SLOP]. No blocking human-verify checkpoint required for this set. Reproducibility enforced via committed lockfiles. |

No high-severity threats for pure monorepo scaffolding + tooling installation beyond reproducible-dependency provenance, which the committed lockfiles + version pins address.
</threat_model>

<verification>
- Repo root has no `main.py` / `pyproject.toml`; Python project is in `services/ai/` and importable.
- `uv run ruff check .` + `uv run pytest --co` clean from `services/ai/`.
- `pnpm install` links the workspace; `apps/web` type-checks against `@aerchain/shared-types`.
- json-schema-to-typescript@15 + turbo installed at root; generated TS path is prettier-exempt.
</verification>

<success_criteria>
- The monorepo layout matches RESEARCH.md "Recommended Project Structure".
- The pnpm workspace link `apps/web → @aerchain/shared-types` resolves and type-checks (PLAT-02 wiring half — codegen target package exists and is consumed).
- Dev tooling spine (ruff+pytest, prettier+eslint) runs clean on both sides.
- json2ts is on PATH for Plan 02's codegen.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
