---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/ai/schemas/__init__.py
  - services/ai/schemas/envelope.py
  - services/ai/schemas/events.py
  - services/ai/schemas/domain.py
  - services/ai/scripts/codegen.py
  - services/ai/tests/test_codegen_drift.py
  - packages/shared-types/index.d.ts
autonomous: true
requirements: [PLAT-01, PLAT-02]
must_haves:
  truths:
    - "Every contract field models absence as a first-class 5-state enum {status, value?, evidence?} — there is no nullable that silently collapses to a blank."
    - "Running the codegen script regenerates packages/shared-types/index.d.ts mechanically from the pydantic schemas — the TS contract is never hand-mirrored."
    - "The drift-check test fails (non-zero) when a pydantic field changes without regenerating; passes when committed TS matches."
  artifacts:
    - path: "services/ai/schemas/envelope.py"
      provides: "Generic Field[T] absence envelope, Evidence, ConflictingValue[T], FlagStatus enum"
      contains: "class Field"
    - path: "services/ai/schemas/events.py"
      provides: "SSE EventEnvelope (closed enum type) + ErrorPayload"
      contains: "ErrorPayload"
    - path: "services/ai/schemas/domain.py"
      provides: "RFQ/VendorResponse/ExtractionResult/ComparisonResult compiling stubs"
      contains: "class ExtractionResult"
    - path: "services/ai/scripts/codegen.py"
      provides: "pydantic2ts → packages/shared-types codegen entry point"
    - path: "services/ai/tests/test_codegen_drift.py"
      provides: "Drift-check test (regenerate to temp, assert byte-equal)"
    - path: "packages/shared-types/index.d.ts"
      provides: "Generated TS contract committed and drift-checked"
      contains: "FieldStr"
  key_links:
    - from: "services/ai/scripts/codegen.py"
      to: "packages/shared-types/index.d.ts"
      via: "generate_typescript_defs with json2ts_cmd pointing at ./node_modules/.bin/json2ts"
      pattern: "generate_typescript_defs"
    - from: "services/ai/tests/test_codegen_drift.py"
      to: "packages/shared-types/index.d.ts"
      via: "byte-equality assertion against committed file"
      pattern: "read_text"
---

<objective>
Define the foundational contract primitives in pydantic and mechanically generate the TypeScript contract with an enforced drift-check. Build the generic `Field[T]` absence envelope, the evidence type, the 5-state flag enum, the SSE event envelope, and the four named domain schemas as compiling stubs (D-04..D-10). Then write the codegen script (D-14, PLAT-02) and a pytest drift-check that goes red on drift.

Purpose: This is the contract — the single source of truth that everything downstream is typed by, with absence as a first-class enum (PLAT-01). The codegen + drift-check makes "never hand-mirrored" mechanically enforced (PLAT-02).

Output: `services/ai/schemas/` (envelope, events, domain stubs), `services/ai/scripts/codegen.py`, the committed generated `packages/shared-types/index.d.ts`, and a passing drift-check test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- The contract shape is VERIFIED in RESEARCH.md "Code Examples" → "Generic Field[T] envelope".
     The executor uses that exact shape — no codebase exploration needed. Key identifiers: -->

FlagStatus (str, Enum): present | missing | unclear | conflicting | unsupported  (D-07)
Evidence: { snippet: str, char_start: int, char_end: int, source_id: str }  (D-04, extra="forbid")
ConflictingValue[T]: { value: T | None, evidence: list[Evidence] }  (D-06)
Field[T]: { status: FlagStatus, value: T | None, evidence: list[Evidence], values: list[ConflictingValue[T]] | None }  (D-05)
EventEnvelope: { type: Literal["status","partial","result","error","done"], payload: ... }  (D-09 closed enum)
ErrorPayload: { code: str, message: str, recoverable: bool }  (D-10)

<!-- Domain stubs (D-08): RFQ, VendorResponse, ExtractionResult, ComparisonResult.
     MINIMAL compiling stubs only — full field shapes deferred to P2/P3/P4.
     Each stub must reference the generic Field[T] with at least 2-3 concrete parametrizations
     (Field[str], Field[Decimal], Field[int]) so codegen monomorphizes FieldStr/FieldDecimal/FieldInt
     and proves the generic produces clean concrete TS (the decisive D-05 proof). -->
</interfaces>

<notes>
- D-05 is RESOLVED in RESEARCH.md: build the GENERIC `Field[T]`. The "concrete per-type classes" fallback is UNNECESSARY — json2ts monomorphizes the generic into FieldStr/FieldDecimal/FieldInt for free. Do NOT hand-write concrete classes.
- Set `model_config = ConfigDict(extra="forbid")` on EVERY contract model — removes the `[k: string]: unknown` index signature from generated TS (RESEARCH Pitfall 3, verified).
- Empty-interface trap (RESEARCH Pitfall 3 / Open Question 2): do NOT create a shared EXPORTED `extra="forbid"` base BaseModel — it leaks as `export interface Strict {}`. Set `model_config` per-model (the lazy correct choice). If any empty interface still leaks, the codegen script may strip it post-gen; prefer per-model config first.
- Codegen robustness (RESEARCH Pitfall 1): run codegen as a Python SCRIPT from inside services/ai (package importable), calling `generate_typescript_defs("schemas", OUT, json2ts_cmd="./node_modules/.bin/json2ts")`. json2ts lives at the REPO ROOT node_modules (installed in Plan 01) — resolve the path relative to repo root, not services/ai.
- Output is byte-identical across runs (RESEARCH verified) → the drift test is a plain `assert generated == committed`. No determinism/ordering work needed.
- Decimal → TS `string` is the expected money shape (RESEARCH A4) — correct, JSON has no decimal. Confirm the UI side later; not a blocker here.
- The committed index.d.ts produced HERE overwrites the Plan 01 placeholder. After this plan, the drift test owns that file.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the contract primitives and domain stubs in pydantic</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Code Examples" → Generic Field[T] envelope; "Architecture Patterns" Patterns 1 & 2; Pitfall 3)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 evidence shape, D-05 generic Field[T], D-06 conflicting values[], D-07 flag enum, D-08 domain stubs, D-09 SSE taxonomy, D-10 error payload)
    - services/ai/pyproject.toml (confirm pydantic installed; from Plan 01)
    - CLAUDE.md §8 (absence is first-class; grounding offsets exist for code validation), §15 (schemas are the contract)
  </read_first>
  <behavior>
    - Field[str] with status=present and a value serializes/validates with value retained.
    - Field[str] with status=missing and value=None is valid (absence is a first-class state, not an error).
    - Field[int] with status=conflicting carries a non-empty values[] list, each ConflictingValue with its own evidence.
    - Evidence rejects extra keys (extra="forbid"): constructing Evidence with an unknown field raises ValidationError.
    - EventEnvelope rejects a type outside {status,partial,result,error,done} (closed Literal).
    - ErrorPayload requires code:str, message:str, recoverable:bool.
    - Each domain stub (RFQ, VendorResponse, ExtractionResult, ComparisonResult) instantiates with its stub fields and uses Field[T] for at least one field.
  </behavior>
  <action>
    Create `services/ai/schemas/envelope.py` with `FlagStatus(str, Enum)` = present|missing|unclear|conflicting|unsupported (D-07); `Evidence` = snippet:str, char_start:int, char_end:int, source_id:str (D-04); generic `ConflictingValue[T]` = value:T|None, evidence:list[Evidence] (D-06); generic `Field[T]` = status:FlagStatus, value:T|None, evidence:list[Evidence], values:list[ConflictingValue[T]]|None (D-05). Set `model_config = ConfigDict(extra="forbid")` on each (no shared exported base — Pitfall 3).

    Create `services/ai/schemas/events.py` with `ErrorPayload` = code:str, message:str, recoverable:bool (D-10) and `EventEnvelope` with `type: Literal["status","partial","result","error","done"]` (D-09 closed enum) + a `payload` field (typed loosely, e.g. dict/Any, since per-agent payloads land in P3/P4).

    Create `services/ai/schemas/domain.py` with MINIMAL compiling stubs (D-08): `RFQ`, `VendorResponse`, `ExtractionResult`, `ComparisonResult`. Each carries a couple of placeholder fields using the generic envelope across concrete types — ensure across the four stubs at least `Field[str]`, `Field[Decimal]` (from `decimal.Decimal`), and `Field[int]` appear so codegen monomorphizes FieldStr/FieldDecimal/FieldInt (the D-05 proof). Add `# ponytail:` comments marking these stub fields exist as the P2/P3/P4 contract placeholder.

    Create `services/ai/schemas/__init__.py` re-exporting all the above so `pydantic2ts --module schemas` discovers them.

    Write a pytest module covering the <behavior> cases above (RED first, then GREEN). Run `uv run pytest` from services/ai — green.
  </action>
  <verify>
    <automated>cd services/ai && uv run pytest tests/ -x && uv run python -c "from schemas import Field, Evidence, FlagStatus, EventEnvelope, ErrorPayload, RFQ, VendorResponse, ExtractionResult, ComparisonResult; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from schemas import Field, Evidence, FlagStatus, EventEnvelope, ErrorPayload, RFQ, VendorResponse, ExtractionResult, ComparisonResult"` exits 0.
    - `FlagStatus` has exactly the 5 members present/missing/unclear/conflicting/unsupported.
    - Constructing `Evidence(snippet="x", char_start=0, char_end=1, source_id="s", bogus=1)` raises ValidationError (extra="forbid").
    - Constructing `EventEnvelope(type="frobnicate", payload={})` raises ValidationError (closed Literal).
    - `uv run pytest tests/ -x` from services/ai exits 0 with the behavior cases passing.
  </acceptance_criteria>
  <done>The contract primitives (Field[T], Evidence, ConflictingValue[T], FlagStatus, EventEnvelope, ErrorPayload) and four domain stubs exist, validate the absence/conflict/closed-enum behaviors, and import cleanly under the schemas package; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Codegen script + drift-check test; generate and commit the TS contract</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Code Examples" → Codegen script + drift-check; Pitfall 1 importability, Pitfall 2 json2ts dependency, Pitfall 3 empty interface)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14 codegen script + drift-check test)
    - services/ai/schemas/__init__.py (the module pydantic2ts imports — from Task 1)
    - packages/shared-types/index.d.ts (the Plan 01 placeholder this overwrites)
    - CLAUDE.md §15 (TS is generated, never hand-mirrored; treat as build artifact)
  </read_first>
  <action>
    Create `services/ai/scripts/codegen.py` that calls `generate_typescript_defs("schemas", OUT, json2ts_cmd=<path to ./node_modules/.bin/json2ts at repo root>)` where OUT resolves to `packages/shared-types/index.d.ts` (resolve both paths relative to the repo root via `pathlib`, not the cwd — Pitfall 1). Run it from inside `services/ai` so the `schemas` package is importable (`uv run python scripts/codegen.py`). If an empty `export interface Strict {}` or any empty interface leaks despite per-model `extra="forbid"`, strip it in a tiny post-gen pass (Pitfall 3) — but verify per-model config alone is clean first.

    Generate the TS, inspect that `packages/shared-types/index.d.ts` contains concrete `FieldStr`, `FieldDecimal`, `FieldInt` interfaces (the D-05 monomorphization proof), each with a correctly-typed `value` and a `status` typed as the 5-state union, and NO `[k: string]: unknown` index signature. Commit this generated file (it replaces the Plan 01 placeholder).

    Create `services/ai/tests/test_codegen_drift.py` (D-14): regenerate the TS into a `tempfile.TemporaryDirectory`, read both, `assert generated == committed` with a clear failure message ("TS contract is stale — run scripts/codegen.py and commit packages/shared-types"). Run `uv run pytest` — green on a freshly generated file.

    Wire a convenience invocation: add a root `package.json` script (e.g. `"codegen": "..."`) OR document the `uv run python scripts/codegen.py` command — Claude's discretion per D-14. Keep it minimal.
  </action>
  <verify>
    <automated>cd services/ai && uv run python scripts/codegen.py && git diff --exit-code ../../packages/shared-types/index.d.ts && uv run pytest tests/test_codegen_drift.py -x && grep -q 'FieldStr' ../../packages/shared-types/index.d.ts && ! grep -q 'k: string' ../../packages/shared-types/index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - Running `uv run python scripts/codegen.py` then `git diff --exit-code packages/shared-types/index.d.ts` exits 0 (no drift; the committed file is freshly generated).
    - `packages/shared-types/index.d.ts` contains `export interface FieldStr` (and FieldDecimal, FieldInt) with a `status` field typed as the 5-state union; contains NO `[k: string]` index signature and NO empty `export interface Strict {}`.
    - `uv run pytest tests/test_codegen_drift.py` exits 0 on a freshly generated file.
    - The drift test FAILS (non-zero) when a pydantic field is added to a schema without rerunning codegen — verify by temporarily adding a field, running the test (expect red), then reverting.
  </acceptance_criteria>
  <done>codegen.py mechanically regenerates packages/shared-types/index.d.ts from the pydantic schemas; the committed TS shows clean monomorphized FieldStr/FieldDecimal/FieldInt with no index signature or empty base; the drift test passes when in-sync and goes red on drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pydantic schema → generated TS | Codegen shells out to the json2ts binary; the generated file is a committed build artifact consumed by apps/web |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Hand-edited / stale `packages/shared-types/index.d.ts` (contract drift) | mitigate | The D-14 drift-check test regenerates into a temp dir and asserts byte-equality with the committed file — a stale or hand-edited contract turns the test red, blocking the build (PLAT-02 enforced in code, not by convention). |
| T-02-02 | Tampering | json2ts binary invocation path | accept | json2ts is the version-pinned json-schema-to-typescript@15 installed at the repo root in Plan 01 (RESEARCH "Approved"); codegen resolves it by explicit path. No untrusted input flows into codegen — schemas are first-party source. Low risk. |
| T-02-03 | Information Disclosure | Schema content in generated TS | accept | Phase 1 schemas are structural stubs with no secrets or PII; nothing sensitive is emitted. |

No high-severity threats for compile-time contract generation. The realistic risk is silent contract drift, which the drift-check test mitigates by design.
</threat_model>

<verification>
- All contract primitives + domain stubs import cleanly under the `schemas` package; behavior tests green.
- `scripts/codegen.py` regenerates `packages/shared-types/index.d.ts`; `git diff --exit-code` is clean immediately after.
- Generated TS shows monomorphized FieldStr/FieldDecimal/FieldInt, the 5-state status union, no index signature, no empty base interface.
- Drift test passes in-sync and fails on an un-regenerated schema change.
</verification>

<success_criteria>
- ROADMAP success criterion 1 made TRUE: every contract field models absence as the first-class enum (no silent nullable).
- ROADMAP success criterion 2 made TRUE: the codegen script regenerates shared-types from pydantic and the drift test goes red on drift.
- The generic `Field[T]` is proven to produce clean concrete TS (D-05 verdict confirmed in the actual artifact).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
