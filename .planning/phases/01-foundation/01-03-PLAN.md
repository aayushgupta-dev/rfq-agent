---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/ai/llm/__init__.py
  - services/ai/llm/factory.py
  - services/ai/api/__init__.py
  - services/ai/api/app.py
  - services/ai/agents/__init__.py
  - services/ai/agents/_demo.py
  - services/ai/scripts/verify_access.py
  - services/ai/tests/test_llm_factory.py
  - services/ai/tests/test_sse_demo.py
  - .env.example
autonomous: false
user_setup:
  - service: openai
    why: "PLAT-03 requires a live ping confirming the org/key has gpt-5.4 / gpt-5.4-mini access before anything is built on it; both the standalone verify-access script and the FastAPI startup check need it."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys"
      - name: MODEL_REASONING
        source: "Set to the gpt-5.4 model id (confirm exact string)"
      - name: MODEL_CHEAP
        source: "Set to the gpt-5.4-mini model id (confirm exact string)"
requirements: [PLAT-03, PLAT-04]
must_haves:
  truths:
    - "A live ping confirms the org/key has gpt-5.4 / gpt-5.4-mini access before anything is built on it; it fails loudly with a clear message when access is missing."
    - "Callers ask get_llm('reasoning'|'cheap') for a tier, never a model string — model IDs are read from env, enforcing the gpt-5.4/mini-never-5.5 discipline in one place."
    - "A trivial LangGraph stream is observable end-to-end as {type, payload} SSE via curl -N, emitting the full taxonomy status->partial->result->done."
    - "Emitted SSE event type values are validated at test time against the EVENT_TYPES constant from schemas.events — the SSE emitter cannot drift from the closed taxonomy defined in Plan 02."
    - "FastAPI startup calls verify_access() and aborts with a clear gpt-5.4/gpt-5.4-mini access error when the org/key lacks access (D-16)."
    - "Unit tests do NOT require a live OpenAI key — they import modules and use the demo graph without booting the live server; the startup check only fires on actual uvicorn boot."
    - "Realizes decisions D-15: tier factory get_llm('reasoning'|'cheap') picking model by env-configured ID; D-16: standalone verify-access script + FastAPI startup check (both halves); D-09: proves the closed SSE event taxonomy with a trivial emitter."
  artifacts:
    - path: "services/ai/llm/factory.py"
      provides: "get_llm(tier) tier factory + verify_access()"
      contains: "def get_llm"
    - path: "services/ai/scripts/verify_access.py"
      provides: "Standalone CLI access ping (README setup step + PLAT-03 live proof)"
    - path: "services/ai/api/app.py"
      provides: "FastAPI app with lifespan calling verify_access() on boot + GET /stream/demo SSE route"
      contains: "EventSourceResponse"
    - path: "services/ai/agents/_demo.py"
      provides: "Trivial LangGraph graph emitting the SSE taxonomy via get_stream_writer"
      contains: "get_stream_writer"
  key_links:
    - from: "services/ai/agents/_demo.py"
      to: "services/ai/schemas/events.py EVENT_TYPES"
      via: "test_sse_demo.py imports EVENT_TYPES and asserts all emitted type values are members"
      pattern: "EVENT_TYPES"
    - from: "services/ai/api/app.py"
      to: "services/ai/agents/_demo.py"
      via: "graph.astream(stream_mode='custom') reshaped into EventSourceResponse"
      pattern: "stream_mode=\"custom\""
    - from: "services/ai/api/app.py"
      to: "services/ai/llm/factory.py verify_access"
      via: "FastAPI lifespan calls verify_access() on startup; aborts if org/key lacks access"
      pattern: "verify_access"
---

<objective>
Stand up the model-access and streaming spine and prove both live (D-15, D-16, D-09, PLAT-03, PLAT-04). Build the env-configured tier factory `get_llm('reasoning'|'cheap')`, a standalone `verify-access` ping script, a FastAPI app whose lifespan calls `verify_access()` on boot (aborting loudly on missing access), and a trivial LangGraph graph that streams the full `{type, payload}` SSE taxonomy observable via `curl -N`.

D-16 requires BOTH halves: (1) the standalone `verify-access` script (README setup step), AND (2) the FastAPI startup check that fails loudly if the org/key lacks `gpt-5.4` / `gpt-5.4-mini` access. The startup check is the live gate that catches the known account-specific blocker before any agent is built on it. Trade-off acknowledged: a transient OpenAI outage will prevent uvicorn from starting — this is the intended behaviour for a graded prototype where access proof matters more than resilience. No bypass env toggle is added (CLAUDE.md §2: no unrequested escape hatches). The SSE emitter validates its event types against the `EVENT_TYPES` constant imported from `schemas.events` so the demo cannot drift from the closed taxonomy defined in Plan 02.

Purpose: PLAT-03's whole point is to catch the known account-specific gpt-5.4 access blocker BEFORE any agent is built on it; PLAT-04 proves the streaming spine the P3/P4 agents reuse. The tier factory enforces the model-tier discipline (gpt-5.4 reasoning / mini cheap, never 5.5) in one place.

Output: `services/ai/llm/factory.py`, `services/ai/scripts/verify_access.py`, `services/ai/api/app.py` (SSE route + lifespan startup gate), `services/ai/agents/_demo.py`, an `.env.example`, and a curl-observable SSE stream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Verified v1 API surface from RESEARCH.md "Code Examples". LangGraph is 1.x and langchain-openai is 1.3.x —
     training data describing v0.x is STALE. Use these exact imports/signatures, no exploration: -->

get_llm(tier: Literal["reasoning","cheap"]) -> chat model
  reads MODEL_REASONING / MODEL_CHEAP from env; wraps init_chat_model(model_id)
  import: from langchain.chat_models import init_chat_model

verify_access() -> None
  for each tier: get_llm(tier).invoke(<minimal ping>); on failure re-raise
  RuntimeError("No access to {tier} model ({model_id}): {e}")

LangGraph streaming (v1):
  from langgraph.config import get_stream_writer
  from langgraph.graph import StateGraph, START, END
  inside a node: w = get_stream_writer(); w({"type": "status", "payload": {...}})
  graph.astream(inputs, stream_mode="custom")  -> yields the {"type","payload"} dicts directly

SSE:
  from sse_starlette import EventSourceResponse
  yield {"data": json.dumps(chunk)} per event; final event {"type":"done","payload":{}}

FastAPI lifespan (for startup check):
  from contextlib import asynccontextmanager
  @asynccontextmanager
  async def lifespan(app: FastAPI):
      verify_access()   # raises / aborts startup on missing access
      yield
  app = FastAPI(lifespan=lifespan)

EVENT_TYPES (from schemas.events — available once Plan 02 lands in this wave):
  EVENT_TYPES: tuple[str, ...] = ("status", "partial", "result", "error", "done")
  If schemas.events is not yet importable at test time (wave parallelism), define a local copy
  in test_sse_demo.py with a comment marking it as "must match schemas.events.EVENT_TYPES".
</interfaces>

<notes>
- BLOCKER (RESEARCH "Missing dependencies with no fallback" + Environment Availability): `.env` is currently EMPTY. The access ping CANNOT pass until a real OPENAI_API_KEY + MODEL_REASONING (gpt-5.4) + MODEL_CHEAP (gpt-5.4-mini) are set. This plan is `autonomous: false` — Task 0 is a blocking human checkpoint to supply the key BEFORE the ping runs.
- `.env` location (RESEARCH Open Question 1): ONE `.env` at repo ROOT; services/ai loads it from one level up. `python-dotenv` is installed in Plan 01 (confirmed in 01-01 acceptance criteria). Use `load_dotenv(Path(__file__).resolve().parents[2] / ".env")` (or equivalent path to repo root `.env`) in `factory.py`. Create `.env.example` documenting the three vars; do NOT commit a real `.env`.
- FastAPI startup check (D-16 — LOCKED): call `verify_access()` inside the FastAPI `lifespan` handler so the server aborts on boot with a clear message if the org/key lacks gpt-5.4 / gpt-5.4-mini access. This is the intended behaviour — a missing-access finding must be loud and early. No bypass toggle is added (CLAUDE.md §2: no unrequested escape hatches). Acknowledged trade-off: a transient OpenAI outage prevents server start; for a graded prototype this is the right call.
- Unit tests must NOT require a live key: `test_sse_demo.py` uses FastAPI's `TestClient` (sync) which does NOT trigger the lifespan on its own in testing mode — use `with TestClient(app) as client:` (which DOES trigger lifespan) only if a live key is available, or use `TestClient(app, raise_server_exceptions=False)` without context-manager for the offline unit path. The clean separation: `uv run pytest` runs offline (no live key needed); `uv run uvicorn` boots live (startup check fires with a real key). Document this distinction explicitly in the test file.
- SSE taxonomy drift prevention (MEDIUM review fix): in `test_sse_demo.py`, import `EVENT_TYPES` from `services.ai.schemas.events` and assert every emitted event's `type` value is a member of that tuple. If the test runs before Plan 02's schemas are importable (wave 2 parallelism), define a local `_EXPECTED_TYPES = ("status", "partial", "result", "error", "done")` with a comment `# must match schemas.events.EVENT_TYPES`. This ensures the SSE emitter and the schema enum cannot drift.
- Ping param surface (RESEARCH Pitfall 5 / Assumption A2 - MEDIUM/LOW confidence): gpt-5.4 reasoning models may reject `temperature`/`max_tokens` or use `max_completion_tokens`. Keep the ping MINIMAL (a tiny invoke); CONFIRM the exact param name against the model before hardcoding any cap (CLAUDE.md: confirm model strings/params, don't assume). On failure, catch and re-raise with the clear "org/key lacks access to {model}" message.
- Error categorization: the ping failure message must distinguish a true access-denied/unknown-model error from a request-param-rejection error (Pitfall 5 / A2). A param rejection (e.g. gpt-5.4 rejecting `temperature`) must NOT be reported as "no access" — that would be a false-negative on the exact check PLAT-03 exists to make trustworthy.
- Security: the ping and startup check must NEVER echo/log the API key. Log only the model IDs and a pass/fail.
- Demo graph location: `services/ai/agents/_demo.py`, kept next to the route it proves. Trivial node emitting status->partial->result; the route appends the final `done`.
- Do NOT add CORS or proxy-buffering config — that is Phase 5 / SHIP-01 (CONTEXT Deferred Ideas). The curl -N proof runs locally, no CORS needed.
- This plan shares NO files with Plan 02 (schemas/, scripts/codegen.py, packages/shared-types) — it runs in parallel with Plan 02 in Wave 2.
- Stop the uvicorn process after capturing `curl -N` in the live verification step (LOW review fix).
</notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (checkpoint): Supply live OpenAI credentials with gpt-5.4 / gpt-5.4-mini access</name>
  <what-built>Nothing yet - this is the PLAT-03 prerequisite. The access ping is about to be built and it requires live OpenAI credentials with gpt-5.4 / gpt-5.4-mini access. The repo `.env` is currently empty.</what-built>
  <action>This is a blocking human-action checkpoint, not autonomous work. The executor pauses here and does NOT proceed to Task 1 until the user confirms the credentials are set, because the PLAT-03 ping in the following tasks will fail without a live key (RESEARCH: the BLOCKER). Present the how-to-verify steps to the user and wait for the resume signal.</action>
  <how-to-verify>
    1. Open the repo-root `.env` (gitignored).
    2. Set `OPENAI_API_KEY=<your key>`.
    3. Set `MODEL_REASONING=<the exact gpt-5.4 model id>` and `MODEL_CHEAP=<the exact gpt-5.4-mini model id>` (confirm the exact strings in your OpenAI account - do not assume).
    4. Confirm the OpenAI org/key actually has access to those two models (this is the blocker PLAT-03 exists to catch).
  </how-to-verify>
  <verify><human-check>User confirms the three env vars are set in the root .env and the org has gpt-5.4 / gpt-5.4-mini access.</human-check></verify>
  <done>The root .env has OPENAI_API_KEY, MODEL_REASONING, MODEL_CHEAP set and the org/key has confirmed access (or the user has reported that access is missing - the intended PLAT-03 finding - and the phase stops here).</done>
  <resume-signal>Type "key set" once the three env vars are in the root .env and the org has gpt-5.4 / gpt-5.4-mini access. If you do NOT yet have gpt-5.4 access, say so - that is exactly the finding PLAT-03 is meant to surface, and we stop here rather than build on an unavailable model.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: LLM tier factory + standalone access-verify script</name>
  <files>services/ai/llm/factory.py, services/ai/llm/__init__.py, services/ai/scripts/verify_access.py, services/ai/tests/test_llm_factory.py, .env.example</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Code Examples" -> LLM tier factory + access ping; Pitfall 4 stale v0 API; Pitfall 5 ping params; Assumption A2)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-15 tier factory, D-16 standalone verify-access script AND FastAPI startup check — both halves required)
    - services/ai/pyproject.toml (confirm langchain-openai / langchain / python-dotenv installed; from Plan 01)
    - .env (root - currently empty; populated by the checkpoint above)
    - CLAUDE.md §5 (model-tier discipline: gpt-5.4 reasoning / mini cheap, never 5.5), §8/§15 (never log secrets)
  </read_first>
  <behavior>
    - get_llm("reasoning") reads MODEL_REASONING from env and returns a chat model configured for that id.
    - get_llm("cheap") reads MODEL_CHEAP from env and returns a chat model configured for that id.
    - get_llm with an unknown tier raises (KeyError / ValueError) - callers cannot pass an arbitrary model string.
    - When a tier's env var is unset, get_llm raises a clear error (not a silent None).
    - verify_access() succeeds (no raise) when both models are reachable; raises RuntimeError naming the failing tier+model id when access is missing - and the error/log never contains the API key.
  </behavior>
  <action>
    Create `services/ai/llm/factory.py` with `get_llm(tier: Literal["reasoning","cheap"])` mapping reasoning->MODEL_REASONING, cheap->MODEL_CHEAP (read from os.environ, with `load_dotenv` loading the repo-root `.env` at module import time — use `python-dotenv` installed in Plan 01, resolving the `.env` path as `Path(__file__).resolve().parents[3] / ".env"` or the equivalent path from llm/factory.py to the repo root `.env`). Return `init_chat_model(model_id)` (langchain v1 — `from langchain.chat_models import init_chat_model`). Unknown tier or unset env var -> clear raise (D-15). Add `verify_access() -> None` that, for both tiers, does a minimal `.invoke(<tiny ping>)` and on exception re-raises `RuntimeError("No access to {tier} model ({model_id}): {e}")` — confirm the minimal ping param surface against the model first (Pitfall 5); never include the API key in any message or log. Error message must distinguish access-denied from param-rejection (Pitfall 5 / A2: gpt-5.4 may reject `temperature`/`max_tokens`; catching a param error and reporting "no access" is a false-negative — inspect the exception type/message to categorize correctly).

    Add `services/ai/llm/__init__.py` re-exporting `get_llm`, `verify_access`.

    Create `services/ai/scripts/verify_access.py` — a standalone CLI that calls `verify_access()`, prints the two model IDs being checked and a clear pass message on success, exits non-zero with the clear failure message on missing access (D-16, README setup step, PLAT-03 live proof). Create `.env.example` at repo root documenting OPENAI_API_KEY / MODEL_REASONING / MODEL_CHEAP (no real values).

    Write `services/ai/tests/test_llm_factory.py` covering the behavior cases. Mock the model invoke for the unit tests (do NOT make the unit suite require a live key — the LIVE proof is the standalone script run in this task's verification). Run `uv run pytest` — green.

    Then run the LIVE script `uv run python scripts/verify_access.py` with the real key (set in the checkpoint) — it must exit 0 and print both model IDs.
  </action>
  <verify>
    <automated>cd services/ai && uv run pytest tests/test_llm_factory.py -x && uv run python scripts/verify_access.py</automated>
  </verify>
  <acceptance_criteria>
    - `get_llm("reasoning")` and `get_llm("cheap")` return configured chat models reading MODEL_REASONING/MODEL_CHEAP; `get_llm("bogus")` raises.
    - `uv run pytest tests/test_llm_factory.py` exits 0 (factory unit tests mock the invoke; no live key required for the unit suite).
    - The standalone `uv run python scripts/verify_access.py` exits 0 with a real key and prints the two model IDs; exits non-zero with a clear "no access to {model}" message when access is missing (verify the failure path by temporarily pointing MODEL_CHEAP at a nonexistent id).
    - The ping failure message distinguishes a true access-denied/unknown-model error from a request-param-rejection error (the Pitfall 5 / A2 risk: gpt-5.4 rejecting `temperature`/`max_tokens` vs requiring `max_completion_tokens`). A param rejection must NOT be reported as "no access" — otherwise PLAT-03 yields a false-negative on the exact check it exists to make trustworthy.
    - No code path logs or prints the value of OPENAI_API_KEY.
    - `.env.example` documents the three vars; no real `.env` is committed.
    - `services/ai/pyproject.toml` includes `python-dotenv` as a runtime dep (confirmed from Plan 01 — this plan's factory uses it to load the root `.env`).
  </acceptance_criteria>
  <done>The tier factory enforces tier-not-model-string access reading IDs from env via dotenv (root .env); the standalone verify-access script confirms live gpt-5.4/gpt-5.4-mini access and fails loudly without it, never leaking the key; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastAPI app with lifespan startup gate + trivial LangGraph SSE demo with taxonomy validation</name>
  <files>services/ai/api/app.py, services/ai/api/__init__.py, services/ai/agents/_demo.py, services/ai/agents/__init__.py, services/ai/tests/test_sse_demo.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Code Examples" -> LangGraph trivial graph -> SSE; FastAPI startup lifespan; Pitfall 4 v1 stream API; System Architecture Diagram)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 SSE taxonomy status|partial|result|error|done, D-16 BOTH halves: standalone script AND FastAPI startup check; Claude's discretion on demo graph)
    - services/ai/llm/factory.py (verify_access - from Task 1)
    - services/ai/schemas/events.py (EVENT_TYPES constant — import this for taxonomy validation in the test; if Plan 02 has not yet run, use a local copy as documented in notes)
    - CLAUDE.md §5 (FastAPI emits SSE, never buffer-and-return), §15 (no CORS/buffering work - that's P5)
  </read_first>
  <behavior>
    - GET /stream/demo streams Server-Sent Events; the first event is type "status", followed by "partial", then "result", and the final event is type "done".
    - Every SSE event line is `data: {json}` where json has exactly the keys `type` and `payload`.
    - The "type" value is always one of the values in EVENT_TYPES from schemas.events (status|partial|result|error|done).
    - The FastAPI app lifespan calls verify_access() on boot; if the org/key lacks gpt-5.4 / gpt-5.4-mini access, startup aborts with a clear error message naming the missing model (D-16).
    - Unit tests (uv run pytest) pass WITHOUT a live OpenAI key — the test does not trigger the lifespan startup check (TestClient used without context manager, or lifespan bypassed in test config); the startup check only fires on actual uvicorn boot.
  </behavior>
  <action>
    Create `services/ai/agents/_demo.py` (D-09 discretion): a trivial LangGraph `StateGraph` with one node that uses `get_stream_writer()` (langgraph v1 — `from langgraph.config import get_stream_writer`) to emit `{"type":"status","payload":{...}}`, then `{"type":"partial","payload":{...}}`, then `{"type":"result","payload":{...}}`; compiled graph with START->node->END. Add `services/ai/agents/__init__.py`.

    Create `services/ai/api/app.py` with a FastAPI app that includes a lifespan handler calling `verify_access()` on startup (D-16 — LOCKED, no bypass toggle). Use the `asynccontextmanager` lifespan pattern: `from contextlib import asynccontextmanager`; inside the lifespan, call `verify_access()` synchronously before `yield` — if it raises, the exception propagates and uvicorn aborts startup with the clear "No access to {tier} model ({model_id})" message. Add a `GET /stream/demo` route that `astream`s the demo graph with `stream_mode="custom"`, reshaping each chunk into an `sse_starlette` `EventSourceResponse` (`yield {"data": json.dumps(chunk)}`), then yields a final `{"type":"done","payload":{}}` event. Do NOT add CORS/proxy-buffering (Phase 5).

    Write `services/ai/tests/test_sse_demo.py` using FastAPI's `TestClient` WITHOUT the context manager (i.e. `client = TestClient(app)` not `with TestClient(app) as client:`) so the lifespan is not triggered and the test runs offline without a live key. Hit `/stream/demo` and assert the event sequence/shape from the behavior block. Validate that every emitted `type` value is a member of `EVENT_TYPES` — import as `from schemas.events import EVENT_TYPES`. If `schemas.events` is not yet importable (wave-2 parallelism), define a local `_EXPECTED_TYPES = ("status", "partial", "result", "error", "done")` with a comment `# must match schemas.events.EVENT_TYPES — reconcile after Plan 02 runs`. Add a comment at the top of the test file explaining the lifespan bypass: `# TestClient instantiated without context manager to skip lifespan (startup access check) — no live key required for unit tests. Run uvicorn directly to exercise the startup gate.` The test does not require a live model call (the demo graph emits hardcoded taxonomy events; no LLM call on the stream path). Run `uv run pytest` — green.

    Finally, LIVE proof (PLAT-04): start the server (`uv run uvicorn api.app:app --port 8000`), capture `curl -N http://localhost:8000/stream/demo` confirming the `data: {"type":...}` lines in taxonomy order ending in `done`, then stop the uvicorn process (kill the process or ctrl-C capture). Record the output in the SUMMARY. Note: uvicorn start triggers the lifespan startup gate — it will only succeed with a valid key and confirmed gpt-5.4/gpt-5.4-mini access (which the checkpoint and Task 1 have already confirmed).
  </action>
  <verify>
    <automated>cd services/ai && uv run pytest tests/test_sse_demo.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sse_demo.py` exits 0 with the event-sequence/shape assertions passing (status->partial->result->done, each `{type,payload}`, type in the closed taxonomy). No live key required for the unit test run.
    - The test asserts every emitted `type` value is in EVENT_TYPES (either imported from schemas.events or from a local copy marked to reconcile).
    - `grep -n "verify_access" services/ai/api/app.py` shows verify_access is called inside the lifespan handler (not at module import time, not inside the route — inside lifespan before yield).
    - With a live key, `uv run uvicorn api.app:app --port 8000` starts successfully (startup gate passes), and `curl -N http://localhost:8000/stream/demo` emits lines `data: {"type":"status",...}` ... ending with `data: {"type":"done",...}` (capture the output in the SUMMARY as the PLAT-04 proof); the uvicorn process is stopped after capturing.
    - With an invalid/missing key, uvicorn startup aborts with a clear "No access to {tier} model ({model_id})" message (verify by temporarily unsetting MODEL_CHEAP).
    - No CORS or proxy-buffering config is present (deferred to P5).
  </acceptance_criteria>
  <done>A trivial LangGraph graph streams the full {type,payload} SSE taxonomy through FastAPI/sse-starlette, observable via curl -N; the FastAPI lifespan gate calls verify_access() and aborts loudly on missing gpt-5.4/gpt-5.4-mini access (D-16); unit tests run offline without a live key; SSE taxonomy validated against EVENT_TYPES; tests green, no premature P5 deploy config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env / `.env` -> AI service | OPENAI_API_KEY (a secret) enters the service; the access ping crosses out to the OpenAI API |
| HTTP client -> GET /stream/demo | unauthenticated local request triggers a streamed graph run (Phase 1 is local-only; no auth by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | OPENAI_API_KEY in `.env`, ping errors, and startup logs | mitigate | `.env` is gitignored (verified) and never committed (`.env.example` only); the factory, verify_access script, lifespan handler, and any log lines reference only model IDs + pass/fail — code paths never interpolate the API key into any message or log. |
| T-03-02 | Denial of Service | unauthenticated GET /stream/demo runs a graph + (in P3) model calls | accept | Phase 1 demo is local-only and emits hardcoded taxonomy events with no model call on the stream path; no public exposure until P5 (which adds CORS). Rate-limiting/auth is out of scope for a single-buyer local prototype (REQUIREMENTS Out of Scope: auth). |
| T-03-03 | Spoofing | wrong/over-tier model silently used (e.g. gpt-5.5) | mitigate | The tier factory is the single call site reading MODEL_REASONING/MODEL_CHEAP from env; callers pass a tier literal, not a model string — enforcing the gpt-5.4/mini-never-5.5 discipline in one place (D-15). |
| T-03-SC | Tampering | langchain/langgraph/sse-starlette installs | mitigate | Installed in Plan 01 from the version-pinned, RESEARCH-"Approved" stack; reproducibility via committed lockfile. No new installs in this plan. |

The headline risk for this plan is secret leakage of the API key, mitigated by gitignored `.env` + a strict "never log the key" rule on every ping/startup code path.
</threat_model>

<verification>
- Tier factory returns env-configured models per tier and rejects unknown tiers/unset env; python-dotenv loads root .env.
- Standalone verify-access script exits 0 with live access, non-zero with a clear message without it; never leaks the key.
- FastAPI lifespan calls verify_access() on boot; server aborts with a clear gpt-5.4/gpt-5.4-mini access error on missing access (D-16); grep confirms the wiring.
- Unit tests (uv run pytest) pass without a live key — TestClient bypasses lifespan; the startup check only fires on actual uvicorn boot.
- /stream/demo emits the full taxonomy as `{type,payload}` SSE, observable via curl -N; uvicorn stopped after capture.
- SSE test validates emitted type values against EVENT_TYPES from schemas.events.
- No CORS/buffering config present (P5 boundary respected).
</verification>

<success_criteria>
- ROADMAP success criterion 3 made TRUE: a live ping confirms gpt-5.4 / gpt-5.4-mini access (and surfaces a missing-access account as the intended finding) — proven by BOTH the standalone script AND the FastAPI startup gate (D-16 requires both).
- ROADMAP success criterion 4 made TRUE: a minimal LangGraph stream is observable as `{type,payload}` SSE via curl -N.
- The model-tier discipline is enforced in one place (the factory).
- SSE emitter cannot drift from the closed taxonomy in schemas.events (EVENT_TYPES validates the test).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Include the captured `curl -N /stream/demo` output as the PLAT-04 proof and the verify-access output as the PLAT-03 proof.
</output>
